var $o = Object.defineProperty, Lo = Object.defineProperties;
var Ao = Object.getOwnPropertyDescriptors;
var on = Object.getOwnPropertySymbols;
var Zo = Object.prototype.hasOwnProperty, Do = Object.prototype.propertyIsEnumerable;
var ie = Math.pow, sn = (t, e, n) => e in t ? $o(t, e, { enumerable: !0, configurable: !0, writable: !0, value: n }) : t[e] = n, oe = (t, e) => {
  for (var n in e || (e = {}))
    Zo.call(e, n) && sn(t, n, e[n]);
  if (on)
    for (var n of on(e))
      Do.call(e, n) && sn(t, n, e[n]);
  return t;
}, Re = (t, e) => Lo(t, Ao(e));
var ve = (t, e, n) => new Promise((o, i) => {
  var s = (u) => {
    try {
      c(n.next(u));
    } catch (h) {
      i(h);
    }
  }, a = (u) => {
    try {
      c(n.throw(u));
    } catch (h) {
      i(h);
    }
  }, c = (u) => u.done ? o(u.value) : Promise.resolve(u.value).then(s, a);
  c((n = n.apply(t, e)).next());
});
import { isReactive as No, reactive as Ee, provide as Me, inject as Ce, watch as F, toRef as we, useId as jo, watchEffect as ue, computed as C, unref as M, ref as B, onMounted as Be, onUnmounted as Xe, nextTick as Zt, defineComponent as U, createElementBlock as $, openBlock as S, createCommentVNode as ae, createBlock as H, resolveDynamicComponent as In, Fragment as X, renderList as Q, mergeProps as ne, renderSlot as K, withModifiers as ge, normalizeClass as ce, normalizeStyle as Ge, useAttrs as Bo, createElementVNode as $n, createTextVNode as Ro, toDisplayString as rn, createVNode as $e, withCtx as te, useSlots as st, createSlots as Dt, normalizeProps as Ot, guardReactiveProps as zt, TransitionGroup as Fe, toHandlers as Vo, readonly as Ve } from "vue";
import { isEqual as Pe, round as an, merge as Ln, mergeWith as Wo, isPlainObject as Ho, debounce as _o, uniq as Uo, chunk as Nt } from "lodash-es";
const Fo = [
  "paths",
  "node-labels",
  "nodes",
  "focusring",
  "edge-labels",
  "edges",
  "base",
  "grid",
  "background",
  "root"
];
function ke(t) {
  return No(t) ? t : Ee(t);
}
function q(t, e = "Parameter") {
  if (t == null)
    throw new Error(`${e} is null`);
  return t;
}
const An = /* @__PURE__ */ Symbol("containers");
function qo(t) {
  Me(An, t);
}
function jt() {
  const t = q(Ce(An), "containers");
  return {
    container: t.container,
    svg: t.svg,
    viewport: t.viewport,
    svgPanZoom: t.svgPanZoom
  };
}
class I {
  static value(e, n) {
    return e instanceof Function ? e(n) : e;
  }
  static values(e, n) {
    return Object.values(e).filter((o) => o instanceof Function).length === 0 ? e : Object.fromEntries(
      Object.entries(e).map(([o, i]) => [o, i instanceof Function ? i(n) : i])
    );
  }
}
var N = /* @__PURE__ */ ((t) => (t.CENTER = "center", t.NORTH = "north", t.NORTH_EAST = "north-east", t.EAST = "east", t.SOUTH_EAST = "south-east", t.SOUTH = "south", t.SOUTH_WEST = "south-west", t.WEST = "west", t.NORTH_WEST = "north-west", t))(N || {});
function bt(t) {
  const e = {};
  return Object.assign(e, t(e));
}
function qr(t) {
  return t;
}
function Xr(t) {
  return t;
}
function ln(t, e, n) {
  const o = I.values(e.normal, t);
  return o.type == "circle" ? {
    width: o.radius * 2 * n,
    height: o.radius * 2 * n
  } : {
    width: o.width * n,
    height: o.height * n
  };
}
function Xo(t, e, n, o) {
  const s = Math.abs(t.x - n.x) < e.width / 2 + o.width / 2, c = Math.abs(t.y - n.y) < e.height / 2 + o.height / 2;
  return s && c;
}
function Ke(t, e) {
  let n = 0;
  return e === 1 || t === void 0 || t === "none" ? n = t != null ? t : 0 : typeof t == "string" ? n = t.split(/\s+/).map((o) => parseInt(o) * e).filter((o) => !isNaN(o)).join(" ") : n = t * e, n && n !== "0" ? n : void 0;
}
function rt(t) {
  let e = 0;
  if (t === void 0 || t === "none")
    e = 0;
  else if (typeof t == "string") {
    const n = t.split(/\s+/).map((o) => parseInt(o)).filter((o) => !isNaN(o));
    n.length % 2 === 0 ? e = n.reduce((o, i) => o + i, 0) : e = n.reduce((o, i) => o + i, 0) * 2;
  } else
    e = t * 2;
  return e;
}
const cn = 20;
class Zn {
  activate(e) {
    const { nodePositions: n, nodes: o, configs: i, emitter: s, scale: a, svgPanZoom: c } = e, u = (r) => {
      for (const [l, d] of Object.entries(r)) {
        const b = this.getOrCreateNodePosition(n, l);
        this.setNodePosition(b, d);
      }
    }, h = (r) => {
      const l = r.filter((x) => !(x in n.value)), d = c.getViewArea(), b = a.value;
      for (const x of l) {
        const m = o.value[x], g = ln(m, i.node, b), w = oe({}, d.center);
        for (; ; ) {
          let p = !1;
          for (const [v, y] of Object.entries(n.value)) {
            if (x === v) continue;
            const k = o.value[v];
            if (!k) continue;
            const O = ln(k, i.node, b);
            if (p = Xo(w, g, y, O), p)
              break;
          }
          if (p)
            w.x += g.width + cn * b, w.x + g.width / 2 > d.box.right && (w.x = d.center.x, w.y += g.height + cn * b);
          else
            break;
        }
        const E = this.getOrCreateNodePosition(n, x);
        this.setNodePosition(E, w);
      }
    };
    h(Object.keys(o.value));
    const f = F(
      () => Pe(new Set(Object.keys(o.value)), new Set(Object.keys(n.value))),
      (r) => {
        r || h(Object.keys(o.value));
      }
    );
    s.on("node:dragstart", u), s.on("node:pointermove", u), s.on("node:dragend", u), this.onDeactivate = () => {
      f(), s.off("node:dragstart", u), s.off("node:pointermove", u), s.off("node:dragend", u);
    };
  }
  deactivate() {
    this.onDeactivate && this.onDeactivate();
  }
  setNodePosition(e, n) {
    e.value.x = an(n.x, 3), e.value.y = an(n.y, 3);
  }
  getOrCreateNodePosition(e, n) {
    const o = we(e.value, n);
    return o.value || (o.value = { x: 0, y: 0 }), o;
  }
}
function Dn() {
  return {
    view: {
      scalingObjects: !1,
      panEnabled: !0,
      zoomEnabled: !0,
      minZoomLevel: 0.1,
      maxZoomLevel: 64,
      doubleClickZoomEnabled: !0,
      mouseWheelZoomEnabled: !0,
      boxSelectionEnabled: !1,
      autoPanAndZoomOnLoad: "center-content",
      fitContentMargin: "8%",
      autoPanOnResize: !0,
      layoutHandler: new Zn(),
      onSvgPanZoomInitialized: void 0,
      grid: {
        visible: !1,
        interval: 10,
        thickIncrements: 5,
        line: {
          color: "#e0e0e0",
          width: 1,
          dasharray: 1
        },
        thick: {
          color: "#cccccc",
          width: 1,
          dasharray: 0
        }
      },
      selection: {
        box: {
          color: "#0000ff20",
          strokeWidth: 1,
          strokeColor: "#aaaaff",
          strokeDasharray: 0
        },
        detector: (t) => {
          const e = /Mac OS/.test(navigator.userAgent) ? t.metaKey : t.ctrlKey;
          return t.type === "keydown" ? e : !e;
        }
      },
      builtInLayerOrder: [],
      onBeforeInitialDisplay: void 0
    },
    node: bt((t) => ({
      normal: {
        type: "circle",
        radius: 16,
        // for rect -->
        width: 32,
        height: 32,
        borderRadius: 4,
        // <-- for rect
        color: "#4466cc",
        strokeWidth: 0,
        strokeColor: "#000000",
        strokeDasharray: 0
      },
      hover: {
        type: (e) => I.value(t.normal.type, e),
        radius: (e) => {
          var n;
          return ((n = I.value(t.normal.radius, e)) != null ? n : 0) + 2;
        },
        width: (e) => {
          var n;
          return ((n = I.value(t.normal.width, e)) != null ? n : 0) + 2;
        },
        height: (e) => {
          var n;
          return ((n = I.value(t.normal.height, e)) != null ? n : 0) + 2;
        },
        borderRadius: (e) => {
          var n;
          return (n = I.value(t.normal.borderRadius, e)) != null ? n : 0;
        },
        strokeWidth: (e) => I.value(t.normal.strokeWidth, e),
        strokeColor: (e) => I.value(t.normal.strokeColor, e),
        strokeDasharray: (e) => I.value(t.normal.strokeDasharray, e),
        color: "#3355bb"
      },
      selected: void 0,
      draggable: !0,
      selectable: !1,
      label: {
        visible: !0,
        fontFamily: void 0,
        fontSize: 11,
        lineHeight: 1.1,
        color: "#000000",
        background: void 0,
        // background: {
        //   visible: true,
        //   color: "#ffffff",
        //   padding: {
        //     vertical: 1,
        //     horizontal: 4,
        //   },
        //   borderRadius: 2
        // },
        margin: 4,
        direction: N.SOUTH,
        directionAutoAdjustment: !1,
        text: "name",
        handleNodeEvents: !0
      },
      focusring: {
        visible: !0,
        width: 4,
        padding: 3,
        color: "#eebb00"
      },
      zOrder: {
        enabled: !1,
        zIndex: 0,
        bringToFrontOnHover: !0,
        bringToFrontOnSelected: !0
      },
      transition: void 0
    })),
    edge: bt((t) => ({
      normal: {
        width: 2,
        color: "#4466cc",
        dasharray: 0,
        linecap: "butt",
        animate: !1,
        animationSpeed: 50
      },
      hover: {
        width: (e) => I.value(t.normal.width, e) + 1,
        color: "#3355bb",
        dasharray: (e) => I.value(t.normal.dasharray, e),
        linecap: (e) => I.value(t.normal.linecap, e),
        animate: (e) => I.value(t.normal.animate, e),
        animationSpeed: (e) => I.value(t.normal.animationSpeed, e)
      },
      selected: {
        width: (e) => I.value(t.normal.width, e) + 1,
        color: "#dd8800",
        dasharray: (e) => {
          const n = I.value(t.normal.width, e);
          return `${n * 1.5} ${n * 2}`;
        },
        linecap: (e) => I.value(t.normal.linecap, e),
        animate: (e) => I.value(t.normal.animate, e),
        animationSpeed: (e) => I.value(t.normal.animationSpeed, e)
      },
      selectable: !1,
      gap: 3,
      type: "straight",
      marker: {
        source: {
          type: "none",
          width: 5,
          height: 5,
          margin: -1,
          offset: 0,
          units: "strokeWidth",
          color: null
        },
        target: {
          type: "none",
          width: 5,
          height: 5,
          margin: -1,
          offset: 0,
          units: "strokeWidth",
          color: null
        }
      },
      margin: null,
      summarize: (e, n) => n.edge.type == "curve" ? !1 : null,
      summarized: {
        label: {
          fontSize: 10,
          lineHeight: 1,
          color: "#4466cc"
        },
        shape: {
          type: "rect",
          // for circle -->
          radius: 6,
          // <-- for circle
          width: 12,
          height: 12,
          borderRadius: 3,
          color: "#ffffff",
          strokeWidth: 1,
          strokeColor: "#4466cc",
          strokeDasharray: void 0
        },
        stroke: {
          width: 5,
          color: "#4466cc",
          dasharray: void 0,
          linecap: void 0,
          animate: !1,
          animationSpeed: 50
        }
      },
      selfLoop: {
        radius: 12,
        isClockwise: !0,
        offset: 10,
        angle: 270
      },
      keepOrder: "clock",
      label: {
        fontFamily: void 0,
        fontSize: 11,
        lineHeight: 1.1,
        color: "#000000",
        background: void 0,
        // background: {
        //   visible: true,
        //   color: "#ffffff",
        //   padding: {
        //     vertical: 1,
        //     horizontal: 4,
        //   },
        //   borderRadius: 2
        // },
        margin: 4,
        padding: 4
      },
      zOrder: {
        enabled: !1,
        zIndex: 0,
        bringToFrontOnHover: !0,
        bringToFrontOnSelected: !0
      }
    })),
    path: bt((t) => ({
      visible: !1,
      clickable: !1,
      hoverable: !1,
      curveInNode: !1,
      end: "centerOfNode",
      margin: 0,
      // @Deprecated
      path: Ee({
        width: 6,
        color: (e) => {
          const n = [
            "#d5000088",
            "#c5116288",
            "#aa00ff88",
            "#6200ea88",
            "#304ffe88",
            "#2962ff88",
            "#0091ea88",
            "#00b8d488",
            "#00bfa588",
            "#00c85388",
            "#64dd1788",
            "#aeea0088",
            "#ffd60088",
            "#ffab0088",
            "#ff6d0088",
            "#dd2c0088"
          ], o = e.edges.map(
            (i) => i.split("").reduce((s, a) => (s = (s << 5) - s + a.charCodeAt(0), s & s), 0)
          ).reduce((i, s) => i + s, 0);
          return n[Math.abs(o) % n.length];
        },
        dasharray: void 0,
        linecap: "round",
        linejoin: "round",
        animate: !1,
        animationSpeed: 50
      }),
      normal: {
        width: (e) => I.value(t.path.width, e),
        color: (e) => I.value(t.path.color, e),
        dasharray: (e) => I.value(t.path.dasharray, e),
        linecap: (e) => I.value(t.path.linecap, e),
        linejoin: (e) => I.value(t.path.linejoin, e),
        animate: (e) => I.value(t.path.animate, e),
        animationSpeed: (e) => I.value(t.path.animationSpeed, e)
      },
      hover: {
        width: (e) => I.value(t.normal.width, e) + 2,
        color: (e) => I.value(t.normal.color, e),
        dasharray: (e) => I.value(t.normal.dasharray, e),
        linecap: (e) => I.value(t.normal.linecap, e),
        linejoin: (e) => I.value(t.normal.linejoin, e),
        animate: (e) => I.value(t.normal.animate, e),
        animationSpeed: (e) => I.value(t.normal.animationSpeed, e)
      },
      selected: {
        width: (e) => I.value(t.normal.width, e) + 2,
        color: (e) => I.value(t.normal.color, e),
        dasharray: "6 12",
        linecap: (e) => I.value(t.normal.linecap, e),
        linejoin: (e) => I.value(t.normal.linejoin, e),
        animate: (e) => I.value(t.normal.animate, e),
        animationSpeed: (e) => I.value(t.normal.animationSpeed, e)
      },
      selectable: !1,
      zOrder: {
        enabled: !1,
        zIndex: 0,
        bringToFrontOnHover: !0,
        bringToFrontOnSelected: !0
      },
      transition: void 0
    }))
  };
}
function Gr(t) {
  const e = Dn();
  return t && Ln(e, t), e;
}
const Nn = /* @__PURE__ */ Symbol("style");
function Go(t, e) {
  return Ho(t) ? Ln(t, e) : e;
}
function Ko(t) {
  const e = Ee(Dn()), n = Object.keys(e);
  for (const o of n)
    F(() => t.value[o], () => {
      Wo(e[o], t.value[o] || {}, Go);
    }, { immediate: !0, deep: !0 });
  return Me(Nn, e), e;
}
function at(t) {
  return q(Ce(Nn), `Configs(${t})`)[t];
}
function Yo() {
  return at("view");
}
function lt() {
  return at("node");
}
function ct() {
  return at("edge");
}
function jn() {
  return at("path");
}
function Qo() {
  return jo();
}
function le(t) {
  return Object.entries(t);
}
function et(t, e) {
  const n = new Set(Object.keys(t));
  le(e).forEach(([o, i]) => {
    Pe(t[o], i) || (t[o] = i), n.delete(o);
  }), n.forEach((o) => delete t[o]);
}
function Jo(t) {
  return t instanceof Promise || t && typeof t.then == "function";
}
function ei(t, e, n) {
  const o = ke({
    edgeLayoutPoints: {},
    edgeGroups: {},
    summarizedEdges: {}
  });
  return ue(() => {
    const { edgeLayoutPoints: i, edgeGroups: s } = ni(
      n,
      t.value,
      e.value
    );
    et(o.edgeLayoutPoints, i), et(o.edgeGroups, s);
  }), ue(() => {
    const i = {};
    for (const [s, { edges: a, groupWidth: c }] of Object.entries(o.edgeGroups)) {
      let u = !1;
      if (c == 0)
        u = !1;
      else if (n.edge.summarize instanceof Function) {
        const h = n.edge.summarize(a, n);
        h === null ? u = un(t.value, a, n, c) : u = h;
      } else n.edge.summarize ? u = un(t.value, a, n, c) : u = !1;
      o.edgeGroups[s].summarize = u, u && Object.keys(a).forEach((h) => i[h] = !0);
    }
    et(o.summarizedEdges, i);
  }), o;
}
function ti(t, e, n, o, i, s) {
  return t ? e ? dn(t.edge, n, o, i, 0, 0, s) : dn(
    t.edge,
    n,
    o,
    i,
    t.groupWidth,
    t.pointInGroup,
    s
  ) : { p1: { x: 0, y: 0 }, p2: { x: 0, y: 0 } };
}
function ni(t, e, n) {
  const o = {}, i = {}, s = {};
  for (const [c, u] of Object.entries(n)) {
    if (!(u.source in e && u.target in e))
      continue;
    const h = [u.source, u.target].sort().join("<=>"), f = s[h] || {};
    f[c] = u, s[h] = f;
  }
  const a = t.edge.gap instanceof Function ? t.edge.gap : (c, u) => t.edge.gap;
  for (const [c, u] of Object.entries(s)) {
    const h = Object.keys(u).length;
    if (h == 0) continue;
    const f = a(u, t), [r, l] = Object.entries(u)[0];
    if (h === 1)
      o[r] = { edge: l, pointInGroup: 0, groupWidth: 0 }, i[c] = { edges: u, groupWidth: 0, summarize: !1 };
    else {
      let d = 0;
      const b = Object.entries(u).map(([g, w]) => {
        let E = I.value(t.edge.normal.width, w);
        return isNaN(+E) && (console.warn(
          "[v-network-graph] Edge width is invalid value. id=[%s] value=[%s]",
          g,
          E
        ), E = 1), E / 2;
      }), x = Object.entries(u).map(([g, w], E) => (E > 0 && (d += b[E - 1] + f + b[E]), [g, w, d])), m = d;
      x.forEach(([g, w, E]) => {
        o[g] = { edge: w, pointInGroup: E, groupWidth: m };
      }), i[c] = { edges: u, groupWidth: m, summarize: !1 };
    }
  }
  return { edgeLayoutPoints: o, edgeGroups: i };
}
function un(t, e, n, o) {
  if (Object.entries(e).length === 1) return !1;
  const s = Math.min(
    ...Object.values(e).flatMap((a) => [t[a.source], t[a.target]]).filter((a) => a).map((a) => {
      const c = I.values(n.node.normal, a);
      return c.type === "circle" ? c.radius * 2 : Math.min(c.width, c.height);
    })
  );
  return o > s;
}
function dn(t, e, n, o, i, s, a) {
  var r, l, d, b, x, m, g, w;
  let c, u, h, f;
  return t.source < t.target ? [c, u, h, f] = fn(
    (r = e == null ? void 0 : e.x) != null ? r : 0,
    (l = e == null ? void 0 : e.y) != null ? l : 0,
    (d = n == null ? void 0 : n.x) != null ? d : 0,
    (b = n == null ? void 0 : n.y) != null ? b : 0,
    o,
    i,
    s,
    a
  ) : [h, f, c, u] = fn(
    (x = n == null ? void 0 : n.x) != null ? x : 0,
    (m = n == null ? void 0 : n.y) != null ? m : 0,
    (g = e == null ? void 0 : e.x) != null ? g : 0,
    (w = e == null ? void 0 : e.y) != null ? w : 0,
    o,
    i,
    s,
    a
  ), { p1: { x: c, y: u }, p2: { x: h, y: f } };
}
function fn(t, e, n, o, i, s, a, c) {
  const u = n - t, h = o - e;
  let f = (s / 2 - a) * i;
  if (f !== 0 && c !== "clock") {
    const r = Math.atan2(o - e, n - t);
    if (c === "vertical") {
      const l = Math.PI / 2;
      (r < -l || r >= l) && (f *= -1);
    } else c === "horizontal" && r < 0 && (f *= -1);
  }
  if (u === 0) {
    const r = h < 0 ? -1 : 1;
    return [t + f * r, e, n + f * r, o];
  } else if (h === 0) {
    const r = u < 0 ? 1 : -1;
    return [t, e + f * r, n, o + f * r];
  } else {
    const l = -1 / (h / u);
    h < 0 && (f = -f);
    const d = f / Math.sqrt(1 + Math.pow(l, 2));
    return [t + d, e + d * l, n + d, o + d * l];
  }
}
function Bn(t, e, n) {
  return n || (n = { x: 0, y: 0 }), n.x = t.x + e.x, n.y = t.y + e.y, n;
}
function Bt(t, e, n) {
  return n || (n = { x: 0, y: 0 }), n.x = t.x - e.x, n.y = t.y - e.y, n;
}
function Rn(t, e, n) {
  return n || (n = { x: 0, y: 0 }), n.x = t.x * e.x, n.y = t.y * e.y, n;
}
function Vn(t, e, n) {
  return n || (n = { x: 0, y: 0 }), n.x = t.x * e, n.y = t.y * e, n;
}
function Rt(t, e, n) {
  return n || (n = { x: 0, y: 0 }), n.x = t.x / e.x, n.y = t.y / e.y, n;
}
function Wn(t, e) {
  return t.x * e.x + t.y * e.y;
}
function Hn(t, e) {
  return t.x * e.y - t.y * e.x;
}
function Vt(t) {
  return t.x * t.x + t.y * t.y;
}
function Wt(t) {
  return Math.sqrt(Vt(t));
}
function Ht(t, e) {
  const n = t.x - e.x, o = t.y - e.y;
  return n * n + o * o;
}
function _n(t, e) {
  return Math.sqrt(Ht(t, e));
}
function Un(t, e) {
  e || (e = { x: 0, y: 0 });
  const n = Wt(t);
  return n === 0 ? (e.x = 1, e.y = 0) : Rt(t, { x: n, y: n }, e), e;
}
function nt(t, e, n) {
  n || (n = { x: 0, y: 0 });
  const o = t.x * Math.cos(e) - t.y * Math.sin(e), i = t.x * Math.sin(e) + t.y * Math.cos(e);
  return n.x = o, n.y = i, n;
}
const oi = 180 / Math.PI;
function ii(t) {
  return t * oi;
}
function _t(t) {
  return Math.atan2(t.y, t.x);
}
function Ut(t) {
  return ii(_t(t));
}
const si = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  add: Bn,
  angle: _t,
  angleDegree: Ut,
  cross: Hn,
  distance: _n,
  distanceSquared: Ht,
  divide: Rt,
  dot: Wn,
  length: Wt,
  lengthSquared: Vt,
  multiply: Rn,
  multiplyScalar: Vn,
  normalize: Un,
  rotate: nt,
  subtract: Bt
}, Symbol.toStringTag, { value: "Module" }));
class V {
  static fromArray(e) {
    return new V(e[0] || 0, e[1] || 0);
  }
  static fromObject(e) {
    return new V(e.x, e.y);
  }
  constructor(e, n) {
    this.x = e, this.y = n;
  }
  // instance methods
  add(e) {
    return Bn(this, e, this);
  }
  subtract(e) {
    return Bt(this, e, this);
  }
  multiply(e) {
    return Rn(this, e, this);
  }
  multiplyScalar(e) {
    return Vn(this, e, this);
  }
  divide(e) {
    return Rt(this, e, this);
  }
  dot(e) {
    return Wn(this, e);
  }
  cross(e) {
    return Hn(this, e);
  }
  lengthSquared() {
    return Vt(this);
  }
  length() {
    return Wt(this);
  }
  distanceSquared(e) {
    return Ht(this, e);
  }
  distance(e) {
    return _n(this, e);
  }
  normalize() {
    return Un(this, this);
  }
  angle() {
    return _t(this);
  }
  angleDegree() {
    return Ut(this);
  }
  rotate(e) {
    return nt(this, e, this);
  }
  isEqualTo(e) {
    return this.x === e.x && this.y === e.y;
  }
  clone() {
    return new V(this.x, this.y);
  }
  toObject() {
    return { x: this.x, y: this.y };
  }
  toArray() {
    return [this.x, this.y];
  }
}
const J = oe({
  Vector2D: V
}, si);
class Y {
  constructor(e, n, o) {
    this.source = e, this.target = n, this.v = o;
  }
  static fromLinePosition(e) {
    const n = V.fromObject(e.p1), o = V.fromObject(e.p2);
    return new Y(n, o, Ne(n, o));
  }
  static fromPositions(e, n) {
    const o = V.fromObject(e), i = V.fromObject(n);
    return new Y(o, i, Ne(o, i));
  }
  static fromVectors(e, n) {
    return new Y(e, n, Ne(e, n));
  }
}
function Ne(t, e) {
  return e.clone().subtract(t);
}
function ri(t) {
  return [V.fromObject(t.p1), V.fromObject(t.p2)];
}
function ai(t) {
  return new V((t.p1.x + t.p2.x) / 2, (t.p1.y + t.p2.y) / 2);
}
function ot(t, e) {
  return { p1: t, p2: e };
}
function Tt(t, e, n) {
  const o = Y.fromLinePosition(t);
  return li(o, e, n);
}
function li(t, e, n) {
  const o = t.v.clone().normalize(), i = t.source.clone().add(o.clone().multiplyScalar(e)), s = t.target.clone().subtract(o.clone().multiplyScalar(n));
  let a = i.toObject(), c = s.toObject();
  const u = Ne(i, s);
  if (t.v.angle() * u.angle() < 0) {
    const h = new V((a.x + c.x) / 2, (a.y + c.y) / 2), f = h.clone().add(o.multiplyScalar(0.5));
    a = h.toObject(), c = f.toObject();
  }
  return { p1: a, p2: c };
}
function ci(t) {
  return { p1: t.p2, p2: t.p1 };
}
function We(t) {
  const e = t.v.clone().normalize().rotate(Math.PI / 2);
  return Y.fromVectors(t.target, t.target.clone().add(e));
}
function Ft(t, e) {
  const n = e.v.clone().normalize(), o = e.source, i = J.subtract(t, o), s = n.dot(i);
  return o.clone().add(n.multiplyScalar(s));
}
function ut(t, e, n, o) {
  if (!(J.lengthSquared(J.subtract(e, n)) - o * o <= Math.pow(1, -10))) return null;
  const a = Y.fromVectors(t, e), c = Ft(n, a), u = J.length(J.subtract(c, n));
  if (o < u) return null;
  if (o === u) return c;
  const h = Math.sqrt(ie(o, 2) - ie(u, 2)), f = a.v.normalize().multiplyScalar(h);
  return c.subtract(f);
}
function ui(t, e, n, o, i) {
  if (!(J.lengthSquared(J.subtract(e, n)) - o * o <= Math.pow(1, -10))) return null;
  const c = Y.fromVectors(t, e), u = Ft(n, c), h = J.length(J.subtract(u, n));
  if (o < h) return null;
  if (o === h) return u;
  const f = Math.sqrt(ie(o, 2) - ie(h, 2)), r = c.v.normalize().multiplyScalar(f), l = u.clone().add(r), d = u.clone().subtract(r), b = i.distance(l), x = i.distance(d);
  return Math.abs(b - x) < 2 ? d : b < x ? l : d;
}
function Ie(t, e) {
  const n = J.subtract(e.source, t.source), o = t.v, i = e.v, s = J.cross(n, o) / J.cross(o, i);
  return e.source.clone().add(i.clone().multiplyScalar(s));
}
function qe(t, e, n, o, i) {
  const s = t, c = n.clone().subtract(s), u = c.length(), h = e + o;
  if (h < u) return null;
  const f = Math.abs(e - o);
  if (u < f) return null;
  if (u === h) {
    const y = c.clone().normalize(), k = t.clone().add(y.multiplyScalar(e));
    return i ? k : [k, k];
  }
  if (u === f) {
    const y = c.clone().normalize(), k = e > o, O = t.clone().add(y.multiplyScalar(k ? e : -e));
    return i ? O : [O, O];
  }
  const r = e, l = o, d = (ie(u, 2) + ie(r, 2) - ie(l, 2)) / (2 * u * r), b = r * d, x = Math.sqrt(ie(r, 2) - ie(b, 2)), m = c.clone().normalize(), g = new V(-m.y, m.x), w = m.clone().multiplyScalar(b), E = g.clone().multiplyScalar(x), p = t.clone().add(w).add(E), v = t.clone().add(w).subtract(E);
  if (i) {
    const y = p.distance(i), k = v.distance(i);
    return y < k ? p : v;
  } else
    return [p, v];
}
function hn(t, e, n, o) {
  const i = Y.fromPositions(t, e), s = (n.width + n.strokeWidth) / 2 * o, a = (n.height + n.strokeWidth) / 2 * o, c = n.borderRadius > 0 ? (n.borderRadius + n.strokeWidth / 2) * o : 0, u = (i.v.angle() - Math.PI / 2) % Math.PI, h = Math.PI / 2 - u % Math.PI, f = a * Math.abs(Math.tan(u)), r = s * Math.abs(Math.tan(h)), l = f <= s - c, d = r <= a - c;
  if (l || d || c === 0)
    return Math.sqrt(l ? ie(a, 2) + ie(f, 2) : ie(s, 2) + ie(r, 2));
  {
    const b = e.x - s + c, x = e.y - a + c, m = e.x + s - c, g = e.y + a - c, w = [
      new V(b, x),
      new V(m, x),
      new V(m, g),
      new V(b, g)
    ], E = Math.floor((i.v.angleDegree() + 360) % 360 / 90), p = w[E], v = ut(
      i.source,
      Ft(p, i),
      p,
      c
    );
    return v ? Ne(v, i.target).length() : Ne(p, i.target).length() + c;
  }
}
function Fn(t, e, n, o, i) {
  const s = Y.fromLinePosition(t), a = s.v.clone().normalize(), c = o === 0 ? s.source : s.source.clone().add(a.clone().multiplyScalar(o * i)), u = o === 0 ? s.target : s.target.clone().subtract(a.clone().multiplyScalar(o * i)), h = (e.width / 2 + n) * i, f = new V(-a.y, a.x).multiplyScalar(h);
  let r = J.subtract(c, f), l = J.add(c, f), d = J.subtract(u, f), b = J.add(u, f);
  const x = s.v.angleDegree();
  return (x < -90 || x >= 90) && ([r, l] = [l, r], [d, b] = [b, d]), {
    source: { above: r, below: l },
    target: { above: d, below: b }
  };
}
function di(t, e, n, o) {
  let i;
  n.type === "circle" ? i = n.radius + n.strokeWidth / 2 : i = hn(
    e,
    t,
    n,
    1
    // scale
  );
  let s;
  return o.type === "circle" ? s = o.radius + o.strokeWidth / 2 : s = hn(
    t,
    e,
    o,
    1
    // scale
  ), [i, s];
}
function Se(t, e, n) {
  const { x: o, y: i } = t, s = o - e.x, a = i - e.y;
  return {
    x: s * Math.cos(n) - a * Math.sin(n) + e.x,
    y: s * Math.sin(n) + a * Math.cos(n) + e.y
  };
}
function It(t) {
  return t > 0 ? -(Math.PI * 2 - t) : Math.PI * 2 + t;
}
function $t(t, e, n, o) {
  const i = [], s = Y.fromVectors(e, t), a = Y.fromVectors(e, n);
  let c = je(s, a);
  o * c < 0 && (c = It(c));
  const u = V.fromObject(Se(t, e, -c / 2)), h = Y.fromVectors(e, u), f = We(h), r = je(s, h);
  let l = We(s);
  if (Math.abs(r) < Math.PI / 2) {
    const b = Ie(l, f);
    i.push(b);
  } else {
    const b = V.fromObject(Se(u, e, r / 2)), x = We(
      Y.fromVectors(e, V.fromObject(b))
    ), m = Ie(l, x), g = Ie(x, f);
    i.push(m, b, g);
  }
  i.push(u);
  const d = je(a, h);
  if (l = We(a), Math.abs(d) < Math.PI / 2) {
    const b = Ie(l, f);
    i.push(b);
  } else {
    const b = V.fromObject(Se(u, e, d / 2)), x = We(
      Y.fromVectors(e, V.fromObject(b))
    ), m = Ie(f, x), g = Ie(x, l);
    i.push(m, b, g);
  }
  return i;
}
function je(t, e) {
  return Math.atan2(
    t.v.y * e.v.x - t.v.x * e.v.y,
    t.v.x * e.v.x + t.v.y * e.v.y
  );
}
function fi(t, e, n) {
  const o = t.x, i = t.y, s = e.x, a = e.y, c = n.x, u = n.y, h = o - s, f = i - a, r = c - s, l = u - a;
  if (h === 0 && f === 0 || r === 0 && l === 0)
    return [t, 0];
  const d = (l * (h * (o + s) + f * (i + a)) - f * (r * (c + s) + l * (u + a))) / (2 * h * l - 2 * f * r), b = (-r * (h * (o + s) + f * (i + a)) + h * (r * (c + s) + l * (u + a))) / (2 * h * l - 2 * f * r), x = Math.sqrt(Math.pow(o - d, 2) + Math.pow(i - b, 2));
  return [new V(d, b), x];
}
function De(t) {
  return t.type == "circle" ? t.radius : Math.min(t.width, t.height) / 2;
}
function hi(t) {
  const e = Object.values(t);
  if (e.length === 0)
    return {
      top: 0,
      bottom: 0,
      left: 0,
      right: 0
    };
  const n = {
    top: e[0].y,
    bottom: e[0].y,
    left: e[0].x,
    right: e[0].x
  };
  return e.forEach((o) => {
    n.top = Math.min(o.y, n.top), n.bottom = Math.max(o.y, n.bottom), n.left = Math.min(o.x, n.left), n.right = Math.max(o.x, n.right);
  }), n;
}
function Qe(...t) {
  return t.find((e) => !!e);
}
const vi = Number.EPSILON * 100;
function gi(t, e, n, o, i, s, a, c) {
  var x, m;
  const u = t.edges, h = t.directions, f = u.map((g, w) => mi(g, h[w], o[g.edgeId])), r = [];
  let l = !1, d = !1;
  {
    const g = f[0];
    let w = De(e[g.source].shape) * i;
    const E = c + (a === "edgeOfNode" ? w : 0), p = E <= 0 ? g.line.source : gn(g, E, n, !0);
    r.push(p), w = De(e[g.target].shape) * i, c > 0 && J.distance(g.line.source, g.line.target) <= E + w && (l = !0);
  }
  const b = u.length;
  for (let g = 1; g < b; g++) {
    const w = f[g - 1], E = f[g], p = E.source, v = V.fromObject((x = n[p]) != null ? x : { x: 0, y: 0 }), y = pi(w, E, v), k = De(e[p].shape) * i, O = Math.max(k * (2 / 3), k - 4 * i), L = Lt(w), T = Lt(E), z = Je(w, v, O, L), A = Je(E, v, O, !T), j = Je(w, v, k, L), G = Je(E, v, k, !T);
    let R;
    if (y) {
      const _ = J.distance(y, v);
      if (_ < O)
        R = [
          Qe(z, j, w.line.target),
          y,
          Qe(A, G, E.line.source)
        ];
      else if (_ <= k) {
        let fe, se;
        j && z ? fe = J.distance(y, z) < J.distance(y, j) ? z : j : fe = j || w.line.target, G && A ? se = J.distance(y, A) < J.distance(y, G) ? A : G : se = G || E.line.source, R = [fe, y, se];
      } else
        z && A ? R = [z, v, A] : j && G ? R = [j, v, G] : R = [
          Qe(z, j, w.line.target),
          v,
          Qe(A, G, E.line.source)
        ];
    } else
      z && A ? R = [z, v, A] : j && G ? R = [j, v, G] : R = [w.line.target, v, E.line.source];
    if (w.curve) {
      const _ = r[r.length - 1];
      if (_) {
        const fe = _ instanceof Array ? _[_.length - 1] : _;
        let se;
        R instanceof Array ? se = s ? R[0] : R[1] : se = R;
        const pe = $t(
          fe,
          w.curve.circle.center,
          se,
          w.curve.theta
        );
        R instanceof Array && s ? r.push([...pe, ...R]) : r.push([...pe, se]);
      }
    } else if (w.loop) {
      const [_, fe, se] = mn(v, w, k);
      r.push(_), r.push(se), R instanceof Array && s ? r.push([fe, R[1], R[2]]) : r.push(R[2]);
    } else
      s || !(R instanceof Array) ? r.push(R) : E.curve ? r.push(R[1]) : E.loop ? r.push(R[0]) : r.push(R[0], R[2]);
  }
  {
    const g = f[f.length - 1];
    let w = De(e[g.target].shape) * i;
    const E = c + (a === "edgeOfNode" ? w : 0), p = E <= 0 ? g.line.target : gn(g, E, n, !1);
    if (w = De(e[g.source].shape) * i, g.loop) {
      const v = V.fromObject((m = n[g.target]) != null ? m : { x: 0, y: 0 }), [y, k, O] = mn(v, g, w);
      r.push(y), r.push(O);
    } else if (g.curve) {
      const v = r[r.length - 1], y = v instanceof Array ? v[v.length - 1] : v, k = $t(
        y,
        g.curve.circle.center,
        p,
        g.curve.theta
      );
      r.push([...k, p]);
    } else
      r.push(p);
    c > 0 && J.distance(g.line.source, g.line.target) <= E + w && (d = !0);
  }
  return l && (r.shift(), r[0] instanceof Array && r.unshift(r[0][0])), d && r.pop(), r;
}
function vn(t) {
  const e = t.length;
  if (e === 0) return [];
  if (e <= 1) return [!0];
  const n = [];
  let o = null, i = !0;
  for (let s = 0; s < e; s++) {
    const a = t[s].edge.source, c = t[s].edge.target;
    if (s === 0)
      if (e > 2) {
        const u = qn(t, 0);
        u === null ? i = !0 : i = u === c;
      } else
        i = [t[1].edge.source, t[1].edge.target].includes(c);
    else a === c ? i = !0 : i = o === a;
    n.push(i), o = i ? c : a;
  }
  return n;
}
function qn(t, e) {
  const n = t[e], o = t[e + 1], i = [n.edge.source, n.edge.target].sort(), s = [o.edge.source, o.edge.target].sort();
  if (i[0] === i[1])
    return i[0];
  if (s[0] === s[1])
    return s[0];
  if (n.edgeId === o.edgeId || i[0] === s[0] && i[1] === s[1]) {
    if (e >= t.length - 2)
      return null;
    {
      const a = qn(t, e + 1);
      return a === null ? null : a === i[1] ? i[0] : i[1];
    }
  } else
    return s.includes(i[1]) ? i[1] : i[0];
}
function gn(t, e, n, o) {
  const i = o ? t.source : t.target, s = t.curve;
  if (s) {
    let a = e / s.circle.radius;
    return s.theta > 0 && (a *= -1), o || (a *= -1), V.fromObject(
      Se(
        o ? t.line.source : t.line.target,
        s.circle.center,
        a
      )
    );
  } else {
    let a, c;
    if (o ? (a = t.line.target, c = t.line.source) : (a = t.line.source, c = t.line.target), n[i]) {
      const u = ut(
        a,
        c,
        V.fromObject(n[i]),
        e
      );
      return u === null ? a : u;
    } else
      return a;
  }
}
function pi(t, e, n) {
  let o = null;
  if (t.loop || e.loop)
    o = null;
  else if (t.curve)
    if (e.curve) {
      if (t.line.target.isEqualTo(e.line.source))
        return t.line.target.clone();
      o = qe(
        t.curve.circle.center,
        t.curve.circle.radius,
        e.curve.circle.center,
        e.curve.circle.radius,
        t.curve.center
      );
    } else
      o = ui(
        e.line.target,
        e.line.source,
        t.curve.circle.center,
        t.curve.circle.radius,
        n
      );
  else if (e.curve)
    o = ut(
      t.line.source,
      t.line.target,
      e.curve.circle.center,
      e.curve.circle.radius
    );
  else {
    const i = pn(t.line), s = pn(e.line);
    !isFinite(i) && !isFinite(s) || Math.abs(i - s) < vi ? o = null : o = Ie(t.line, e.line);
  }
  return o;
}
function Je(t, e, n, o) {
  if (t.loop) {
    const i = qe(
      e,
      n,
      t.loop.center,
      t.loop.radius[0]
    );
    return i ? o ? i[0] : i[1] : null;
  } else return t.curve ? qe(
    e,
    n,
    t.curve.circle.center,
    t.curve.circle.radius,
    V.fromObject(t.curve.center)
  ) : ut(
    o ? t.line.source : t.line.target,
    o ? t.line.target : t.line.source,
    e,
    n
  );
}
function mi(t, e, n) {
  let o = n.origin, i = t.edge.source, s = t.edge.target, a = n.curve;
  const c = n.loop;
  c ? o = n.position : e || (o = ci(o), i = t.edge.target, s = t.edge.source, a && (a = Re(oe({}, a), { theta: -a.theta })));
  const u = Y.fromLinePosition(o);
  return {
    edgeId: t.edgeId,
    source: i,
    target: s,
    line: u,
    direction: e,
    curve: a,
    loop: c
  };
}
function pn(t) {
  return (t.target.y - t.source.y) / (t.target.x - t.source.x);
}
function mn(t, e, n) {
  const { radius: o, center: i } = q(e.loop, "Loop of edge parameter"), [s, a] = o, c = qe(t, n, i, o[0]);
  let [u, h] = c ? c.reverse() : [e.line.source, e.line.target];
  const f = Lt(e);
  f || ([u, h] = [h, u]);
  const r = u, l = h, d = V.fromObject(r).subtract(i).angleDegree();
  let m = (V.fromObject(l).subtract(i).angleDegree() + 360 - d) % 360 >= 180;
  return m = f ? m : !m, [r, l, `A ${s} ${a} 0 ${m ? 1 : 0} ${f ? 1 : 0} ${l.x} ${l.y}`];
}
function Lt(t) {
  return t.loop ? t.direction ? t.loop.isClockwise : !t.loop.isClockwise : !0;
}
function yt(t, e, n, o, i, s, a) {
  const c = Ee({});
  F(
    () => new Set(Object.keys(t.value)),
    (h, f) => {
      f || (f = /* @__PURE__ */ new Set([]));
      for (const r of h)
        f.has(r) || bi(t, c, r, !1, e, i);
      for (const r of f)
        h.has(r) || (n.delete(r), o.delete(r), s == null || s(r, c[r]), delete c[r]);
    },
    { immediate: !0 }
  ), F(
    () => [...n],
    (h, f) => {
      const r = f ? h.filter((d) => !f.includes(d)) : h, l = f ? f.filter((d) => !h.includes(d)) : [];
      r.forEach((d) => {
        const b = c[d];
        b && !b.selected && (b.selected = !0);
      }), l.forEach((d) => {
        const b = c[d];
        b && b.selected && (b.selected = !1);
      });
    },
    { immediate: !0 }
    // for specified from the beginning
  ), F(
    () => [...o],
    (h, f) => {
      const r = h.filter((d) => !f.includes(d)), l = f.filter((d) => !h.includes(d));
      r.forEach((d) => {
        const b = c[d];
        b && !b.hovered && (b.hovered = !0);
      }), l.forEach((d) => {
        const b = c[d];
        b && b.hovered && (b.hovered = !1);
      });
    }
  );
  const u = C(() => {
    const h = a ? a() : Object.values(c);
    return e.zOrder.enabled ? yi(h, e.zOrder, o, n) : h;
  });
  return { states: c, zOrderedList: u };
}
function bi(t, e, n, o, i, s) {
  const a = {
    id: n,
    selected: o,
    hovered: !1,
    selectable: C(() => t.value[n] ? I.value(i.selectable, t.value[n]) : M(a.selectable)),
    zIndex: C(() => t.value[n] ? I.value(i.zOrder.zIndex, t.value[n]) : M(a.zIndex))
  };
  e[n] = a, s(
    t,
    n,
    e[n]
    /* get reactive object */
  );
}
function yi(t, e, n, o) {
  return e.bringToFrontOnHover && e.bringToFrontOnSelected ? t.sort((i, s) => {
    const a = n.has(i.id), c = n.has(s.id);
    if (a != c)
      return a ? 1 : -1;
    const u = o.has(i.id), h = o.has(s.id);
    return u != h ? u ? 1 : -1 : i.zIndex - s.zIndex;
  }) : e.bringToFrontOnHover ? t.sort((i, s) => {
    const a = n.has(i.id), c = n.has(s.id);
    return a != c ? a ? 1 : -1 : i.zIndex - s.zIndex;
  }) : e.bringToFrontOnSelected ? t.sort((i, s) => {
    const a = o.has(i.id), c = o.has(s.id);
    return a != c ? a ? 1 : -1 : i.zIndex - s.zIndex;
  }) : t.sort((i, s) => i.zIndex - s.zIndex);
}
function wi(t) {
  return typeof btoa === void 0 ? Buffer.from(t).toString("base64").replaceAll("=", "") : btoa(t).replaceAll("=", "");
}
function Ei() {
  return { markers: Ee({}), referenceCount: {} };
}
function xi(t) {
  const { markers: e, referenceCount: n } = t;
  function o(c, u) {
    var f;
    const h = (f = n[c]) != null ? f : 0;
    n[c] = h + 1, h || (e[c] = u);
  }
  function i(c) {
    var h;
    const u = (h = n[c]) != null ? h : 0;
    u && (u - 1 === 0 ? (delete e[c], delete n[c]) : n[c] = u - 1);
  }
  function s(c) {
    c && i(c);
  }
  function a(c, u, h, f, r) {
    if (c.type === "none") {
      s(h);
      return;
    }
    if (c.type === "custom")
      return s(h), c.customId;
    const l = ki(c, u, f), d = Pi(l, r);
    return d === h || (s(h), o(d, l)), d;
  }
  return {
    makeMarker: a,
    clearMarker: s
  };
}
function ki(t, e, n) {
  var o;
  return Re(oe({}, t), {
    color: (o = t.color) != null ? o : n,
    isSource: e
  });
}
function Pi(t, e) {
  const n = wi(t.color), o = t.isSource ? "L" : "R", i = t.units === "strokeWidth" ? "rel" : "abs";
  return `marker_${e}_${t.type}_${t.width}_${t.height}_${t.margin}_${t.offset}_${n}_${o}_${i}`;
}
function wt(t, e, n) {
  return {
    objects: t,
    selected: e,
    hovered: n
  };
}
const Xn = /* @__PURE__ */ Symbol("states"), Si = {
  type: "none",
  width: 0,
  height: 0,
  margin: 0,
  offset: 0,
  units: "strokeWidth",
  color: null
};
function Mi(t, e, n, o, i, s, a, c) {
  const u = Ee({}), h = ke({});
  ue(() => {
    const p = Object.fromEntries(
      Object.keys(t.objects.value).map((v) => [v, {}])
    );
    Object.entries(e.objects.value).forEach(([v, y]) => {
      p != null && p[y.source] || (p[y.source] = {}), p != null && p[y.target] || (p[y.target] = {}), p[y.source][v] = y.target, p[y.target][v] = y.source;
    }), et(h, p);
  });
  const {
    states: f,
    zOrderedList: r
    //
  } = yt(
    t.objects,
    o.node,
    t.selected,
    t.hovered,
    (p, v, y) => {
      zi(
        p,
        v,
        y,
        o.node,
        h,
        i,
        s
      );
    },
    (p, v) => {
      const y = i.nodes;
      delete y[p];
    }
  ), l = Qo(), d = ei(t.objects, e.objects, o), b = B([]), {
    states: x,
    zOrderedList: m
    //
  } = yt(
    e.objects,
    o.edge,
    e.selected,
    e.hovered,
    (p, v, y) => {
      Ii(
        p,
        v,
        y,
        o.edge,
        a,
        f,
        d,
        i,
        c,
        l
      );
    },
    (p, v) => {
      var y;
      (y = v.stopWatchHandle) == null || y.call(v);
    },
    () => b.value
  );
  ue(() => {
    b.value = $i(d.edgeGroups, x);
  }), F(
    d.edgeGroups,
    (p) => Zi(u, d, o),
    { immediate: !0 }
  );
  const {
    states: g,
    zOrderedList: w
    //
  } = yt(
    n.objects,
    o.path,
    n.selected,
    n.hovered,
    (p, v, y) => {
      const k = y;
      k.clickable = C(() => p.value[v] ? I.value(o.path.clickable, p.value[v]) : !1), k.hoverable = C(() => p.value[v] ? I.value(o.path.hoverable, p.value[v]) : !1), k.path = p.value[v], k.edges = yn(k.path, e), k.directions = vn(k.edges), k.stopWatchHandle = F(
        () => p.value[v].edges,
        () => {
          k.path = p.value[v], k.edges = yn(k.path, e), k.directions = vn(k.edges);
        }
      );
    },
    (p, v) => {
      var y;
      (y = v.stopWatchHandle) == null || y.call(v);
    }
  ), E = {
    nodeStates: f,
    edgeStates: x,
    edgeGroupStates: d,
    summarizedEdgeStates: u,
    pathStates: g,
    layouts: i,
    nodeZOrderedList: r,
    edgeZOrderedList: m,
    pathZOrderedList: w
  };
  return Me(Xn, E), E;
}
function Ci(t) {
  return t.summarized;
}
function me() {
  return q(Ce(Xn), "states");
}
function Oi(t, e, n, o) {
  return n && o.hover ? I.values(o.hover, t) : Gn(t, e, o);
}
function Gn(t, e, n) {
  return e && n.selected ? I.values(n.selected, t) : I.values(n.normal, t);
}
function zi(t, e, n, o, i, s, a) {
  var c, u;
  !s.nodes[e] && ((c = a.nodes) != null && c[e]) && (s.nodes[e] = oe({}, (u = a.nodes) == null ? void 0 : u[e])), n.shape = C(() => t.value[e] ? Oi(t.value[e], n.selected, n.hovered, o) : M(n.shape)), n.staticShape = C(() => t.value[e] ? Gn(t.value[e], n.selected, o) : M(n.staticShape)), n.label = C(() => t.value[e] ? I.values(o.label, t.value[e]) : M(n.label)), n.labelText = C(() => {
    var h, f;
    return o.label.text instanceof Function ? M(n.label).text : t.value[e] ? (f = (h = t.value[e]) == null ? void 0 : h[M(n.label).text]) != null ? f : "" : M(n.labelText);
  }), n.draggable = C(() => t.value[e] ? I.value(o.draggable, t.value[e]) : M(n.draggable)), n.oppositeNodeIds = we(i, e), n.oppositeNodes = C(() => Object.entries(n.oppositeNodeIds).reduce(
    (h, f) => {
      const [r, l] = f, d = s.nodes[l];
      return d && (h[r] = { nodeId: l, pos: oe({}, d) }), h;
    },
    {}
  ));
}
function Ti(t, e, n, o) {
  return e ? I.values(o.selected, t) : n && o.hover ? I.values(o.hover, t) : I.values(o.normal, t);
}
function bn(t) {
  return t.type === "none" ? Si : t;
}
function Ii(t, e, n, o, i, s, a, c, u, h) {
  const { makeMarker: f, clearMarker: r } = xi(i);
  Object.assign(n, {
    origin: { p1: { x: 0, y: 0 }, p2: { x: 0, y: 0 } },
    labelPosition: { p1: { x: 0, y: 0 }, p2: { x: 0, y: 0 } },
    position: { p1: { x: 0, y: 0 }, p2: { x: 0, y: 0 } }
  }), n.label = C(() => t.value[e] ? I.values(o.label, t.value[e]) : M(n.label));
  const l = C(() => {
    const g = t.value[e], w = Ti(g, n.selected, n.hovered, o);
    isNaN(+w.width) && (console.warn(
      "[v-network-graph] Edge width is invalid value. id=[%s] value=[%s]",
      e,
      w.width
    ), w.width = 1), (w.color === void 0 || w.color === null) && (console.warn(
      "[v-network-graph] Edge color is invalid value. id=[%s] value=[%s]",
      e,
      w.color
    ), w.color = "#000000");
    let E = I.value(o.normal.width, g);
    isNaN(+E) && (E = 1);
    const p = bn(I.values(o.marker.source, [g, w])), v = bn(I.values(o.marker.target, [g, w]));
    return { stroke: w, normalWidth: E, source: p, target: v };
  });
  n.line = l;
  const d = we(
    a.edgeLayoutPoints,
    e
  ), b = we(a.summarizedEdges, e), x = ue(() => {
    var R, _, fe, se, pe, Oe, ze;
    const g = t.value[e];
    if (!g) return;
    const w = (R = s[g.source]) == null ? void 0 : R.staticShape, E = (_ = s[g.target]) == null ? void 0 : _.staticShape;
    if (!w || !E)
      return;
    const p = (fe = c.nodes[g == null ? void 0 : g.source]) != null ? fe : { x: 0, y: 0 }, v = (se = c.nodes[g == null ? void 0 : g.target]) != null ? se : { x: 0, y: 0 }, y = ti(
      d.value,
      (pe = b.value) != null ? pe : !1,
      p,
      v,
      u.value,
      o.keepOrder
    ), [k, O] = di(p, v, w, E), L = u.value;
    n.labelPosition = Tt(
      y,
      k * L,
      O * L
    );
    let T = 0, z = 0;
    const A = l.value;
    if (A.source.type !== "none") {
      const re = A.source;
      T = re.margin + re.width, re.units === "strokeWidth" && (T *= A.normalWidth);
    }
    if (A.target.type !== "none") {
      const re = A.target;
      z = re.margin + re.width, re.units === "strokeWidth" && (z *= A.normalWidth);
    }
    o.margin && (T += o.margin, z += o.margin);
    const j = !!o.margin || A.source.type !== "none" || A.target.type !== "none";
    if (g.source === g.target) {
      n.origin = ot(p, v);
      const re = I.values(o.selfLoop, g), [Le, Ae] = Ai(
        p,
        w,
        re,
        j,
        T,
        z,
        (ze = (Oe = d.value) == null ? void 0 : Oe.pointInGroup) != null ? ze : 0,
        L
      );
      n.position = Le, n.loop = Ae, n.curve = void 0;
      return;
    } else
      n.loop = void 0;
    if (j && (T += k, z += O), o.type === "straight" || b.value)
      n.origin = y, n.curve = void 0, T === 0 && z === 0 ? n.position = n.origin : n.position = Tt(n.origin, T * L, z * L);
    else {
      n.origin = ot(p, v);
      const re = d.value ? d.value.groupWidth / 2 - d.value.pointInGroup : 0, [Le, Ae] = Li(
        n.origin,
        y,
        re,
        T * L,
        z * L
      );
      n.position = Le, n.curve = Ae;
    }
  }), m = ue(() => {
    t.value[e] && (n.sourceMarkerId = f(
      l.value.source,
      !0,
      n.sourceMarkerId,
      l.value.stroke.color,
      h
    ), n.targetMarkerId = f(
      l.value.target,
      !1,
      n.targetMarkerId,
      l.value.stroke.color,
      h
    ));
  });
  n.stopWatchHandle = () => {
    x(), m(), r(n.sourceMarkerId), r(n.targetMarkerId);
  };
}
function $i(t, e) {
  return Object.entries(t).map(([n, o]) => {
    var i;
    return o.summarize ? {
      id: (i = Object.keys(o.edges)[0]) != null ? i : n,
      summarized: !0,
      key: n,
      group: o,
      zIndex: Object.keys(o.edges).map((s) => {
        var a, c;
        return (c = (a = e[s]) == null ? void 0 : a.zIndex) != null ? c : 0;
      }).reduce((s, a) => Math.max(s, a))
    } : Object.entries(o.edges).map(
      ([s, a]) => {
        var c, u;
        return {
          id: s,
          summarized: !1,
          key: s,
          edge: a,
          zIndex: (u = (c = e[s]) == null ? void 0 : c.zIndex) != null ? u : 0
        };
      }
    );
  }).flat();
}
function Li(t, e, n, o, i) {
  const s = Y.fromLinePosition(t), a = Y.fromLinePosition(e), c = ai(e), [u, h] = fi(
    s.source,
    s.target,
    c
  );
  let f, r;
  if (h === 0)
    return [t, r];
  if (n === 0)
    return o === 0 && i === 0 ? f = t : f = Tt(t, o, i), [f, r];
  const l = Y.fromVectors(u, c), d = je(
    Y.fromVectors(u, s.source),
    l
  );
  if (o === 0 && i === 0)
    f = t;
  else {
    let g = o / h, w = i / h;
    d > 0 && (g *= -1, w *= -1), f = ot(
      Se(s.source, u, g),
      Se(s.target, u, -w)
    );
    let E = je(
      Y.fromVectors(u, s.source),
      Y.fromVectors(u, s.target)
    ), p = je(
      Y.fromPositions(u, f.p1),
      Y.fromPositions(u, f.p2)
    );
    if (d * E < 0 && (E = It(E), d * p < 0 && (p = It(p))), E * p < 0) {
      const v = c.clone().add(a.v.normalize().multiplyScalar(0.5));
      return f = ot(c, v), [f, r];
    }
  }
  const [b, x] = ri(f), m = $t(b, u, x, d).map((g) => g.toObject());
  return r = {
    center: c,
    theta: d,
    circle: { center: u, radius: h },
    control: m
  }, [f, r];
}
function Ai(t, e, n, o, i, s, a, c) {
  const u = c, h = (n.radius + a / 2) * u, f = n.offset * u + h, r = (n.angle - 90) * (Math.PI / 180), l = V.fromObject({
    x: t.x + f * Math.cos(r),
    y: t.y + f * Math.sin(r)
  }), d = n.isClockwise;
  let b, x;
  if (o) {
    const p = qe(
      l,
      h,
      V.fromObject(t),
      De(e) * u
    );
    if (p) {
      [b, x] = p;
      let v = 1;
      if (d || ([b, x] = [x, b], v = -1), i !== 0 || s !== 0) {
        const y = i * u / h * v, k = s * u / h * v;
        b = Se(b, l, y), x = Se(x, l, -k);
      }
    }
  }
  if (b === void 0 || x === void 0) {
    const p = V.fromObject(t).subtract(l).normalize().multiplyScalar(h);
    let v = 1 * (Math.PI / 180);
    d || (v *= -1), b = l.clone().add(nt(p, v)), x = l.clone().add(nt(p, -v));
  }
  const m = V.fromObject(b).subtract(l).angleDegree(), E = (V.fromObject(x).subtract(l).angleDegree() + 360 - m) % 360 >= 180;
  return [
    { p1: b, p2: x },
    {
      center: l,
      radius: [h, h],
      isLargeArc: d ? E : !E,
      isClockwise: d
    }
  ];
}
function Zi(t, e, n) {
  const o = e.edgeGroups;
  Object.entries(o).filter(([i, s]) => s.summarize && !(i in t)).forEach(([i, s]) => {
    const a = { stroke: void 0 };
    a.stroke = C(
      () => I.values(n.edge.summarized.stroke, s.edges)
    ), t[i] = a;
  }), Object.keys(t).forEach((i) => {
    var s;
    (s = e.edgeGroups[i]) != null && s.summarize || delete t[i];
  });
}
function yn(t, e) {
  return t.edges.map((n) => ({ edgeId: n, edge: e.objects.value[n] })).filter((n) => n.edge);
}
class Ue {
  static valueOf(e) {
    return Array.from(e.values());
  }
}
const Di = 3, Ni = 6, Kn = 500;
function tt(t) {
  return t === "touch" ? Ni : Di;
}
function qt(t, e, n, o) {
  let i = t.get(e);
  if (i)
    i.id !== n && (i = void 0);
  else {
    const c = Array.from(t.entries()).find(([u, h]) => h.id === n);
    if (c) {
      const [u, h] = c;
      t.delete(u), i = h;
    }
  }
  let s, a;
  return [i, s, a] = Yn(i, o, n), t.set(e, i), [s, a];
}
function Yn(t, e, n) {
  const o = Date.now();
  t && o - t.lastTime <= Kn ? (t.count++, t.lastTime = o) : t = { count: 1, lastTime: o, id: n };
  const i = {
    view: window,
    screenX: e.screenX,
    screenY: e.screenY,
    clientX: e.clientX,
    clientY: e.clientY,
    ctrlKey: e.ctrlKey,
    shiftKey: e.shiftKey,
    altKey: e.altKey,
    metaKey: e.metaKey,
    button: e.button,
    buttons: e.buttons,
    detail: t.count
  };
  let s, a;
  return e instanceof PointerEvent ? (Object.assign(i, {
    pointerId: e.pointerId,
    width: e.width,
    height: e.height,
    pressure: e.pressure,
    tangentialPressure: e.tangentialPressure,
    tiltX: e.tiltX,
    tiltY: e.tiltY,
    twist: e.twist,
    pointerType: e.pointerType,
    isPrimary: e.isPrimary
  }), s = new PointerEvent("click", i), t.count === 2 && (a = new PointerEvent("dblclick", i))) : (s = new MouseEvent("click", i), t.count === 2 && (a = new MouseEvent("dblclick", i))), [t, s, a];
}
function Xt(t) {
  const e = Date.now();
  Array.from(t.entries()).filter(([n, o]) => e - o.lastTime > Kn).map(([n, o]) => t.delete(n));
}
function ji(t, e, n, o, i, s, a) {
  const c = {
    pointers: /* @__PURE__ */ new Map(),
    follow: {
      followedPointerId: -1,
      nodeBasePositions: {}
    },
    hoveredNodesPre: /* @__PURE__ */ new Set(),
    clicks: /* @__PURE__ */ new Map()
  }, u = {
    pointermove: d,
    pointerup: x,
    pointercancel: b
  };
  function h(p) {
    const v = c.follow.followedPointerId === p.pointerId, y = i.has(p.nodeId), k = !(p.pointerId in c.pointers);
    if (v && k || v && !y) {
      const O = Ue.valueOf(c.pointers).find((L) => i.has(L.nodeId));
      if (!O) {
        c.follow = { followedPointerId: -1, nodeBasePositions: {} };
        return;
      }
      p = O, c.follow.followedPointerId = p.pointerId;
    } else {
      const O = c.pointers.get(c.follow.followedPointerId);
      if (!O) {
        c.follow = { followedPointerId: -1, nodeBasePositions: {} };
        return;
      }
      p = O;
    }
    if (v || y) {
      const O = Ue.valueOf(c.pointers).map((L) => L.nodeId);
      c.follow.nodeBasePositions = Object.fromEntries(
        Array.from(i).filter((L) => !O.includes(L)).filter((L) => {
          var T;
          return (T = t[L]) == null ? void 0 : T.draggable;
        }).map((L) => [L, Et(e.nodes, L)])
      ), p.dragBasePosition = oe({}, p.latestPosition), p.nodeBasePosition = Et(e.nodes, p.nodeId);
    }
  }
  F(i, (p) => {
    const v = c.pointers.get(c.follow.followedPointerId);
    v && h(v), p.size > 0 && n.selectionMode.value !== "node" ? n.selectionMode.value = "node" : p.size === 0 && n.selectionMode.value === "node" && (n.selectionMode.value = "container");
  }), F(n.selectionMode, (p) => {
    p !== "node" && i.clear();
  });
  function f(p, v) {
    const y = p.dragBasePosition.x - v.pageX, k = p.dragBasePosition.y - v.pageY, O = c.follow.followedPointerId == p.pointerId ? oe({
      [p.nodeId]: p.nodeBasePosition
    }, c.follow.nodeBasePositions) : { [p.nodeId]: p.nodeBasePosition }, L = s.value;
    return Object.fromEntries(
      Object.entries(O).map(([T, z]) => [
        T,
        {
          x: z.x - y / L,
          y: z.y - k / L
        }
      ])
    );
  }
  function r(p, v) {
    var k, O;
    if (v.isTrusted || v.shiftKey && !["container", "node"].includes(n.selectionMode.value))
      return;
    n.selectionMode.value = "node";
    const y = (O = (k = t[p]) == null ? void 0 : k.selectable) != null ? O : !1;
    if (y) {
      const L = Ue.valueOf(c.pointers).filter((T) => i.has(T.nodeId)).length > 0;
      v.shiftKey || L ? i.has(p) ? i.delete(p) : typeof y == "number" && i.size >= y || i.add(p) : i.has(p) || (i.clear(), i.add(p));
    }
    a.emit("node:click", { node: p, event: v });
  }
  function l(p, v) {
    v.isTrusted || a.emit("node:dblclick", { node: p, event: v });
  }
  function d(p) {
    var O;
    p.stopPropagation();
    const v = c.pointers.get(p.pointerId);
    if (!v)
      return;
    v.latestPosition = { x: p.pageX, y: p.pageY }, v.moveCounter++;
    const y = tt(p.pointerType);
    if (v.moveCounter <= y || !((O = t[v.nodeId]) != null && O.draggable))
      return;
    if (v.moveCounter === y + 1) {
      const L = f(v, {
        pointerId: v.pointerId,
        pageX: v.dragBasePosition.x,
        pageY: v.dragBasePosition.y
      });
      a.emit("node:dragstart", L);
    }
    const k = f(v, p);
    a.emit("node:pointermove", k);
  }
  function b(p) {
    p.stopPropagation();
    let v = c.pointers.get(p.pointerId);
    if (v) {
      for (v of c.pointers.values()) {
        const y = v.nodeId, k = tt(p.pointerType);
        if (v.moveCounter > k) {
          const L = f(v, {
            pointerId: v.pointerId,
            pageX: v.latestPosition.x,
            pageY: v.latestPosition.y
          });
          a.emit("node:dragend", L);
        }
        a.emit("node:pointerup", { node: y, event: p });
      }
      c.pointers.clear(), c.follow = { followedPointerId: -1, nodeBasePositions: {} }, le(u).forEach(([y, k]) => {
        document.removeEventListener(y, k);
      }), n.viewMode.value = "default";
    }
  }
  function x(p) {
    var L, T, z;
    p.stopPropagation();
    const v = c.pointers.get(p.pointerId);
    if (!v)
      return;
    c.pointers.delete(p.pointerId);
    const y = v.nodeId, k = tt(p.pointerType), O = v.moveCounter > k;
    if (O) {
      if ((L = t[v.nodeId]) != null && L.draggable) {
        const A = f(v, p);
        a.emit("node:dragend", A), a.emit("node:pointerup", { node: y, event: p });
      }
    } else
      a.emit("node:pointerup", { node: y, event: p });
    if (!O) {
      const [A, j] = qt(
        c.clicks,
        v.pointerId,
        y,
        p
      );
      (T = v.eventTarget) == null || T.dispatchEvent(A), j && ((z = v.eventTarget) == null || z.dispatchEvent(j));
    }
    c.pointers.size === 0 ? (c.follow = { followedPointerId: -1, nodeBasePositions: {} }, le(u).forEach(([A, j]) => {
      document.removeEventListener(A, j);
    }), Xt(c.clicks), n.viewMode.value = "default") : h(v), o.clear(), c.hoveredNodesPre.forEach(o.add, o);
  }
  function m(p, v) {
    if (v.button == 2 || (v.stopPropagation(), !["default", "node"].includes(n.viewMode.value)))
      return;
    c.pointers.size == 0 && (n.viewMode.value = "node", le(u).forEach(([k, O]) => {
      document.addEventListener(k, O);
    }));
    const y = {
      pointerId: v.pointerId,
      nodeId: p,
      moveCounter: 0,
      nodeBasePosition: Et(e.nodes, p),
      dragBasePosition: { x: v.pageX, y: v.pageY },
      latestPosition: { x: v.pageX, y: v.pageY },
      eventTarget: v.currentTarget
    };
    c.pointers.set(v.pointerId, y), i.has(p) && (c.follow.followedPointerId < 0 ? (c.follow.followedPointerId = v.pointerId, h(y)) : delete c.follow.nodeBasePositions[y.nodeId]), a.emit("node:pointerdown", { node: p, event: v });
  }
  function g(p, v) {
    c.hoveredNodesPre.add(p), !(c.pointers.size > 0) && (o.add(p), a.emit("node:pointerover", { node: p, event: v }));
  }
  function w(p, v) {
    c.hoveredNodesPre.delete(p), !(c.pointers.size > 0) && (o.delete(p), a.emit("node:pointerout", { node: p, event: v }));
  }
  function E(p, v) {
    v.stopPropagation(), a.emit("node:contextmenu", { node: p, event: v });
  }
  return {
    handleNodePointerDownEvent: m,
    handleNodePointerOverEvent: g,
    handleNodePointerOutEvent: w,
    handleNodeClickEvent: r,
    handleNodeDoubleClickEvent: l,
    handleNodeContextMenu: E
  };
}
function Et(t, e) {
  var o;
  const n = (o = t[e]) != null ? o : { x: 0, y: 0 };
  return oe({}, n);
}
function Bi(t, e, n, o, i) {
  const s = {
    pointers: /* @__PURE__ */ new Map(),
    // <PointerId, ...>
    pointerPeekCount: 0,
    clicks: /* @__PURE__ */ new Map()
  }, a = {
    pointerup: u,
    pointercancel: h
  };
  F(o, (v) => {
    v.size > 0 && e.selectionMode.value !== "edge" ? e.selectionMode.value = "edge" : v.size === 0 && e.selectionMode.value === "edge" && (e.selectionMode.value = "container");
  }), F(e.selectionMode, (v) => {
    v !== "edge" && o.clear();
  });
  function c(v, y) {
    if (y.button == 2 || (y.stopPropagation(), !["default", "edge"].includes(e.viewMode.value)))
      return;
    s.pointers.size == 0 && (e.viewMode.value = "edge", le(a).forEach(([O, L]) => {
      document.addEventListener(O, L);
    }), s.pointerPeekCount = 0), s.pointerPeekCount++;
    const k = {
      pointerId: y.pointerId,
      id: v,
      eventTarget: y.currentTarget
    };
    s.pointers.set(y.pointerId, k), i.emit("edge:pointerdown", he(v, y));
  }
  function u(v) {
    var T, z;
    v.stopPropagation();
    const y = s.pointers.get(v.pointerId);
    if (!y)
      return;
    s.pointers.delete(v.pointerId);
    const k = y.id;
    i.emit("edge:pointerup", he(k, v));
    const [O, L] = qt(
      s.clicks,
      y.pointerId,
      k instanceof Array ? k.join(",") : k,
      v
    );
    (T = y.eventTarget) == null || T.dispatchEvent(O), L && ((z = y.eventTarget) == null || z.dispatchEvent(L)), s.pointers.size === 0 && (s.pointerPeekCount = 0, le(a).forEach(([A, j]) => {
      document.removeEventListener(A, j);
    }), Xt(s.clicks), e.viewMode.value = "default");
  }
  function h(v) {
    if (v.stopPropagation(), !!s.pointers.get(v.pointerId)) {
      for (const k of s.pointers.values()) {
        const O = k.id;
        i.emit("edge:pointerup", he(O, v));
      }
      s.pointers.clear(), s.pointerPeekCount = 0, le(a).forEach(([k, O]) => {
        document.removeEventListener(k, O);
      }), i.emit("view:mode", "default");
    }
  }
  function f(v, y) {
    var L;
    if (y.isTrusted || y.shiftKey && !["container", "edge"].includes(e.selectionMode.value))
      return;
    e.selectionMode.value = "edge";
    const k = v instanceof Array ? v : [v], O = Ue.valueOf(s.pointers).filter((T) => (T.id instanceof Array ? T.id : [T.id]).every((A) => o.has(A))).length > 0;
    if (v instanceof Array)
      k.find((z) => {
        var A;
        return (A = t[z]) == null ? void 0 : A.selectable;
      }) && (y.shiftKey || O ? k.some((z) => o.has(z)) ? k.forEach((z) => o.delete(z)) : k.forEach((z) => {
        var j;
        const A = (j = t[z]) == null ? void 0 : j.selectable;
        typeof A == "number" && o.size >= A || o.add(z);
      }) : (o.clear(), k.forEach((z) => o.add(z))));
    else {
      const T = (L = t[v]) == null ? void 0 : L.selectable;
      T && (y.shiftKey || O ? o.has(v) ? o.delete(v) : typeof T == "number" && o.size >= T || o.add(v) : o.has(v) || (o.clear(), o.add(v)));
    }
    i.emit("edge:click", he(v, y));
  }
  function r(v, y) {
    y.isTrusted || i.emit("edge:dblclick", he(v, y));
  }
  function l(v, y) {
    n.add(v), i.emit("edge:pointerover", he(v, y));
  }
  function d(v, y) {
    n.delete(v), i.emit("edge:pointerout", he(v, y));
  }
  function b(v, y) {
    y.stopPropagation(), i.emit("edge:contextmenu", he(v, y));
  }
  function x(v, y) {
    if (y.button == 2 || (y.stopPropagation(), !["default", "edge"].includes(e.viewMode.value)))
      return;
    s.pointers.size == 0 && (e.viewMode.value = "edge", le(a).forEach(([O, L]) => {
      document.addEventListener(O, L);
    }), s.pointerPeekCount = 0), s.pointerPeekCount++;
    const k = {
      pointerId: y.pointerId,
      id: v,
      eventTarget: y.currentTarget
    };
    s.pointers.set(y.pointerId, k), i.emit("edge:pointerdown", he(v, y));
  }
  function m(v, y) {
    v.forEach((k) => n.add(k)), i.emit("edge:pointerover", he(v, y));
  }
  function g(v, y) {
    v.forEach((k) => n.delete(k)), i.emit("edge:pointerout", he(v, y));
  }
  function w(v, y) {
    f(v, y);
  }
  function E(v, y) {
    r(v, y);
  }
  function p(v, y) {
    y.stopPropagation(), i.emit("edge:contextmenu", he(v, y));
  }
  return {
    handleEdgePointerDownEvent: c,
    handleEdgePointerOverEvent: l,
    handleEdgePointerOutEvent: d,
    handleEdgeClickEvent: f,
    handleEdgeDoubleClickEvent: r,
    handleEdgeContextMenu: b,
    handleEdgesPointerDownEvent: x,
    handleEdgesPointerOverEvent: m,
    handleEdgesPointerOutEvent: g,
    handleEdgesClickEvent: w,
    handleEdgesDoubleClickEvent: E,
    handleEdgesContextMenu: p
  };
}
function he(t, e) {
  return t instanceof Array ? {
    edges: t,
    event: e,
    summarized: !0
  } : {
    edge: t,
    edges: [t],
    event: e,
    summarized: !1
  };
}
function Ri(t, e, n, o) {
  const i = {
    moveCounter: 0,
    pointerCounter: 0,
    clickState: void 0
  }, s = {
    pointermove: c,
    pointerup: u,
    pointercancel: u
  };
  function a(d) {
    i.moveCounter = 0, i.pointerCounter === 0 && le(s).forEach(([b, x]) => {
      document.addEventListener(b, x, { passive: !0 });
    }), i.pointerCounter++;
  }
  function c(d) {
    i.moveCounter++;
  }
  function u(d) {
    if (i.pointerCounter--, i.pointerCounter <= 0) {
      i.pointerCounter = 0, le(s).forEach(([x, m]) => {
        document.removeEventListener(x, m);
      });
      const b = tt(d.pointerType);
      if (i.moveCounter <= b) {
        if (d.shiftKey && e.selectionMode.value !== "container")
          return;
        e.selectionMode.value = "container";
        const [x, m, g] = Yn(
          i.clickState,
          d,
          "view"
        );
        i.clickState = x, t.value.dispatchEvent(m), g && t.value.dispatchEvent(g);
      }
    }
  }
  function h(d) {
    d.isTrusted || (d.stopPropagation(), o.emit("view:click", { event: d }));
  }
  function f(d) {
    d.isTrusted || (d.stopPropagation(), o.emit("view:dblclick", { event: d }));
  }
  function r(d) {
    o.emit("view:contextmenu", { event: d }), i.pointerCounter > 0 && (i.pointerCounter = 0, le(s).forEach(([b, x]) => {
      var m;
      (m = t.value) == null || m.removeEventListener(b, x);
    }));
  }
  const l = (d) => {
    d.preventDefault();
  };
  Be(() => {
    const d = t.value;
    d && (d.addEventListener("pointerdown", a, { passive: !0 }), d.addEventListener("click", h, { passive: !1 }), d.addEventListener("dblclick", f, { passive: !1 }), d.addEventListener("contextmenu", r, { passive: !1 }), n.value && d.addEventListener("wheel", l, { passive: !1 }));
  }), Xe(() => {
    const d = t.value;
    d && (d.removeEventListener("pointerdown", a), d.removeEventListener("click", h), d.removeEventListener("dblclick", f), d.removeEventListener("contextmenu", r), n.value && d.removeEventListener("wheel", l));
  }), F(n, (d, b) => {
    const x = t.value;
    !x || d === b || (d ? x.addEventListener("wheel", l, { passive: !1 }) : x.removeEventListener("wheel", l));
  });
}
function Vi(t, e, n, o, i, s) {
  const a = {
    pointers: /* @__PURE__ */ new Map(),
    // <PointerId, ...>
    pointerPeekCount: 0,
    clicks: /* @__PURE__ */ new Map()
  };
  function c(g, w) {
    var E, p;
    return i.value ? { path: (p = (E = t[g]) == null ? void 0 : E.path) != null ? p : g, event: w } : { path: g, event: w };
  }
  const u = {
    pointerup: f,
    pointercancel: r
  };
  F(o, (g) => {
    g.size > 0 && e.selectionMode.value !== "path" ? e.selectionMode.value = "path" : g.size === 0 && e.selectionMode.value === "path" && (e.selectionMode.value = "container");
  }), F(e.selectionMode, (g) => {
    g !== "path" && o.clear();
  });
  function h(g, w) {
    var p;
    if (!((p = t[g]) != null && p.clickable) || w.button == 2 || (w.stopPropagation(), !["default", "path"].includes(e.viewMode.value)))
      return;
    a.pointers.size == 0 && (e.viewMode.value = "path", le(u).forEach(([v, y]) => {
      document.addEventListener(v, y);
    }), a.pointerPeekCount = 0), a.pointerPeekCount++;
    const E = {
      pointerId: w.pointerId,
      id: g,
      eventTarget: w.currentTarget
    };
    a.pointers.set(w.pointerId, E), s.emit("path:pointerdown", c(g, w));
  }
  function f(g) {
    var y, k;
    const w = a.pointers.get(g.pointerId);
    if (!w)
      return;
    g.stopPropagation(), a.pointers.delete(g.pointerId);
    const E = w.id;
    s.emit("path:pointerup", c(E, g));
    const [p, v] = qt(
      a.clicks,
      w.pointerId,
      E,
      g
    );
    (y = w.eventTarget) == null || y.dispatchEvent(p), v && ((k = w.eventTarget) == null || k.dispatchEvent(v)), a.pointers.size === 0 && (a.pointerPeekCount = 0, le(u).forEach(([O, L]) => {
      document.removeEventListener(O, L);
    }), Xt(a.clicks), e.viewMode.value = "default");
  }
  function r(g) {
    if (a.pointers.get(g.pointerId)) {
      g.stopPropagation();
      for (const E of a.pointers.values()) {
        const p = E.id;
        s.emit("path:pointerup", c(p, g));
      }
      a.pointers.clear(), a.pointerPeekCount = 0, le(u).forEach(([E, p]) => {
        document.removeEventListener(E, p);
      }), s.emit("view:mode", "default");
    }
  }
  function l(g, w) {
    var E;
    (E = t[g]) != null && E.hoverable && (n.add(g), s.emit("path:pointerover", c(g, w)));
  }
  function d(g, w) {
    var E;
    (E = t[g]) != null && E.hoverable && (n.delete(g), s.emit("path:pointerout", c(g, w)));
  }
  function b(g, w) {
    var p, v, y;
    if (w.isTrusted || !((p = t[g]) != null && p.clickable) || w.shiftKey && !["container", "path"].includes(e.selectionMode.value))
      return;
    e.selectionMode.value = "path";
    const E = (y = (v = t[g]) == null ? void 0 : v.selectable) != null ? y : !1;
    if (E) {
      const k = Ue.valueOf(a.pointers).filter((O) => o.has(O.id)).length > 0;
      w.shiftKey || k ? o.has(g) ? o.delete(g) : typeof E == "number" && o.size >= E || o.add(g) : o.has(g) || (o.clear(), o.add(g));
    }
    s.emit("path:click", c(g, w));
  }
  function x(g, w) {
    var E;
    w.isTrusted || (E = t[g]) != null && E.clickable && s.emit("path:dblclick", c(g, w));
  }
  function m(g, w) {
    var E;
    (E = t[g]) != null && E.clickable && (w.stopPropagation(), s.emit("path:contextmenu", c(g, w)));
  }
  return {
    handlePathPointerDownEvent: h,
    handlePathPointerOverEvent: l,
    handlePathPointerOutEvent: d,
    handlePathClickEvent: b,
    handlePathDoubleClickEvent: x,
    handlePathContextMenu: m
  };
}
function Wi(t) {
  return ve(this, null, function* () {
    const n = yield (yield fetch(t)).blob();
    return new Promise((o, i) => {
      try {
        const s = new FileReader();
        s.onload = function() {
          o(this.result);
        }, s.readAsDataURL(n);
      } catch (s) {
        i(s);
      }
    });
  });
}
function At(t, e, n) {
  var s;
  const o = t.createSVGPoint();
  o.x = n.x, o.y = n.y;
  const i = o.matrixTransform((s = e.getCTM()) == null ? void 0 : s.inverse());
  return { x: i.x, y: i.y };
}
function Hi(t, e, n) {
  const o = t.createSVGPoint();
  o.x = n.x, o.y = n.y;
  const i = o.matrixTransform(e.getCTM());
  return { x: i.x, y: i.y };
}
function Qn(t, e, n) {
  var h;
  const o = t.cloneNode(!0), i = e.getBBox(), s = 1 / n, a = {
    x: Math.floor((i.x - 10) * s),
    y: Math.floor((i.y - 10) * s),
    width: Math.ceil((i.width + 20) * s),
    height: Math.ceil((i.height + 20) * s)
  };
  o.setAttribute("width", a.width.toString()), o.setAttribute("height", a.height.toString());
  const c = o.querySelector(".v-ng-viewport");
  c.setAttribute("transform", `translate(${-a.x} ${-a.y}), scale(${s})`), c.removeAttribute("style"), o.setAttribute("viewBox", `0 0 ${a.width} ${a.height}`), o.removeAttribute("style");
  const u = document.createNodeIterator(o, NodeFilter.SHOW_COMMENT);
  for (; u.nextNode(); ) {
    const f = u.referenceNode;
    (h = f.parentNode) == null || h.removeChild(f);
  }
  return o;
}
function _i(t) {
  return ve(this, null, function* () {
    let e = !1, n = t.getAttribute("href");
    if (n || (e = !0, n = t.getAttribute("xlink:href")), !(!n || n.startsWith("data:")))
      try {
        const o = yield Wi(n);
        t.setAttribute(e ? "xlink:href" : "href", o);
      } catch (o) {
        console.warn("Image download failed.", n);
        return;
      }
  });
}
function Ui(i, s, a) {
  return ve(this, arguments, function* (t, e, n, o = {}) {
    const c = Qn(t, e, n);
    if (o.embedImages) {
      const h = Array.from(c.querySelectorAll("image")).map((f) => _i(f));
      yield Promise.all(h);
    }
    return c;
  });
}
function Fi(t, e, n, o, i, s) {
  const a = C(
    () => !!s.node.selectable && s.view.boxSelectionEnabled
  ), c = C(() => e.viewMode.value === "box-selection"), u = B(), h = B(), f = {
    pointers: /* @__PURE__ */ new Set(),
    // Set of pointers being pressed
    points: /* @__PURE__ */ new Map(),
    // Point indicated by each pointer
    startPoint: null,
    // Point where the drag started
    selectedNodesAtSelectStarted: /* @__PURE__ */ new Set(),
    // Nodes selected at the start of the selection
    selectionType: "append",
    // Selection method for nodes included in the dragged range
    options: {
      // options specified by user at start of selection mode
      stopTrigger: "pointerup",
      selectionType: "append",
      selectionTypeWithShiftKey: "same"
    }
  }, r = _o(
    () => {
      qi(
        q(t.value, "container"),
        q(u.value, "viewport"),
        h,
        n.nodes,
        o,
        i,
        f.selectedNodesAtSelectStarted,
        f.selectionType
      );
    },
    50,
    { maxWait: 100 }
  ), l = new Xi(
    t,
    b,
    x,
    m,
    d,
    g
  );
  function d(y) {
    if (f.options.stopTrigger !== "click" || f.pointers.size > 0) return;
    const k = { x: y.offsetX, y: y.offsetY };
    f.startPoint && V.fromObject(f.startPoint).distance(k) > 10 || v();
  }
  function b(y) {
    y.stopPropagation();
    const k = { x: y.offsetX, y: y.offsetY };
    f.pointers.size === 0 && (f.startPoint = k, l.activate(), f.selectedNodesAtSelectStarted.clear(), i.forEach((O) => f.selectedNodesAtSelectStarted.add(O)), f.options.selectionTypeWithShiftKey === "same" ? f.selectionType = f.options.selectionType : f.selectionType = y.shiftKey ? f.options.selectionTypeWithShiftKey : f.options.selectionType), f.pointers.has(y.pointerId) || f.pointers.add(y.pointerId), f.points.set(y.pointerId, k), w();
  }
  function x(y) {
    if (f.pointers.delete(y.pointerId), f.pointers.size === 1) {
      const k = q(t.value).getBoundingClientRect(), O = { x: y.x - k.x, y: y.y - k.y };
      f.startPoint = O;
    } else f.pointers.size === 0 && (l.deactivate(), f.options.stopTrigger === "pointerup" && v(), f.selectedNodesAtSelectStarted.clear());
    w();
  }
  function m(y) {
    const k = q(t.value).getBoundingClientRect(), O = { x: y.x - k.x, y: y.y - k.y };
    f.points.set(y.pointerId, O), w(), r();
  }
  function g(y) {
    y.key === "Escape" && f.options.stopTrigger !== "manual" && (y.stopPropagation(), v());
  }
  function w() {
    let y, k;
    const O = Array.from(f.pointers);
    if (O.length >= 2) {
      const L = O[0], T = O[O.length - 1];
      y = f.points.get(L), k = f.points.get(T);
    } else if (f.startPoint && O.length === 1) {
      const L = O[0];
      y = f.startPoint, k = f.points.get(L);
    } else
      y = void 0, k = void 0;
    y && k ? (h.value = {
      pos: {
        x: Math.min(y.x, k.x),
        y: Math.min(y.y, k.y)
      },
      size: {
        width: Math.abs(k.x - y.x),
        height: Math.abs(k.y - y.y)
      }
    }, h.value.size.width === 0 && (h.value.size.width = 1), h.value.size.height === 0 && (h.value.size.height = 1)) : h.value && (h.value = void 0);
  }
  const E = new Gi(
    t,
    // key down
    (y) => {
      a.value && s.view.selection.detector(y) && (p({
        stop: "manual",
        type: "append",
        withShiftKey: "invert"
      }), E.activate());
    },
    // key up
    (y) => {
      a.value && s.view.selection.detector(y) && (f.pointers.size === 0 ? v() : f.options.stopTrigger = "pointerup", E.deactivate());
    }
  );
  F(a, (y) => {
    y ? E.register() : E.unregister();
  }), Be(() => {
    var y;
    u.value = (y = t.value) == null ? void 0 : y.querySelector(".v-ng-viewport"), a.value && E.register();
  }), Xe(() => {
    v(), a.value && E.unregister();
  });
  function p(y = {}) {
    var k, O, L;
    f.options = {
      stopTrigger: (k = y.stop) != null ? k : "pointerup",
      selectionType: (O = y.type) != null ? O : "append",
      selectionTypeWithShiftKey: (L = y.withShiftKey) != null ? L : "same"
    }, e.viewMode.value !== "box-selection" && (e.viewMode.value = "box-selection", f.pointers.clear(), l.register());
  }
  function v() {
    e.viewMode.value === "box-selection" && (e.viewMode.value = "default", l.unregister());
  }
  return { isBoxSelectionMode: c, selectionBox: h, startBoxSelection: p, stopBoxSelection: v };
}
function qi(t, e, n, o, i, s, a, c) {
  if (!n.value) return;
  const u = n.value, h = At(t, e, u.pos), f = At(t, e, {
    x: u.pos.x + u.size.width,
    y: u.pos.y + u.size.height
  }), r = new Set(
    Object.entries(o).filter(([l, d]) => h.x <= d.x && d.x <= f.x && h.y <= d.y && d.y <= f.y).map(([l, d]) => l)
  );
  if (c === "append")
    s.forEach((l) => {
      r.has(l) || s.delete(l);
    }), r.forEach((l) => {
      var b, x;
      const d = (x = (b = i[l]) == null ? void 0 : b.selectable) != null ? x : !1;
      (d === !0 || typeof d == "number" && s.size < d) && s.add(l);
    });
  else {
    const l = new Set(a);
    l.forEach((d) => {
      r.has(d) && l.delete(d);
    }), r.forEach((d) => {
      var b, x;
      if (!a.has(d)) {
        const m = (x = (b = i[d]) == null ? void 0 : b.selectable) != null ? x : !1;
        (m === !0 || typeof m == "number" && l.size < m) && l.add(d);
      }
    }), s.clear(), l.forEach((d) => s.add(d));
  }
}
class Xi {
  constructor(e, n, o, i, s, a) {
    this._container = e, this._handlePointerDownEvent = n, this._handlePointerUpEvent = o, this._handlePointerMoveEvent = i, this._handleClickEvent = s, this._handleKeyDownEvent = a, this._ignoreEvent = (c) => c.stopPropagation();
  }
  register() {
    const e = { capture: !0, passive: !1 }, n = q(this._container.value, "container");
    n.addEventListener("pointerdown", this._handlePointerDownEvent, e), n.addEventListener("click", this._handleClickEvent, e), n.addEventListener("pointerenter", this._ignoreEvent, e), n.addEventListener("pointerleave", this._ignoreEvent, e), document.addEventListener("keydown", this._handleKeyDownEvent, e);
  }
  activate() {
    const e = { capture: !0, passive: !1 };
    document.addEventListener("pointermove", this._handlePointerMoveEvent, e), document.addEventListener("pointerup", this._handlePointerUpEvent, e);
  }
  deactivate() {
    const e = { capture: !0 };
    document.removeEventListener("pointermove", this._handlePointerMoveEvent, e), document.removeEventListener("pointerup", this._handlePointerUpEvent, e);
  }
  unregister() {
    this.deactivate();
    const e = { capture: !0 };
    if (this._container.value) {
      const n = this._container.value;
      n.removeEventListener("pointerdown", this._handlePointerDownEvent, e), n.removeEventListener("click", this._handleClickEvent, e), n.removeEventListener("pointerenter", this._ignoreEvent, e), n.removeEventListener("pointerleave", this._ignoreEvent, e), document.removeEventListener("keydown", this._handleKeyDownEvent, e);
    }
  }
}
class Gi {
  constructor(e, n, o) {
    this._container = e, this._handleKeyDownEvent = n, this._handleKeyUpEvent = o, this._preventDefault = (i) => {
      i.stopPropagation(), i.preventDefault();
    };
  }
  register() {
    document.addEventListener("keydown", this._handleKeyDownEvent, { capture: !0, passive: !0 });
  }
  activate() {
    document.addEventListener("keyup", this._handleKeyUpEvent, { capture: !0, passive: !0 }), q(this._container.value, "container").addEventListener("contextmenu", this._preventDefault, { passive: !1 });
  }
  deactivate() {
    document.removeEventListener("keyup", this._handleKeyUpEvent, { capture: !0 }), this._container.value && this._container.value.removeEventListener("contextmenu", this._preventDefault);
  }
  unregister() {
    this.deactivate(), document.removeEventListener("keydown", this._handleKeyDownEvent, { capture: !0 });
  }
}
const Jn = /* @__PURE__ */ Symbol("mouseEventHandlers");
function Ki(t, e, n, o, i, s, a, c, u, h, f, r, l, d, b, x) {
  const m = {
    selectionMode: B("container"),
    viewMode: B("default")
  };
  a.size > 0 ? m.selectionMode.value = "node" : c.size > 0 ? m.selectionMode.value = "edge" : u.size > 0 && (m.selectionMode.value = "path"), F(m.viewMode, (w) => {
    x.emit("view:mode", w);
  }), Ri(t, m, d, x);
  const g = oe(oe(oe(oe({
    selectedNodes: a,
    hoveredNodes: h,
    selectedEdges: c,
    hoveredEdges: f,
    selectedPaths: u,
    hoveredPaths: r
  }, ji(
    o,
    e,
    m,
    h,
    a,
    n,
    x
  )), Bi(i, m, f, c, x)), Vi(
    s,
    m,
    r,
    u,
    l,
    x
  )), Fi(
    t,
    m,
    e,
    o,
    a,
    b
  ));
  return Me(Jn, g), g;
}
function Ye() {
  return q(Ce(Jn), "mouseEventHandlers");
}
function Yi(t) {
  return { all: t = t || /* @__PURE__ */ new Map(), on: function(e, n) {
    var o = t.get(e);
    o ? o.push(n) : t.set(e, [n]);
  }, off: function(e, n) {
    var o = t.get(e);
    o && (n ? o.splice(o.indexOf(n) >>> 0, 1) : t.set(e, []));
  }, emit: function(e, n) {
    var o = t.get(e);
    o && o.slice().map(function(i) {
      i(n);
    }), (o = t.get("*")) && o.slice().map(function(i) {
      i(e, n);
    });
  } };
}
const eo = /* @__PURE__ */ Symbol("emitter");
function Qi() {
  const t = Yi();
  return Me(eo, t), t;
}
function Ji() {
  return q(Ce(eo), "event emitter");
}
var wn = typeof globalThis != "undefined" ? globalThis : typeof window != "undefined" ? window : typeof global != "undefined" ? global : typeof self != "undefined" ? self : {};
function es(t) {
  return t && t.__esModule && Object.prototype.hasOwnProperty.call(t, "default") ? t.default : t;
}
var xt, En;
function dt() {
  if (En) return xt;
  En = 1, xt = {
    /**
     * Get global this object
     *
     * @return {Object}        global this object
     */
    getGlobalThis: function() {
      if (typeof globalThis != "undefined") return globalThis;
      if (typeof self != "undefined") return self;
      if (typeof window != "undefined") return window;
      if (typeof wn != "undefined") return wn;
      if (typeof this != "undefined") return this;
      throw new Error("Unable to locate global `this`");
    },
    /**
     * Extends an object
     *
     * @param  {Object} target object to extend
     * @param  {Object} source object to take properties from
     * @return {Object}        extended object
     */
    extend: function(e, n) {
      e = e || {};
      for (var o in n)
        this.isObject(n[o]) ? e[o] = this.extend(e[o], n[o]) : e[o] = n[o];
      return e;
    },
    /**
     * Checks if an object is a DOM element
     *
     * @param  {Object}  o HTML element or String
     * @return {Boolean}   returns true if object is a DOM element
     */
    isElement: function(e) {
      return e instanceof HTMLElement || e instanceof SVGElement || e instanceof SVGSVGElement || //DOM2
      e && typeof e == "object" && e !== null && e.nodeType === 1 && typeof e.nodeName == "string";
    },
    /**
     * Checks if an object is an Object
     *
     * @param  {Object}  o Object
     * @return {Boolean}   returns true if object is an Object
     */
    isObject: function(e) {
      return Object.prototype.toString.call(e) === "[object Object]";
    },
    /**
     * Checks if variable is Number
     *
     * @param  {Integer|Float}  n
     * @return {Boolean}   returns true if variable is Number
     */
    isNumber: function(e) {
      return !isNaN(parseFloat(e)) && isFinite(e);
    },
    /**
     * Search for an SVG element
     *
     * @param  {Object|String} elementOrSelector DOM Element or selector String
     * @return {Object|Null}                   SVG or null
     */
    getSvg: function(e) {
      var n, o;
      if (this.isElement(e))
        n = e;
      else if (typeof e == "string" || e instanceof String) {
        if (n = document.querySelector(e), !n)
          throw new Error(
            "Provided selector did not find any elements. Selector: " + e
          );
      } else
        throw new Error("Provided selector is not an HTML object nor String");
      if (n.tagName.toLowerCase() === "svg")
        o = n;
      else if (n.tagName.toLowerCase() === "object")
        o = n.contentDocument.documentElement;
      else if (n.tagName.toLowerCase() === "embed")
        o = n.getSVGDocument().documentElement;
      else
        throw n.tagName.toLowerCase() === "img" ? new Error(
          'Cannot script an SVG in an "img" element. Please use an "object" element or an in-line SVG.'
        ) : new Error("Cannot get SVG.");
      return o;
    },
    /**
     * Attach a given context to a function
     * @param  {Function} fn      Function
     * @param  {Object}   context Context
     * @return {Function}           Function with certain context
     */
    proxy: function(e, n) {
      return function() {
        return e.apply(n, arguments);
      };
    },
    /**
     * Returns object type
     * Uses toString that returns [object SVGPoint]
     * And than parses object type from string
     *
     * @param  {Object} o Any object
     * @return {String}   Object type
     */
    getType: function(e) {
      return Object.prototype.toString.apply(e).replace(/^\[object\s/, "").replace(/\]$/, "");
    },
    /**
     * If it is a touch event than add clientX and clientY to event object
     *
     * @param  {Event} evt
     * @param  {SVGSVGElement} svg
     */
    mouseAndTouchNormalize: function(e, n) {
      if (e.clientX === void 0 || e.clientX === null)
        if (e.clientX = 0, e.clientY = 0, e.touches !== void 0 && e.touches.length) {
          if (e.touches[0].clientX !== void 0)
            e.clientX = e.touches[0].clientX, e.clientY = e.touches[0].clientY;
          else if (e.touches[0].pageX !== void 0) {
            var o = n.getBoundingClientRect();
            e.clientX = e.touches[0].pageX - o.left, e.clientY = e.touches[0].pageY - o.top;
          }
        } else e.originalEvent !== void 0 && e.originalEvent.clientX !== void 0 && (e.clientX = e.originalEvent.clientX, e.clientY = e.originalEvent.clientY);
    },
    /**
     * If it is a touch event than add clientX and clientY to event object
     *
     * @param  {Event} evt
     * @param  {SVGSVGElement} svg
     * @param  {Number} touch
     */
    touchNormalize: function(e, n, o) {
      if (e.touches !== void 0 && e.touches.length) {
        if (e.touches[o].clientX !== void 0)
          e.clientX = e.touches[o].clientX, e.clientY = e.touches[o].clientY;
        else if (e.touches[o].pageX !== void 0) {
          var i = n.getBoundingClientRect();
          e.clientX = e.touches[o].pageX - i.left, e.clientY = e.touches[o].pageY - i.top;
        }
      } else
        (e.clientX === void 0 || e.clientX === null) && (e.clientX = 0, e.clientY = 0, e.originalEvent !== void 0 && e.originalEvent.clientX !== void 0 && (e.clientX = e.originalEvent.clientX, e.clientY = e.originalEvent.clientY));
    },
    /**
     * Check if an event is a double click/tap
     * TODO: For touch gestures use a library (hammer.js) that takes in account other events
     * (touchmove and touchend). It should take in account tap duration and traveled distance
     *
     * @param  {Event}  evt
     * @param  {Event}  prevEvt Previous Event
     * @return {Boolean}
     */
    isDblClick: function(e, n) {
      if (e.detail === 2)
        return !0;
      if (n != null) {
        var o = e.timeStamp - n.timeStamp, i = Math.sqrt(
          Math.pow(e.clientX - n.clientX, 2) + Math.pow(e.clientY - n.clientY, 2)
        );
        return o < 250 && i < 10;
      }
      return !1;
    },
    /**
     * Returns current timestamp as an integer
     *
     * @return {Number}
     */
    now: Date.now || function() {
      return (/* @__PURE__ */ new Date()).getTime();
    },
    // From underscore.
    // Returns a function, that, when invoked, will only be triggered at most once
    // during a given window of time. Normally, the throttled function will run
    // as much as it can, without ever going more than once per `wait` duration;
    // but if you'd like to disable the execution on the leading edge, pass
    // `{leading: false}`. To disable execution on the trailing edge, ditto.
    throttle: function(e, n, o) {
      var i = this, s, a, c, u = null, h = 0;
      o || (o = {});
      var f = function() {
        h = o.leading === !1 ? 0 : i.now(), u = null, c = e.apply(s, a), u || (s = a = null);
      };
      return function() {
        var r = i.now();
        !h && o.leading === !1 && (h = r);
        var l = n - (r - h);
        return s = this, a = arguments, l <= 0 || l > n ? (clearTimeout(u), u = null, h = r, c = e.apply(s, a), u || (s = a = null)) : !u && o.trailing !== !1 && (u = setTimeout(f, l)), c;
      };
    },
    /**
     * Create a requestAnimationFrame simulation
     *
     * @param  {Number|String} refreshRate
     * @return {Function}
     */
    createRequestAnimationFrame: function(e) {
      var n = null;
      return e !== "auto" && e < 60 && e > 1 && (n = Math.floor(1e3 / e)), n === null ? window.requestAnimationFrame || t(33) : t(n);
    },
    /**
     * Calculate distance of points
     *
     * @param  {SVGPoint} point1
     * @param  {SVGPoint} point2
     * @return {Number}
     */
    calculateDistance: function(e, n) {
      var o = e.x - n.x, i = e.y - n.y;
      return Math.sqrt(o * o + i * i);
    }
  };
  function t(e) {
    return function(n) {
      window.setTimeout(n, e);
    };
  }
  return xt;
}
var kt, xn;
function ts() {
  if (xn) return kt;
  xn = 1;
  var t = dt();
  return kt = (function() {
    var e = "", n, o, i, s = [], a = { passive: !0 }, c = { passive: !1 };
    t.getGlobalThis().addEventListener ? (n = "addEventListener", o = "removeEventListener") : (n = "attachEvent", o = "detachEvent", e = "on");
    function u() {
      return i || (i = "onwheel" in document.createElement("div") ? "wheel" : document.onmousewheel !== void 0 ? "mousewheel" : "DOMMouseScroll"), i;
    }
    function h(m, g) {
      var w = function(E) {
        !E && (E = window.event);
        var p = {
          // keep a ref to the original event object
          originalEvent: E,
          target: E.target || E.srcElement,
          type: "wheel",
          deltaMode: E.type == "MozMousePixelScroll" ? 0 : 1,
          deltaX: 0,
          delatZ: 0,
          preventDefault: function() {
            E.preventDefault ? E.preventDefault() : E.returnValue = !1;
          }
        };
        return u() == "mousewheel" ? (p.deltaY = -0.025 * E.wheelDelta, E.wheelDeltaX && (p.deltaX = -0.025 * E.wheelDeltaX)) : p.deltaY = E.detail, g(p);
      };
      return s.push({
        element: m,
        fn: w
      }), w;
    }
    function f(m) {
      for (var g = 0; g < s.length; g++)
        if (s[g].element === m)
          return s[g].fn;
      return function() {
      };
    }
    function r(m) {
      for (var g = 0; g < s.length; g++)
        if (s[g].element === m)
          return s.splice(g, 1);
    }
    function l(m, g, w, E) {
      var p;
      u() === "wheel" ? p = w : p = h(m, w), m[n](
        e + g,
        p,
        E ? a : c
      );
    }
    function d(m, g, w, E) {
      var p;
      u() === "wheel" ? p = w : p = f(m), m[o](
        e + g,
        p,
        E ? a : c
      ), r(m);
    }
    function b(m, g, w) {
      l(m, u(), g, w), u() == "DOMMouseScroll" && l(
        m,
        "MozMousePixelScroll",
        g,
        w
      );
    }
    function x(m, g, w) {
      d(m, u(), g, w), u() == "DOMMouseScroll" && d(
        m,
        "MozMousePixelScroll",
        g,
        w
      );
    }
    return {
      on: b,
      off: x
    };
  })(), kt;
}
var He, kn;
function Gt() {
  if (kn) return He;
  kn = 1;
  var t = dt(), e = "unknown";
  return (
    /*@cc_on!@*/
    typeof document != "undefined" && document.documentMode && (e = "ie"), He = {
      svgNS: "http://www.w3.org/2000/svg",
      xmlNS: "http://www.w3.org/XML/1998/namespace",
      xmlnsNS: "http://www.w3.org/2000/xmlns/",
      xlinkNS: "http://www.w3.org/1999/xlink",
      evNS: "http://www.w3.org/2001/xml-events",
      /**
       * Get svg dimensions: width and height
       *
       * @param  {SVGSVGElement} svg
       * @return {Object}     {width: 0, height: 0}
       */
      getBoundingClientRectNormalized: function(n) {
        if (n.clientWidth && n.clientHeight)
          return { width: n.clientWidth, height: n.clientHeight };
        if (n.getBoundingClientRect())
          return n.getBoundingClientRect();
        throw new Error("Cannot get BoundingClientRect for SVG.");
      },
      /**
       * Gets g element with class of "viewport" or creates it if it doesn't exist
       *
       * @param  {SVGSVGElement} svg
       * @return {SVGElement}     g (group) element
       */
      getOrCreateViewport: function(n, o) {
        var i = null;
        if (t.isElement(o) ? i = o : i = n.querySelector(o), !i) {
          var s = Array.prototype.slice.call(n.childNodes || n.children).filter(function(f) {
            return f.nodeName !== "defs" && f.nodeName !== "#text";
          });
          s.length === 1 && s[0].nodeName === "g" && s[0].getAttribute("transform") === null && (i = s[0]);
        }
        if (!i) {
          var a = "viewport-" + (/* @__PURE__ */ new Date()).toISOString().replace(/\D/g, "");
          i = document.createElementNS(this.svgNS, "g"), i.setAttribute("id", a);
          var c = n.childNodes || n.children;
          if (c && c.length > 0)
            for (var u = c.length; u > 0; u--)
              c[c.length - u].nodeName !== "defs" && i.appendChild(c[c.length - u]);
          n.appendChild(i);
        }
        var h = [];
        return i.getAttribute("class") && (h = i.getAttribute("class").split(" ")), ~h.indexOf("svg-pan-zoom_viewport") || (h.push("svg-pan-zoom_viewport"), i.setAttribute("class", h.join(" "))), i;
      },
      /**
       * Set SVG attributes
       *
       * @param  {SVGSVGElement} svg
       */
      setupSvgAttributes: function(n) {
        if (n.setAttribute("xmlns", this.svgNS), n.setAttributeNS(this.xmlnsNS, "xmlns:xlink", this.xlinkNS), n.setAttributeNS(this.xmlnsNS, "xmlns:ev", this.evNS), n.parentNode !== null) {
          var o = n.getAttribute("style") || "";
          o.toLowerCase().indexOf("overflow") === -1 && n.setAttribute("style", "overflow: hidden; " + o);
        }
      },
      /**
       * How long Internet Explorer takes to finish updating its display (ms).
       */
      internetExplorerRedisplayInterval: 300,
      /**
       * Forces the browser to redisplay all SVG elements that rely on an
       * element defined in a 'defs' section. It works globally, for every
       * available defs element on the page.
       * The throttling is intentionally global.
       *
       * This is only needed for IE. It is as a hack to make markers (and 'use' elements?)
       * visible after pan/zoom when there are multiple SVGs on the page.
       * See bug report: https://connect.microsoft.com/IE/feedback/details/781964/
       * also see svg-pan-zoom issue: https://github.com/ariutta/svg-pan-zoom/issues/62
       */
      refreshDefsGlobal: t.throttle(
        function() {
          for (var n = document.querySelectorAll("defs"), o = n.length, i = 0; i < o; i++) {
            var s = n[i];
            s.parentNode.insertBefore(s, s);
          }
        },
        He ? He.internetExplorerRedisplayInterval : null
      ),
      /**
       * Sets the current transform matrix of an element
       *
       * @param {SVGElement} element
       * @param {SVGMatrix} matrix  CTM
       * @param {SVGElement} defs
       */
      setCTM: function(n, o, i) {
        var s = this, a = "matrix(" + o.a + "," + o.b + "," + o.c + "," + o.d + "," + o.e + "," + o.f + ")";
        n.setAttributeNS(null, "transform", a), "transform" in n.style ? n.style.transform = a : "-ms-transform" in n.style ? n.style["-ms-transform"] = a : "-webkit-transform" in n.style && (n.style["-webkit-transform"] = a), e === "ie" && i && (i.parentNode.insertBefore(i, i), window.setTimeout(function() {
          s.refreshDefsGlobal();
        }, s.internetExplorerRedisplayInterval));
      },
      /**
       * Instantiate an SVGPoint object with given event coordinates
       *
       * @param {Event} evt
       * @param  {SVGSVGElement} svg
       * @return {SVGPoint}     point
       */
      getEventPoint: function(n, o) {
        var i = o.createSVGPoint();
        return t.mouseAndTouchNormalize(n, o), i.x = n.clientX, i.y = n.clientY, i;
      },
      /**
       * Instantiate an SVGPoint object with given touch event coordinates
       *
       * @param {Event} evt
       * @param  {SVGSVGElement} svg
       * @param  {Number} touch
       * @return {SVGPoint}     point
       */
      getTouchPoint: function(n, o, i) {
        var s = o.createSVGPoint();
        return t.touchNormalize(n, o, i), s.x = n.clientX, s.y = n.clientY, s;
      },
      /**
       * Get SVG center point
       *
       * @param  {SVGSVGElement} svg
       * @return {SVGPoint}
       */
      getSvgCenterPoint: function(n, o, i) {
        return this.createSVGPoint(n, o / 2, i / 2);
      },
      /**
       * Create a SVGPoint with given x and y
       *
       * @param  {SVGSVGElement} svg
       * @param  {Number} x
       * @param  {Number} y
       * @return {SVGPoint}
       */
      createSVGPoint: function(n, o, i) {
        var s = n.createSVGPoint();
        return s.x = o, s.y = i, s;
      }
    }, He
  );
}
var Pt, Pn;
function ns() {
  if (Pn) return Pt;
  Pn = 1;
  var t = Gt();
  return Pt = {
    enable: function(e) {
      var n = e.svg.querySelector("defs");
      n || (n = document.createElementNS(t.svgNS, "defs"), e.svg.appendChild(n));
      var o = n.querySelector("style#svg-pan-zoom-controls-styles");
      if (!o) {
        var i = document.createElementNS(t.svgNS, "style");
        i.setAttribute("id", "svg-pan-zoom-controls-styles"), i.setAttribute("type", "text/css"), i.textContent = ".svg-pan-zoom-control { cursor: pointer; fill: black; fill-opacity: 0.333; } .svg-pan-zoom-control:hover { fill-opacity: 0.8; } .svg-pan-zoom-control-background { fill: white; fill-opacity: 0.5; } .svg-pan-zoom-control-background { fill-opacity: 0.8; }", n.appendChild(i);
      }
      var s = document.createElementNS(t.svgNS, "g");
      s.setAttribute("id", "svg-pan-zoom-controls"), s.setAttribute(
        "transform",
        "translate(" + (e.width - 70) + " " + (e.height - 76) + ") scale(0.75)"
      ), s.setAttribute("class", "svg-pan-zoom-control"), s.appendChild(this._createZoomIn(e)), s.appendChild(this._createZoomReset(e)), s.appendChild(this._createZoomOut(e)), e.svg.appendChild(s), e.controlIcons = s;
    },
    _createZoomIn: function(e) {
      var n = document.createElementNS(t.svgNS, "g");
      n.setAttribute("id", "svg-pan-zoom-zoom-in"), n.setAttribute("transform", "translate(30.5 5) scale(0.015)"), n.setAttribute("class", "svg-pan-zoom-control"), n.addEventListener(
        "click",
        function() {
          e.getPublicInstance().zoomIn();
        },
        !1
      ), n.addEventListener(
        "touchstart",
        function() {
          e.getPublicInstance().zoomIn();
        },
        !1
      );
      var o = document.createElementNS(t.svgNS, "rect");
      o.setAttribute("x", "0"), o.setAttribute("y", "0"), o.setAttribute("width", "1500"), o.setAttribute("height", "1400"), o.setAttribute("class", "svg-pan-zoom-control-background"), n.appendChild(o);
      var i = document.createElementNS(t.svgNS, "path");
      return i.setAttribute(
        "d",
        "M1280 576v128q0 26 -19 45t-45 19h-320v320q0 26 -19 45t-45 19h-128q-26 0 -45 -19t-19 -45v-320h-320q-26 0 -45 -19t-19 -45v-128q0 -26 19 -45t45 -19h320v-320q0 -26 19 -45t45 -19h128q26 0 45 19t19 45v320h320q26 0 45 19t19 45zM1536 1120v-960 q0 -119 -84.5 -203.5t-203.5 -84.5h-960q-119 0 -203.5 84.5t-84.5 203.5v960q0 119 84.5 203.5t203.5 84.5h960q119 0 203.5 -84.5t84.5 -203.5z"
      ), i.setAttribute("class", "svg-pan-zoom-control-element"), n.appendChild(i), n;
    },
    _createZoomReset: function(e) {
      var n = document.createElementNS(t.svgNS, "g");
      n.setAttribute("id", "svg-pan-zoom-reset-pan-zoom"), n.setAttribute("transform", "translate(5 35) scale(0.4)"), n.setAttribute("class", "svg-pan-zoom-control"), n.addEventListener(
        "click",
        function() {
          e.getPublicInstance().reset();
        },
        !1
      ), n.addEventListener(
        "touchstart",
        function() {
          e.getPublicInstance().reset();
        },
        !1
      );
      var o = document.createElementNS(
        t.svgNS,
        "rect"
      );
      o.setAttribute("x", "2"), o.setAttribute("y", "2"), o.setAttribute("width", "182"), o.setAttribute("height", "58"), o.setAttribute(
        "class",
        "svg-pan-zoom-control-background"
      ), n.appendChild(o);
      var i = document.createElementNS(
        t.svgNS,
        "path"
      );
      i.setAttribute(
        "d",
        "M33.051,20.632c-0.742-0.406-1.854-0.609-3.338-0.609h-7.969v9.281h7.769c1.543,0,2.701-0.188,3.473-0.562c1.365-0.656,2.048-1.953,2.048-3.891C35.032,22.757,34.372,21.351,33.051,20.632z"
      ), i.setAttribute(
        "class",
        "svg-pan-zoom-control-element"
      ), n.appendChild(i);
      var s = document.createElementNS(
        t.svgNS,
        "path"
      );
      return s.setAttribute(
        "d",
        "M170.231,0.5H15.847C7.102,0.5,0.5,5.708,0.5,11.84v38.861C0.5,56.833,7.102,61.5,15.847,61.5h154.384c8.745,0,15.269-4.667,15.269-10.798V11.84C185.5,5.708,178.976,0.5,170.231,0.5z M42.837,48.569h-7.969c-0.219-0.766-0.375-1.383-0.469-1.852c-0.188-0.969-0.289-1.961-0.305-2.977l-0.047-3.211c-0.03-2.203-0.41-3.672-1.142-4.406c-0.732-0.734-2.103-1.102-4.113-1.102h-7.05v13.547h-7.055V14.022h16.524c2.361,0.047,4.178,0.344,5.45,0.891c1.272,0.547,2.351,1.352,3.234,2.414c0.731,0.875,1.31,1.844,1.737,2.906s0.64,2.273,0.64,3.633c0,1.641-0.414,3.254-1.242,4.84s-2.195,2.707-4.102,3.363c1.594,0.641,2.723,1.551,3.387,2.73s0.996,2.98,0.996,5.402v2.32c0,1.578,0.063,2.648,0.19,3.211c0.19,0.891,0.635,1.547,1.333,1.969V48.569z M75.579,48.569h-26.18V14.022h25.336v6.117H56.454v7.336h16.781v6H56.454v8.883h19.125V48.569z M104.497,46.331c-2.44,2.086-5.887,3.129-10.34,3.129c-4.548,0-8.125-1.027-10.731-3.082s-3.909-4.879-3.909-8.473h6.891c0.224,1.578,0.662,2.758,1.316,3.539c1.196,1.422,3.246,2.133,6.15,2.133c1.739,0,3.151-0.188,4.236-0.562c2.058-0.719,3.087-2.055,3.087-4.008c0-1.141-0.504-2.023-1.512-2.648c-1.008-0.609-2.607-1.148-4.796-1.617l-3.74-0.82c-3.676-0.812-6.201-1.695-7.576-2.648c-2.328-1.594-3.492-4.086-3.492-7.477c0-3.094,1.139-5.664,3.417-7.711s5.623-3.07,10.036-3.07c3.685,0,6.829,0.965,9.431,2.895c2.602,1.93,3.966,4.73,4.093,8.402h-6.938c-0.128-2.078-1.057-3.555-2.787-4.43c-1.154-0.578-2.587-0.867-4.301-0.867c-1.907,0-3.428,0.375-4.565,1.125c-1.138,0.75-1.706,1.797-1.706,3.141c0,1.234,0.561,2.156,1.682,2.766c0.721,0.406,2.25,0.883,4.589,1.43l6.063,1.43c2.657,0.625,4.648,1.461,5.975,2.508c2.059,1.625,3.089,3.977,3.089,7.055C108.157,41.624,106.937,44.245,104.497,46.331z M139.61,48.569h-26.18V14.022h25.336v6.117h-18.281v7.336h16.781v6h-16.781v8.883h19.125V48.569z M170.337,20.14h-10.336v28.43h-7.266V20.14h-10.383v-6.117h27.984V20.14z"
      ), s.setAttribute(
        "class",
        "svg-pan-zoom-control-element"
      ), n.appendChild(s), n;
    },
    _createZoomOut: function(e) {
      var n = document.createElementNS(t.svgNS, "g");
      n.setAttribute("id", "svg-pan-zoom-zoom-out"), n.setAttribute("transform", "translate(30.5 70) scale(0.015)"), n.setAttribute("class", "svg-pan-zoom-control"), n.addEventListener(
        "click",
        function() {
          e.getPublicInstance().zoomOut();
        },
        !1
      ), n.addEventListener(
        "touchstart",
        function() {
          e.getPublicInstance().zoomOut();
        },
        !1
      );
      var o = document.createElementNS(t.svgNS, "rect");
      o.setAttribute("x", "0"), o.setAttribute("y", "0"), o.setAttribute("width", "1500"), o.setAttribute("height", "1400"), o.setAttribute("class", "svg-pan-zoom-control-background"), n.appendChild(o);
      var i = document.createElementNS(t.svgNS, "path");
      return i.setAttribute(
        "d",
        "M1280 576v128q0 26 -19 45t-45 19h-896q-26 0 -45 -19t-19 -45v-128q0 -26 19 -45t45 -19h896q26 0 45 19t19 45zM1536 1120v-960q0 -119 -84.5 -203.5t-203.5 -84.5h-960q-119 0 -203.5 84.5t-84.5 203.5v960q0 119 84.5 203.5t203.5 84.5h960q119 0 203.5 -84.5 t84.5 -203.5z"
      ), i.setAttribute("class", "svg-pan-zoom-control-element"), n.appendChild(i), n;
    },
    disable: function(e) {
      e.controlIcons && (e.controlIcons.parentNode.removeChild(e.controlIcons), e.controlIcons = null);
    }
  }, Pt;
}
var St, Sn;
function os() {
  if (Sn) return St;
  Sn = 1;
  var t = Gt(), e = dt(), n = function(o, i) {
    this.init(o, i);
  };
  return n.prototype.init = function(o, i) {
    this.viewport = o, this.options = i, this.originalState = { zoom: 1, x: 0, y: 0 }, this.activeState = { zoom: 1, x: 0, y: 0 }, this.updateCTMCached = e.proxy(this.updateCTM, this), this.requestAnimationFrame = e.createRequestAnimationFrame(
      this.options.refreshRate
    ), this.viewBox = { x: 0, y: 0, width: 0, height: 0 }, this.cacheViewBox();
    var s = this.processCTM();
    this.setCTM(s), this.updateCTM();
  }, n.prototype.cacheViewBox = function() {
    var o = this.options.svg.getAttribute("viewBox");
    if (o) {
      var i = o.split(/[\s\,]/).filter(function(a) {
        return a;
      }).map(parseFloat);
      this.viewBox.x = i[0], this.viewBox.y = i[1], this.viewBox.width = i[2], this.viewBox.height = i[3];
      var s = Math.min(
        this.options.width / this.viewBox.width,
        this.options.height / this.viewBox.height
      );
      this.activeState.zoom = isFinite(s) ? s : 1, this.activeState.x = (this.options.width - this.viewBox.width * s) / 2, this.activeState.y = (this.options.height - this.viewBox.height * s) / 2, this.updateCTMOnNextFrame(), this.options.svg.removeAttribute("viewBox");
    } else
      this.simpleViewBoxCache();
  }, n.prototype.simpleViewBoxCache = function() {
    var o = this.viewport.getBBox();
    this.viewBox.x = o.x, this.viewBox.y = o.y, this.viewBox.width = o.width, this.viewBox.height = o.height;
  }, n.prototype.getViewBox = function() {
    return e.extend({}, this.viewBox);
  }, n.prototype.processCTM = function() {
    var o = this.getCTM();
    if (this.options.fit || this.options.contain) {
      var i;
      this.options.fit ? i = Math.min(
        this.options.width / this.viewBox.width,
        this.options.height / this.viewBox.height
      ) : i = Math.max(
        this.options.width / this.viewBox.width,
        this.options.height / this.viewBox.height
      ), i = isFinite(i) ? i : 1, o.a = i, o.d = i, o.e = -this.viewBox.x * i, o.f = -this.viewBox.y * i;
    }
    if (this.options.center) {
      var s = (this.options.width - (this.viewBox.width + this.viewBox.x * 2) * o.a) * 0.5, a = (this.options.height - (this.viewBox.height + this.viewBox.y * 2) * o.a) * 0.5;
      o.e = s, o.f = a;
    }
    return this.originalState.zoom = o.a, this.originalState.x = o.e, this.originalState.y = o.f, o;
  }, n.prototype.getOriginalState = function() {
    return e.extend({}, this.originalState);
  }, n.prototype.getState = function() {
    return e.extend({}, this.activeState);
  }, n.prototype.getZoom = function() {
    return this.activeState.zoom;
  }, n.prototype.getRelativeZoom = function() {
    return this.activeState.zoom / this.originalState.zoom;
  }, n.prototype.computeRelativeZoom = function(o) {
    return o / this.originalState.zoom;
  }, n.prototype.getPan = function() {
    return { x: this.activeState.x, y: this.activeState.y };
  }, n.prototype.getCTM = function() {
    var o = this.options.svg.createSVGMatrix();
    return o.a = this.activeState.zoom, o.b = 0, o.c = 0, o.d = this.activeState.zoom, o.e = this.activeState.x, o.f = this.activeState.y, o;
  }, n.prototype.setCTM = function(o) {
    var i = this.isZoomDifferent(o), s = this.isPanDifferent(o);
    if (i || s) {
      if (i && (this.options.beforeZoom(
        this.getRelativeZoom(),
        this.computeRelativeZoom(o.a)
      ) === !1 ? (o.a = o.d = this.activeState.zoom, i = !1) : (this.updateCache(o), this.options.onZoom(this.getRelativeZoom()))), s) {
        var a = this.options.beforePan(this.getPan(), {
          x: o.e,
          y: o.f
        }), c = !1, u = !1;
        a === !1 ? (o.e = this.getPan().x, o.f = this.getPan().y, c = u = !0) : e.isObject(a) && (a.x === !1 ? (o.e = this.getPan().x, c = !0) : e.isNumber(a.x) && (o.e = a.x), a.y === !1 ? (o.f = this.getPan().y, u = !0) : e.isNumber(a.y) && (o.f = a.y)), c && u || !this.isPanDifferent(o) ? s = !1 : (this.updateCache(o), this.options.onPan(this.getPan()));
      }
      (i || s) && this.updateCTMOnNextFrame();
    }
  }, n.prototype.isZoomDifferent = function(o) {
    return this.activeState.zoom !== o.a;
  }, n.prototype.isPanDifferent = function(o) {
    return this.activeState.x !== o.e || this.activeState.y !== o.f;
  }, n.prototype.updateCache = function(o) {
    this.activeState.zoom = o.a, this.activeState.x = o.e, this.activeState.y = o.f;
  }, n.prototype.pendingUpdate = !1, n.prototype.updateCTMOnNextFrame = function() {
    this.pendingUpdate || (this.pendingUpdate = !0, this.requestAnimationFrame.call(window, this.updateCTMCached));
  }, n.prototype.updateCTM = function() {
    var o = this.getCTM();
    t.setCTM(this.viewport, o, this.defs), this.pendingUpdate = !1, this.options.onUpdatedCTM && this.options.onUpdatedCTM(o);
  }, St = function(o, i) {
    return new n(o, i);
  }, St;
}
var Mt, Mn;
function is() {
  if (Mn) return Mt;
  Mn = 1;
  var t = ts(), e = ns(), n = dt(), o = Gt(), i = os(), s = function(r, l) {
    this.init(r, l);
  }, a = {
    viewportSelector: ".svg-pan-zoom_viewport",
    // Viewport selector. Can be querySelector string or SVGElement
    panEnabled: !0,
    // enable or disable panning (default enabled)
    controlIconsEnabled: !1,
    // insert icons to give user an option in addition to mouse events to control pan/zoom (default disabled)
    zoomEnabled: !0,
    // enable or disable zooming (default enabled)
    dblClickZoomEnabled: !0,
    // enable or disable zooming by double clicking (default enabled)
    mouseWheelZoomEnabled: !0,
    // enable or disable zooming by mouse wheel (default enabled)
    preventMouseEventsDefault: !0,
    // enable or disable preventDefault for mouse events
    zoomScaleSensitivity: 0.1,
    // Zoom sensitivity
    minZoom: 0.5,
    // Minimum Zoom level
    maxZoom: 10,
    // Maximum Zoom level
    fit: !0,
    // enable or disable viewport fit in SVG (default true)
    contain: !1,
    // enable or disable viewport contain the svg (default false)
    center: !0,
    // enable or disable viewport centering in SVG (default true)
    refreshRate: "auto",
    // Maximum number of frames per second (altering SVG's viewport)
    beforeZoom: null,
    onZoom: null,
    beforePan: null,
    onPan: null,
    customEventsHandler: null,
    eventsListenerElement: null,
    onUpdatedCTM: null
  }, c = { passive: !0 }, u = { passive: !1 };
  s.prototype.init = function(r, l) {
    var d = this;
    this.svg = r, this.defs = r.querySelector("defs"), o.setupSvgAttributes(this.svg), this.options = n.extend(n.extend({}, a), l), this.state = "none";
    var b = o.getBoundingClientRectNormalized(r);
    this.width = b.width, this.height = b.height, this.viewport = i(
      o.getOrCreateViewport(this.svg, this.options.viewportSelector),
      {
        svg: this.svg,
        width: this.width,
        height: this.height,
        fit: this.options.fit,
        contain: this.options.contain,
        center: this.options.center,
        refreshRate: this.options.refreshRate,
        // Put callbacks into functions as they can change through time
        beforeZoom: function(m, g) {
          if (d.viewport && d.options.beforeZoom)
            return d.options.beforeZoom(m, g);
        },
        onZoom: function(m) {
          if (d.viewport && d.options.onZoom)
            return d.options.onZoom(m);
        },
        beforePan: function(m, g) {
          if (d.viewport && d.options.beforePan)
            return d.options.beforePan(m, g);
        },
        onPan: function(m) {
          if (d.viewport && d.options.onPan)
            return d.options.onPan(m);
        },
        onUpdatedCTM: function(m) {
          if (d.viewport && d.options.onUpdatedCTM)
            return d.options.onUpdatedCTM(m);
        }
      }
    );
    var x = this.getPublicInstance();
    x.setBeforeZoom(this.options.beforeZoom), x.setOnZoom(this.options.onZoom), x.setBeforePan(this.options.beforePan), x.setOnPan(this.options.onPan), x.setOnUpdatedCTM(this.options.onUpdatedCTM), this.options.controlIconsEnabled && e.enable(this), this.lastMouseWheelEventTime = Date.now(), this.setupHandlers();
  }, s.prototype.setupHandlers = function() {
    var r = this, l = null;
    if (this.eventListeners = {
      // Mouse down group
      pointerdown: function(m) {
        if (m.pointerType !== "touch") {
          var g = r.handleMouseDown(m, l);
          return l = m, g;
        }
      },
      touchstart: function(m) {
        var g = r.handleTouchStart(m, l);
        return l = m, g;
      },
      // Mouse up group
      pointerup: function(m) {
        if (m.pointerType !== "touch")
          return r.handleMouseUp(m);
      },
      touchend: function(m) {
        return r.handleTouchEnd(m);
      },
      // Mouse move group
      pointermove: function(m) {
        if (m.pointerType !== "touch")
          return r.handleMouseMove(m);
      },
      touchmove: function(m) {
        return r.handleTouchMove(m);
      },
      // Mouse leave group
      pointerleave: function(m) {
        if (m.pointerType !== "touch")
          return r.handleMouseUp(m);
      },
      pointercancel: function(m) {
        if (m.pointerType !== "touch")
          return r.handleMouseUp(m);
      },
      touchleave: function(m) {
        return r.handleTouchEnd(m);
      },
      touchcancel: function(m) {
        return r.handleTouchEnd(m);
      }
    }, this.options.customEventsHandler != null) {
      this.options.customEventsHandler.init({
        svgElement: this.svg,
        eventsListenerElement: this.options.eventsListenerElement,
        instance: this.getPublicInstance()
      });
      var d = this.options.customEventsHandler.haltEventListeners;
      if (d && d.length)
        for (var b = d.length - 1; b >= 0; b--)
          this.eventListeners.hasOwnProperty(d[b]) && delete this.eventListeners[d[b]];
    }
    for (var x in this.eventListeners)
      (this.options.eventsListenerElement || this.svg).addEventListener(
        x,
        this.eventListeners[x],
        this.options.preventMouseEventsDefault ? u : c
      );
    this.options.mouseWheelZoomEnabled && (this.options.mouseWheelZoomEnabled = !1, this.enableMouseWheelZoom());
  }, s.prototype.enableMouseWheelZoom = function() {
    if (!this.options.mouseWheelZoomEnabled) {
      var r = this;
      this.wheelListener = function(d) {
        return r.handleMouseWheel(d);
      };
      var l = !this.options.preventMouseEventsDefault;
      t.on(
        this.options.eventsListenerElement || this.svg,
        this.wheelListener,
        l
      ), this.options.mouseWheelZoomEnabled = !0;
    }
  }, s.prototype.disableMouseWheelZoom = function() {
    if (this.options.mouseWheelZoomEnabled) {
      var r = !this.options.preventMouseEventsDefault;
      t.off(
        this.options.eventsListenerElement || this.svg,
        this.wheelListener,
        r
      ), this.options.mouseWheelZoomEnabled = !1;
    }
  }, s.prototype.handleMouseWheel = function(r) {
    if (!(!this.options.zoomEnabled || this.state !== "none")) {
      this.options.preventMouseEventsDefault && (r.preventDefault ? r.preventDefault() : r.returnValue = !1);
      var l = r.deltaY || 1, d = Date.now() - this.lastMouseWheelEventTime, b = 3 + Math.max(0, 30 - d);
      this.lastMouseWheelEventTime = Date.now(), "deltaMode" in r && r.deltaMode === 0 && r.wheelDelta && (l = r.deltaY === 0 ? 0 : Math.abs(r.wheelDelta) / r.deltaY), l = -0.3 < l && l < 0.3 ? l : (l > 0 ? 1 : -1) * Math.log(Math.abs(l) + 10) / b;
      var x = this.svg.getScreenCTM().inverse(), m = o.getEventPoint(r, this.svg).matrixTransform(
        x
      ), g = Math.pow(1 + this.options.zoomScaleSensitivity, -1 * l);
      this.zoomAtPoint(g, m);
    }
  }, s.prototype.zoomAtPoint = function(r, l, d) {
    var b = this.viewport.getOriginalState();
    d ? (r = Math.max(
      this.options.minZoom * b.zoom,
      Math.min(this.options.maxZoom * b.zoom, r)
    ), r = r / this.getZoom()) : this.getZoom() * r < this.options.minZoom * b.zoom ? r = this.options.minZoom * b.zoom / this.getZoom() : this.getZoom() * r > this.options.maxZoom * b.zoom && (r = this.options.maxZoom * b.zoom / this.getZoom());
    var x = this.viewport.getCTM(), m = l.matrixTransform(x.inverse()), g = this.svg.createSVGMatrix().translate(m.x, m.y).scale(r).translate(-m.x, -m.y), w = x.multiply(g);
    w.a !== x.a && this.viewport.setCTM(w);
  }, s.prototype.zoom = function(r, l) {
    this.zoomAtPoint(
      r,
      o.getSvgCenterPoint(this.svg, this.width, this.height),
      l
    );
  }, s.prototype.publicZoom = function(r, l) {
    l && (r = this.computeFromRelativeZoom(r)), this.zoom(r, l);
  }, s.prototype.publicZoomAtPoint = function(r, l, d) {
    if (d && (r = this.computeFromRelativeZoom(r)), n.getType(l) !== "SVGPoint")
      if ("x" in l && "y" in l)
        l = o.createSVGPoint(this.svg, l.x, l.y);
      else
        throw new Error("Given point is invalid");
    this.zoomAtPoint(r, l, d);
  }, s.prototype.getZoom = function() {
    return this.viewport.getZoom();
  }, s.prototype.getRelativeZoom = function() {
    return this.viewport.getRelativeZoom();
  }, s.prototype.computeFromRelativeZoom = function(r) {
    return r * this.viewport.getOriginalState().zoom;
  }, s.prototype.resetZoom = function() {
    var r = this.viewport.getOriginalState();
    this.zoom(r.zoom, !0);
  }, s.prototype.resetPan = function() {
    this.pan(this.viewport.getOriginalState());
  }, s.prototype.reset = function() {
    this.resetZoom(), this.resetPan();
  }, s.prototype.handleDblClick = function(r) {
    if (this.options.preventMouseEventsDefault && (r.preventDefault ? r.preventDefault() : r.returnValue = !1), this.options.controlIconsEnabled) {
      var l = r.target.getAttribute("class") || "";
      if (l.indexOf("svg-pan-zoom-control") > -1)
        return !1;
    }
    var d;
    r.shiftKey ? d = 1 / ((1 + this.options.zoomScaleSensitivity) * 2) : d = (1 + this.options.zoomScaleSensitivity) * 2;
    var b = o.getEventPoint(r, this.svg).matrixTransform(
      this.svg.getScreenCTM().inverse()
    );
    this.zoomAtPoint(d, b);
  }, s.prototype.handleMouseDown = function(r, l) {
    this.options.preventMouseEventsDefault && (r.preventDefault ? r.preventDefault() : r.returnValue = !1), n.mouseAndTouchNormalize(r, this.svg), this.options.dblClickZoomEnabled && n.isDblClick(r, l) ? this.handleDblClick(r) : (this.state = "pan", this.firstEventCTM = this.viewport.getCTM(), this.stateOrigin = o.getEventPoint(r, this.svg).matrixTransform(
      this.firstEventCTM.inverse()
    ));
  }, s.prototype.handleMouseMove = function(r) {
    if (this.options.preventMouseEventsDefault && (r.preventDefault ? r.preventDefault() : r.returnValue = !1), this.state === "pan" && this.options.panEnabled) {
      var l = o.getEventPoint(r, this.svg).matrixTransform(
        this.firstEventCTM.inverse()
      ), d = this.firstEventCTM.translate(
        l.x - this.stateOrigin.x,
        l.y - this.stateOrigin.y
      );
      this.viewport.setCTM(d);
    }
  }, s.prototype.handleMouseUp = function(r) {
    this.options.preventMouseEventsDefault && (r.preventDefault ? r.preventDefault() : r.returnValue = !1), this.state === "pan" && (this.state = "none");
  }, s.prototype.handleTouchStart = function(r, l) {
    if (r.touches.length == 1)
      this.handleMouseDown(r, l);
    else {
      this.options.preventMouseEventsDefault && (r.preventDefault ? r.preventDefault() : r.returnValue = !1), this.firstEventCTM = this.viewport.getCTM();
      var d = o.getTouchPoint(r, this.svg, 0), b = o.getTouchPoint(r, this.svg, 1);
      this.firstDistance = n.calculateDistance(d, b), d.x = (d.x + b.x) / 2, d.y = (d.y + b.y) / 2, this.stateOrigin = d.matrixTransform(this.firstEventCTM.inverse()), this.firstZoomLevel = this.getZoom();
    }
  }, s.prototype.handleTouchMove = function(r) {
    if (r.touches.length == 1)
      this.handleMouseMove(r);
    else {
      if (this.options.preventMouseEventsDefault && (r.preventDefault ? r.preventDefault() : r.returnValue = !1), !this.options.panEnabled && !this.options.zoomEnabled)
        return;
      var l = o.getTouchPoint(r, this.svg, 0), d = o.getTouchPoint(r, this.svg, 1), b = this.svg.createSVGPoint();
      if (b.x = (l.x + d.x) / 2, b.y = (l.y + d.y) / 2, this.state === "pan" && this.options.panEnabled) {
        var x = b.matrixTransform(this.firstEventCTM.inverse()), m = this.firstEventCTM.translate(
          x.x - this.stateOrigin.x,
          x.y - this.stateOrigin.y
        );
        this.viewport.setCTM(m);
      }
      if (this.options.zoomEnabled) {
        var g = n.calculateDistance(l, d), w = g / this.firstDistance, E = this.svg.getScreenCTM().inverse(), p = b.matrixTransform(E);
        this.zoomAtPoint(this.firstZoomLevel * w, p, !0);
      }
    }
  }, s.prototype.handleTouchEnd = function(r) {
    if (r.touches.length == 0)
      this.handleMouseUp(r);
    else if (this.options.preventMouseEventsDefault && (r.preventDefault ? r.preventDefault() : r.returnValue = !1), this.firstEventCTM = this.viewport.getCTM(), r.touches.length == 1)
      this.stateOrigin = o.getEventPoint(r, this.svg).matrixTransform(
        this.firstEventCTM.inverse()
      );
    else {
      var l = o.getTouchPoint(r, this.svg, 0), d = o.getTouchPoint(r, this.svg, 1);
      this.firstDistance = n.calculateDistance(l, d), l.x = (l.x + d.x) / 2, l.y = (l.y + d.y) / 2, this.stateOrigin = l.matrixTransform(this.firstEventCTM.inverse());
    }
  }, s.prototype.fit = function() {
    var r = this.viewport.getViewBox(), l = Math.min(
      this.width / r.width,
      this.height / r.height
    );
    this.zoom(l, !0);
  }, s.prototype.contain = function() {
    var r = this.viewport.getViewBox(), l = Math.max(
      this.width / r.width,
      this.height / r.height
    );
    this.zoom(l, !0);
  }, s.prototype.center = function() {
    var r = this.viewport.getViewBox(), l = (this.width - (r.width + r.x * 2) * this.getZoom()) * 0.5, d = (this.height - (r.height + r.y * 2) * this.getZoom()) * 0.5;
    this.getPublicInstance().pan({ x: l, y: d });
  }, s.prototype.updateBBox = function() {
    this.viewport.simpleViewBoxCache();
  }, s.prototype.pan = function(r) {
    var l = this.viewport.getCTM();
    l.e = r.x, l.f = r.y, this.viewport.setCTM(l);
  }, s.prototype.panBy = function(r) {
    var l = this.viewport.getCTM();
    l.e += r.x, l.f += r.y, this.viewport.setCTM(l);
  }, s.prototype.getPan = function() {
    var r = this.viewport.getState();
    return { x: r.x, y: r.y };
  }, s.prototype.resize = function() {
    var r = o.getBoundingClientRectNormalized(
      this.svg
    );
    this.width = r.width, this.height = r.height;
    var l = this.viewport;
    l.options.width = this.width, l.options.height = this.height, l.processCTM(), this.options.controlIconsEnabled && (this.getPublicInstance().disableControlIcons(), this.getPublicInstance().enableControlIcons());
  }, s.prototype.destroy = function() {
    var r = this;
    this.beforeZoom = null, this.onZoom = null, this.beforePan = null, this.onPan = null, this.onUpdatedCTM = null, this.options.customEventsHandler != null && this.options.customEventsHandler.destroy({
      svgElement: this.svg,
      eventsListenerElement: this.options.eventsListenerElement,
      instance: this.getPublicInstance()
    });
    for (var l in this.eventListeners)
      (this.options.eventsListenerElement || this.svg).removeEventListener(
        l,
        this.eventListeners[l],
        this.options.preventMouseEventsDefault ? u : c
      );
    this.disableMouseWheelZoom(), this.getPublicInstance().disableControlIcons(), h = h.filter(function(d) {
      return d.svg !== r.svg;
    }), delete this.options, delete this.viewport, delete this.publicInstance, delete this.pi, this.getPublicInstance = function() {
      return null;
    };
  }, s.prototype.getPublicInstance = function() {
    var r = this;
    return this.publicInstance || (this.publicInstance = this.pi = {
      // Pan
      enablePan: function() {
        return r.options.panEnabled = !0, r.pi;
      },
      disablePan: function() {
        return r.options.panEnabled = !1, r.pi;
      },
      isPanEnabled: function() {
        return !!r.options.panEnabled;
      },
      pan: function(l) {
        return r.pan(l), r.pi;
      },
      panBy: function(l) {
        return r.panBy(l), r.pi;
      },
      getPan: function() {
        return r.getPan();
      },
      // Pan event
      setBeforePan: function(l) {
        return r.options.beforePan = l === null ? null : n.proxy(l, r.publicInstance), r.pi;
      },
      setOnPan: function(l) {
        return r.options.onPan = l === null ? null : n.proxy(l, r.publicInstance), r.pi;
      },
      // Zoom and Control Icons
      enableZoom: function() {
        return r.options.zoomEnabled = !0, r.pi;
      },
      disableZoom: function() {
        return r.options.zoomEnabled = !1, r.pi;
      },
      isZoomEnabled: function() {
        return !!r.options.zoomEnabled;
      },
      enableControlIcons: function() {
        return r.options.controlIconsEnabled || (r.options.controlIconsEnabled = !0, e.enable(r)), r.pi;
      },
      disableControlIcons: function() {
        return r.options.controlIconsEnabled && (r.options.controlIconsEnabled = !1, e.disable(r)), r.pi;
      },
      isControlIconsEnabled: function() {
        return !!r.options.controlIconsEnabled;
      },
      // Double click zoom
      enableDblClickZoom: function() {
        return r.options.dblClickZoomEnabled = !0, r.pi;
      },
      disableDblClickZoom: function() {
        return r.options.dblClickZoomEnabled = !1, r.pi;
      },
      isDblClickZoomEnabled: function() {
        return !!r.options.dblClickZoomEnabled;
      },
      // Mouse wheel zoom
      enableMouseWheelZoom: function() {
        return r.enableMouseWheelZoom(), r.pi;
      },
      disableMouseWheelZoom: function() {
        return r.disableMouseWheelZoom(), r.pi;
      },
      isMouseWheelZoomEnabled: function() {
        return !!r.options.mouseWheelZoomEnabled;
      },
      // Zoom scale and bounds
      setZoomScaleSensitivity: function(l) {
        return r.options.zoomScaleSensitivity = l, r.pi;
      },
      setMinZoom: function(l) {
        return r.options.minZoom = l, r.pi;
      },
      setMaxZoom: function(l) {
        return r.options.maxZoom = l, r.pi;
      },
      // Zoom event
      setBeforeZoom: function(l) {
        return r.options.beforeZoom = l === null ? null : n.proxy(l, r.publicInstance), r.pi;
      },
      setOnZoom: function(l) {
        return r.options.onZoom = l === null ? null : n.proxy(l, r.publicInstance), r.pi;
      },
      // Zooming
      zoom: function(l) {
        return r.publicZoom(l, !0), r.pi;
      },
      zoomBy: function(l) {
        return r.publicZoom(l, !1), r.pi;
      },
      zoomAtPoint: function(l, d) {
        return r.publicZoomAtPoint(l, d, !0), r.pi;
      },
      zoomAtPointBy: function(l, d) {
        return r.publicZoomAtPoint(l, d, !1), r.pi;
      },
      zoomIn: function() {
        return this.zoomBy(1 + r.options.zoomScaleSensitivity), r.pi;
      },
      zoomOut: function() {
        return this.zoomBy(1 / (1 + r.options.zoomScaleSensitivity)), r.pi;
      },
      getZoom: function() {
        return r.getRelativeZoom();
      },
      // CTM update
      setOnUpdatedCTM: function(l) {
        return r.options.onUpdatedCTM = l === null ? null : n.proxy(l, r.publicInstance), r.pi;
      },
      // Reset
      resetZoom: function() {
        return r.resetZoom(), r.pi;
      },
      resetPan: function() {
        return r.resetPan(), r.pi;
      },
      reset: function() {
        return r.reset(), r.pi;
      },
      // Fit, Contain and Center
      fit: function() {
        return r.fit(), r.pi;
      },
      contain: function() {
        return r.contain(), r.pi;
      },
      center: function() {
        return r.center(), r.pi;
      },
      // Size and Resize
      updateBBox: function() {
        return r.updateBBox(), r.pi;
      },
      resize: function() {
        return r.resize(), r.pi;
      },
      getSizes: function() {
        return {
          width: r.width,
          height: r.height,
          realZoom: r.getZoom(),
          viewBox: r.viewport.getViewBox()
        };
      },
      // Destroy
      destroy: function() {
        return r.destroy(), r.pi;
      }
    }), this.publicInstance;
  };
  var h = [], f = function(r, l) {
    var d = n.getSvg(r);
    if (d === null)
      return null;
    for (var b = h.length - 1; b >= 0; b--)
      if (h[b].svg === d)
        return h[b].instance.getPublicInstance();
    return h.push({
      svg: d,
      instance: new s(d, l)
    }), h[h.length - 1].instance.getPublicInstance();
  };
  return Mt = f, Mt;
}
var ss = is();
const rs = /* @__PURE__ */ es(ss), as = {
  getViewArea() {
    const t = this.getSizes(), e = this.getPan(), n = t.realZoom;
    e.x /= n, e.y /= n;
    const o = {
      width: t.width / n,
      height: t.height / n
    };
    return {
      box: {
        top: -e.y,
        bottom: o.height - e.y,
        left: -e.x,
        right: o.width - e.x
      },
      center: {
        x: o.width / 2 - e.x,
        y: o.height / 2 - e.y
      }
    };
  },
  getViewBox() {
    return this.getViewArea().box;
  },
  setViewBox(t) {
    const e = t.right - t.left, n = t.bottom - t.top, { width: o, height: i } = this.getSizes(), s = e / n, a = o / i, c = s < a ? n * a : e, u = s > a ? e / a : n, h = Math.min(
      o / c,
      i / u
    ), f = this.getRealZoom(), r = this.getZoom(), l = f / r;
    this.zoom(h / l);
    const d = {
      x: (t.left + e / 2) * h,
      y: (t.top + n / 2) * h
    };
    this.pan({
      x: -d.x + c / 2 * h,
      y: -d.y + u / 2 * h
    });
  },
  getRealZoom() {
    return this.getSizes().realZoom;
  },
  applyAbsoluteZoomLevel(t, e, n) {
    const o = Math.max(1e-4, e), i = Math.max(o, n), s = Math.max(Math.min(i, t), o), a = this.getRealZoom(), c = this.getZoom(), u = a / c;
    this.setMinZoom(o / u).setMaxZoom(i / u).zoom(s / u);
  },
  isPanEnabled() {
    return this._isPanEnabled;
  },
  enablePan() {
    return this._isPanEnabled = !0, this._internalEnablePan(), this;
  },
  disablePan() {
    return this._isPanEnabled = !1, this._internalDisablePan(), this;
  },
  isZoomEnabled() {
    return this._isZoomEnabled;
  },
  enableZoom() {
    return this._isZoomEnabled = !0, this._internalEnableZoom(), this;
  },
  disableZoom() {
    return this._isZoomEnabled = !1, this._internalDisableZoom(), this;
  },
  setPanEnabled(t) {
    return t ? this.enablePan() : this.disablePan(), this;
  },
  setZoomEnabled(t) {
    return t ? (this.enableZoom(), this.enableDblClickZoom()) : (this.disableZoom(), this.disableDblClickZoom()), this;
  }
};
function ls(t, e) {
  var o, i;
  const n = t;
  return n._isPanEnabled = (o = e.panEnabled) != null ? o : !0, n._isZoomEnabled = (i = e == null ? void 0 : e.zoomEnabled) != null ? i : !0, n._internalIsPanEnabled = n.isPanEnabled, n._internalEnablePan = n.enablePan, n._internalDisablePan = n.disablePan, n._internalIsZoomEnabled = n.isZoomEnabled, n._internalEnableZoom = n.enableZoom, n._internalDisableZoom = n.disableZoom, Object.assign(t, as), n;
}
function cs(t, e) {
  var s, a, c, u, h, f;
  const n = (a = (s = e.customEventsHandler) == null ? void 0 : s.init) != null ? a : ((r) => {
  }), o = (u = (c = e.customEventsHandler) == null ? void 0 : c.destroy) != null ? u : ((r) => {
  }), i = (f = (h = e.customEventsHandler) == null ? void 0 : h.haltEventListeners) != null ? f : [];
  return e.mouseWheelZoomEnabled === void 0 && (e.mouseWheelZoomEnabled = e.zoomEnabled), e.customEventsHandler = {
    init: (r) => {
      ls(r.instance, e), n(r);
    },
    destroy: (r) => o(r),
    haltEventListeners: i
  }, rs(t, e);
}
function us(t, e) {
  const n = B();
  let o = 0;
  const i = [], s = [], a = () => {
    o = 1, i.forEach((f) => f()), i.length = 0;
  }, c = () => {
    o = 2, s.forEach((f) => f()), s.length = 0;
  };
  return Be(() => {
    var x, m, g, w, E, p;
    const f = q(t.value, "<svg>"), r = (m = (x = e.customEventsHandler) == null ? void 0 : x.init) != null ? m : ((v) => {
    }), l = (w = (g = e.customEventsHandler) == null ? void 0 : g.destroy) != null ? w : ((v) => {
    }), d = (p = (E = e.customEventsHandler) == null ? void 0 : E.haltEventListeners) != null ? p : [];
    e.customEventsHandler = {
      init: (v) => {
        n.value = v.instance, r(v), a();
      },
      destroy: (v) => {
        c(), l(v);
      },
      haltEventListeners: d
    };
    const b = () => {
      const v = f.getBoundingClientRect();
      v.width !== 0 && v.height !== 0 ? cs(f, e) : setTimeout(b, 200);
    };
    b();
  }), Xe(() => {
    var f;
    (f = n.value) == null || f.destroy(), n.value = void 0;
  }), { svgPanZoom: n, onSvgPanZoomMounted: (f) => {
    o === 0 ? i.push(f) : o === 1 && f();
  }, onSvgPanZoomUnmounted: (f) => {
    o === 0 || o === 1 ? s.push(f) : f();
  } };
}
const to = /* @__PURE__ */ Symbol("zoomLevel");
function ds(t, e) {
  const n = C(() => e.scalingObjects ? 1 : 1 / t.value);
  return Me(to, {
    zoomLevel: t,
    scale: n
  }), { scale: n };
}
function de() {
  return q(Ce(to), "zoomLevel");
}
function fs(t) {
  return t instanceof Promise || t && typeof t.then == "function";
}
function hs() {
  let t = null;
  const e = B({
    enabled: !1,
    duration: 300,
    timingFunction: "linear"
  });
  function n(o, i = 300, s = "linear") {
    t && (clearTimeout(t), t = null), e.value = {
      enabled: !0,
      duration: i,
      timingFunction: s
    }, Zt(() => ve(null, null, function* () {
      const a = o();
      fs(a) && (yield a), t && clearTimeout(t), t = window == null ? void 0 : window.setTimeout(() => {
        e.value.enabled = !1, t = null;
      }, i);
    }));
  }
  return { transitionWhile: n, transitionOption: e };
}
function vs(t) {
  const e = B({}), n = B(!1);
  let o = 1;
  const i = /* @__PURE__ */ new Map();
  return ue(() => {
    if (t.value instanceof Array) {
      const s = /* @__PURE__ */ new Set([]);
      if (e.value = Object.fromEntries(
        t.value.map((a) => {
          let c = a.id;
          return c || (n.value || (n.value = !0, console.warn(
            "[v-network-graph] Please specify the `id` field for the `Path` object. Currently, this works for compatibility, but in the future, the id field will be required."
          )), c = i.get(a), c || (c = "path-" + o++, i.set(a, c))), s.add(c), [c, a];
        })
      ), n.value)
        for (const [a, c] of Array.from(i.entries()))
          s.has(c) || i.delete(a);
    } else
      e.value = t.value;
  }), { objects: e, isInCompatibilityModeForPath: n };
}
function gs(t, e, n, o) {
  if (o) {
    const s = B(o(t[e])), a = (c) => {
      Pe(c, s.value) || (s.value = c), Pe(c, t[e]) || n(`update:${e}`, c);
    };
    return F(() => o(s.value), a), F(() => t[e], (c) => a(o(c))), s.value !== t[e] && n(`update:${e}`, s.value), s;
  }
  const i = B(t[e]);
  return F(
    () => t[e],
    (s) => {
      Pe(s, i.value) || (i.value = s);
    }
  ), F(i, (s) => {
    Pe(s, t[e]) || n(`update:${e}`, s);
  }), i;
}
function Ct(t, e, n, o) {
  const i = Ee(/* @__PURE__ */ new Set());
  return F(
    () => t[e],
    () => {
      const a = t[e].filter((c) => c in n.value);
      Pe(a, Array.from(i)) || (i.clear(), a.forEach(i.add, i));
    },
    { deep: !0, immediate: !0 }
  ), F(i, () => {
    const s = Array.from(i);
    Pe(t[e], s) || o(`update:${e}`, s);
  }), ke(i);
}
const no = /* @__PURE__ */ Symbol("selection");
function ps(t, e, n) {
  Me(no, {
    selectedNodes: t,
    selectedEdges: e,
    selectedPaths: n
  });
}
function ms() {
  return q(Ce(no), "Selections");
}
const oo = /* @__PURE__ */ Symbol("layouts");
function bs(t) {
  Me(oo, t);
}
function Kt() {
  return q(Ce(oo), "Layouts");
}
function ys(t, e) {
  for (let n = 0; n < t.length - 1; n++)
    e(t[n], t[n + 1]);
}
function _e(t, e) {
  const n = t.indexOf(e);
  n >= 0 && t.splice(n, 1);
}
function ws(t, e, n) {
  const o = t.indexOf(e);
  o < 0 || t.splice(o + 1, 0, n);
}
function Es(t, e) {
  const n = [
    "edges",
    "edge-labels",
    "focusring",
    "nodes",
    "node-labels",
    "paths"
  ];
  return C(() => {
    const o = Uo(t.view.builtInLayerOrder).filter((s) => {
      const a = n.includes(s);
      return a || console.warn(`Layer ${s} is not a built-in layer.`), a;
    }).reverse(), i = [...n];
    return ys(o, (s, a) => {
      _e(i, a), ws(i, s, a);
    }), "edge-label" in e || "edges-label" in e || _e(i, "edge-labels"), t.node.focusring.visible || _e(i, "focusring"), t.node.label.visible === !1 && _e(i, "node-labels"), t.path.visible || _e(i, "paths"), i;
  });
}
const Cn = () => new Promise((t) => Zt(t));
function xs(t, e) {
  const n = Math.max(t.width, t.height, e.width, e.height) / 1e4;
  return Math.abs(t.x - e.x) < n && Math.abs(t.y - e.y) < n && Math.abs(t.width - e.width) < n && Math.abs(t.height - e.height) < n;
}
function ks(t, e) {
  return {
    top: t.top + e.top,
    left: t.left + e.left,
    right: t.right + e.right,
    bottom: t.bottom + e.bottom
  };
}
function Ps(t, e) {
  return {
    top: t.top * e,
    left: t.left * e,
    right: t.right * e,
    bottom: t.bottom * e
  };
}
function Ss(t, e) {
  return {
    top: t.top / e,
    left: t.left / e,
    right: t.right / e,
    bottom: t.bottom / e
  };
}
function io(t) {
  return {
    top: t.y,
    left: t.x,
    right: t.x + t.width,
    bottom: t.y + t.height
  };
}
function so(t) {
  return {
    x: t.left,
    y: t.top,
    width: t.right - t.left,
    height: t.bottom - t.top
  };
}
function Ms(t, e) {
  return {
    top: Math.min(t.top, e.top),
    left: Math.min(t.left, e.left),
    right: Math.max(t.right, e.right),
    bottom: Math.max(t.bottom, e.bottom)
  };
}
const Cs = new RegExp("^\\d+$");
function Os(t, e) {
  let n = { top: 0, left: 0, right: 0, bottom: 0 };
  if (typeof t == "string") {
    const o = Ze(t, e.width), i = Ze(t, e.height);
    o === void 0 || i === void 0 ? console.warn("Invalid `fitContentMargin` value.", t) : n = { top: i, left: o, right: o, bottom: i };
  } else if (typeof t == "number") {
    const o = t;
    n = { top: o, left: o, right: o, bottom: o };
  } else {
    if (t.top) {
      const o = Ze(t.top, e.height);
      o === void 0 ? console.warn("Invalid `fitContentMargin` value.", t.top) : n.top = o;
    }
    if (t.left) {
      const o = Ze(t.left, e.width);
      o === void 0 ? console.warn("Invalid `fitContentMargin` value.", t.left) : n.left = o;
    }
    if (t.right) {
      const o = Ze(t.right, e.width);
      o === void 0 ? console.warn("Invalid `fitContentMargin` value.", t.right) : n.right = o;
    }
    if (t.bottom) {
      const o = Ze(t.bottom, e.height);
      o === void 0 ? console.warn("Invalid `fitContentMargin` value.", t.bottom) : n.bottom = o;
    }
  }
  return n;
}
function zs(t, e, n, o, i, s) {
  const a = t.getBBox();
  if (s)
    return Is(a, e, n, i);
  {
    const c = Ts(t);
    return $s(
      a,
      c,
      e,
      n,
      o,
      i
    );
  }
}
function Ze(t, e) {
  if (typeof t == "string") {
    if (t.endsWith("%")) {
      const n = parseInt(t.toString());
      if (Number.isFinite(n))
        return e * (n / 100);
    } else if (t.endsWith("px") || Cs.test(t)) {
      const n = parseInt(t.toString());
      if (Number.isFinite(n))
        return n;
    }
  } else if (typeof t == "number")
    return t;
}
function Ts(t) {
  return Array.from(t.querySelectorAll(".v-ng-graph-objects")).map(
    (n) => n.getBBox()
  ).reduce(
    (n, o, i) => {
      if (i === 0) return o;
      const s = Math.min(n.x, o.x), a = Math.min(n.y, o.y);
      return {
        x: s,
        y: a,
        width: Math.max(n.x + n.width - s, o.x + o.width - s),
        height: Math.max(n.y + n.height - a, o.y + o.height - a)
      };
    },
    { x: 0, y: 0, width: 0, height: 0 }
  );
}
function Is(t, e, n, o) {
  if (Object.keys(n).length <= 1)
    return;
  const i = ro(t, e, o);
  if (i > 0) {
    const s = io(t);
    return {
      zoom: i,
      pos: ao(s, i, e, o)
    };
  } else
    return;
}
function $s(t, e, n, o, i, s) {
  if (Object.keys(o).length <= 1)
    return;
  const a = hi(o), c = {
    top: (a.top - e.y) * i,
    left: (a.left - e.x) * i,
    right: (e.x + e.width - a.right) * i,
    bottom: (e.y + e.height - a.bottom) * i
  };
  let u = ro(t, n, s);
  if (u === 0)
    return;
  const h = it(n, ks(s, c));
  if (h.width <= 0 || h.height <= 0)
    return;
  const f = io(t), r = it(n, s), l = xs(t, e);
  let d = 0, b = 0, x = { top: 0, left: 0, right: 0, bottom: 0 };
  do {
    b = u;
    const m = Ss(c, u), g = {
      top: a.top - m.top,
      left: a.left - m.left,
      right: a.right + m.right,
      bottom: a.bottom + m.bottom
    };
    x = l ? g : Ms(f, g);
    const w = so(x), p = [r.width / w.width, r.height / w.height].filter((v) => v > 0);
    if (p.length === 0)
      return;
    u = Math.min(...p), d++;
  } while (Math.abs(b - u) > 1e-6 && d < 10);
  return {
    zoom: u,
    pos: ao(x, u, n, s)
  };
}
function ro(t, e, n) {
  if (t.width === 0 || t.height === 0) return 0;
  const o = it(e, n);
  if (o.width <= 0 || o.height <= 0)
    return 0;
  const i = [o.width / t.width, o.height / t.height];
  return Math.min(...i);
}
function ao(t, e, n, o) {
  const i = it(n, o), s = so(Ps(t, e)), a = (i.width - s.width) / 2, c = (i.height - s.height) / 2, u = s.x - o.left, h = s.y - o.top;
  return {
    x: -u + a,
    y: -h + c
  };
}
function it(t, e) {
  const n = e.left + e.right, o = e.top + e.bottom;
  return {
    width: t.width - n,
    height: t.height - o
  };
}
const Ls = ["x", "y", "width", "height", "fill", "stroke", "stroke-width", "stroke-dasharray"], As = /* @__PURE__ */ U({
  __name: "VSelectionBox",
  props: {
    box: {},
    config: {}
  },
  setup(t) {
    return (e, n) => (S(), $("rect", {
      class: "v-ng-selection-box",
      x: Math.round(t.box.pos.x),
      y: Math.round(t.box.pos.y),
      width: Math.ceil(t.box.size.width),
      height: Math.ceil(t.box.size.height),
      fill: t.config.color,
      stroke: t.config.strokeColor,
      "stroke-width": t.config.strokeWidth,
      "stroke-dasharray": t.config.strokeDasharray
    }, null, 8, Ls));
  }
}), Zs = ["points", "fill"], Ds = /* @__PURE__ */ U({
  __name: "VMarkerHeadArrow",
  props: {
    width: {},
    height: {},
    refX: {},
    color: {},
    isSource: { type: Boolean },
    units: {}
  },
  setup(t) {
    const e = t, n = C(() => {
      const o = e.width, i = e.height;
      return e.isSource ? `${o} ${i}, 0 ${i / 2}, ${o} 0` : `0 0, ${o} ${i / 2}, 0 ${i}`;
    });
    return (o, i) => (S(), $("polygon", {
      points: n.value,
      fill: t.color
    }, null, 8, Zs));
  }
}), Ns = ["points", "stroke-width", "stroke"], js = /* @__PURE__ */ U({
  __name: "VMarkerHeadAngle",
  props: {
    width: {},
    height: {},
    refX: {},
    color: {},
    isSource: { type: Boolean },
    units: {}
  },
  setup(t) {
    const e = t, n = C(() => e.units === "strokeWidth" ? 1 : Math.min(e.width, e.height) / 5), o = C(() => {
      const i = n.value / 2, s = e.width, a = e.height;
      return e.isSource ? `${s - i} ${a - i}, ${i} ${a / 2}, ${s - i} ${i}` : `${i} ${i}, ${s - i} ${a / 2}, ${i} ${a - i}`;
    });
    return (i, s) => (S(), $("polyline", {
      points: o.value,
      fill: "none",
      "stroke-width": n.value,
      stroke: t.color,
      "stroke-linecap": "round",
      "stroke-linejoin": "round"
    }, null, 8, Ns));
  }
}), Bs = ["fill", "cx", "cy", "rx", "ry"], Rs = /* @__PURE__ */ U({
  __name: "VMarkerHeadCircle",
  props: {
    width: {},
    height: {},
    refX: {},
    color: {},
    isSource: { type: Boolean },
    units: {}
  },
  setup(t) {
    return (e, n) => (S(), $("ellipse", {
      fill: t.color,
      cx: t.width / 2,
      cy: t.height / 2,
      rx: t.width / 2,
      ry: t.height / 2
    }, null, 8, Bs));
  }
}), Vs = ["id", "markerWidth", "markerHeight", "refX", "refY", "markerUnits"], Ws = /* @__PURE__ */ U({
  __name: "VMarkerHead",
  props: {
    id: {},
    marker: {},
    scale: {}
  },
  setup(t) {
    const e = {
      arrow: Ds,
      angle: js,
      circle: Rs
    }, n = t, o = C(
      () => n.marker.width * (n.marker.units === "strokeWidth" ? 1 : n.scale)
    ), i = C(
      () => n.marker.height * (n.marker.units === "strokeWidth" ? 1 : n.scale)
    ), s = C(() => {
      const c = n.marker.margin * (n.marker.units === "strokeWidth" ? 1 : n.scale);
      return n.marker.isSource ? o.value + c : -c;
    }), a = C(() => {
      const c = n.marker.offset * (n.marker.units === "strokeWidth" ? 1 : n.scale);
      return n.marker.isSource ? c : -c;
    });
    return (c, u) => t.marker.type !== "none" && t.marker.type !== "custom" ? (S(), $("marker", {
      key: 0,
      id: t.id,
      markerWidth: o.value,
      markerHeight: i.value,
      refX: s.value,
      refY: i.value / 2 + a.value,
      orient: "auto",
      markerUnits: t.marker.units,
      class: "v-ng-marker"
    }, [
      (S(), H(In(e[t.marker.type]), {
        width: o.value,
        height: i.value,
        refX: s.value,
        color: t.marker.color,
        "is-source": t.marker.isSource,
        units: t.marker.units
      }, null, 8, ["width", "height", "refX", "color", "is-source", "units"]))
    ], 8, Vs)) : ae("", !0);
  }
}), Hs = {
  class: "v-ng-background-grid",
  "shape-rendering": "crispEdges"
}, _s = ["d"], Us = ["d"], Fs = ["d"], qs = ["d"], Xs = /* @__PURE__ */ U({
  __name: "VBackgroundGrid",
  setup(t) {
    const { container: e, svgPanZoom: n } = jt(), { zoomLevel: o } = de(), i = Ji(), s = Yo(), a = B({ x: 0, y: 0 }), c = B({ width: 500, height: 500 });
    Be(() => {
      var b;
      const l = (b = n.value) == null ? void 0 : b.getPan();
      l && (a.value = {
        x: -l.x,
        y: -l.y
      });
      const d = e.value.getBoundingClientRect();
      c.value = {
        width: d.width,
        height: d.height
      };
    }), i.on("view:resize", (l) => {
      c.value = { width: l.width, height: l.height };
    }), i.on("view:pan", (l) => {
      a.value = { x: -l.x, y: -l.y };
    }), i.on("view:zoom", () => {
      var d;
      const l = (d = n.value) == null ? void 0 : d.getPan();
      l && (a.value = { x: -l.x, y: -l.y });
    });
    const u = B([]), h = B([]), f = B([]), r = B([]);
    return ue(() => {
      const l = [], d = [], b = [], x = [], m = 1 / o.value, g = s.grid.interval, w = a.value.x * m, E = a.value.y * m, p = Math.floor(c.value.width / g + 1) * g, v = Math.floor(c.value.height / g + 1) * g, y = (a.value.x + p) * m, k = (a.value.y + v) * m, O = s.grid.thickIncrements, L = s.grid.line.dasharray, T = s.grid.thick.dasharray;
      let z = {
        stroke: s.grid.thick.color,
        "stroke-width": s.grid.thick.width,
        "stroke-dasharray": T,
        "stroke-dashoffset": T ? w / m : void 0
      }, A = {
        stroke: s.grid.line.color,
        "stroke-width": s.grid.line.width,
        "stroke-dasharray": L,
        "stroke-dashoffset": L ? w / m : void 0
      };
      const j = (a.value.x + p) * m;
      for (let R = E; R <= k; R += g) {
        const _ = Math.floor(R / g);
        O && _ % O === 0 ? l.push([_, _ * g, w, j, z]) : b.push([_, _ * g, w, j, A]);
      }
      z = oe({}, z), z["stroke-dashoffset"] = T ? E / m : void 0, A = oe({}, A), A["stroke-dashoffset"] = L ? E / m : void 0;
      const G = (a.value.y + v) * m;
      for (let R = w; R <= y; R += g) {
        const _ = Math.floor(R / g);
        O && _ % O === 0 ? d.push([_, _ * g, E, G, z]) : x.push([_, _ * g, E, G, A]);
      }
      h.value = l, u.value = d, r.value = b, f.value = x;
    }), (l, d) => (S(), $("g", Hs, [
      (S(!0), $(X, null, Q(r.value, ([b, x, m, g, w]) => (S(), $("path", ne({
        key: `nv${b}`,
        d: `M ${m} ${x} L ${g} ${x}`
      }, { ref_for: !0 }, w, { style: { "vector-effect": "non-scaling-stroke" } }), null, 16, _s))), 128)),
      (S(!0), $(X, null, Q(f.value, ([b, x, m, g, w]) => (S(), $("path", ne({
        key: `nh${b}`,
        d: `M ${x} ${m} L ${x} ${g}`
      }, { ref_for: !0 }, w, { style: { "vector-effect": "non-scaling-stroke" } }), null, 16, Us))), 128)),
      (S(!0), $(X, null, Q(h.value, ([b, x, m, g, w]) => (S(), $("path", ne({
        key: `tv${b}`,
        d: `M ${m} ${x} L ${g} ${x}`
      }, { ref_for: !0 }, w, { style: { "vector-effect": "non-scaling-stroke" } }), null, 16, Fs))), 128)),
      (S(!0), $(X, null, Q(u.value, ([b, x, m, g, w]) => (S(), $("path", ne({
        key: `th${b}`,
        d: `M ${x} ${m} L ${x} ${g}`
      }, { ref_for: !0 }, w, { style: { "vector-effect": "non-scaling-stroke" } }), null, 16, qs))), 128))
    ]));
  }
}), Gs = /* @__PURE__ */ U({
  __name: "VBackgroundViewport",
  setup(t) {
    const { viewport: e } = jt(), n = B(), o = (s, a, c) => {
      s.forEach((u) => {
        var h;
        return a.setAttribute(u, (h = c.getAttribute(u)) != null ? h : "");
      });
    }, i = new MutationObserver((s) => {
      if (!n.value) return;
      const a = s.map((c) => {
        var u;
        return (u = c.attributeName) != null ? u : "";
      }).filter(Boolean);
      o(a, n.value, e.value);
    });
    return Be(() => {
      const s = ["transform", "style"];
      i.observe(e.value, {
        attributes: !0,
        attributeFilter: s
      }), n.value && o(s, n.value, e.value);
    }), Xe(() => {
      i.disconnect();
    }), (s, a) => (S(), $("g", {
      ref_key: "background",
      ref: n,
      class: "v-ng-background-viewport"
    }, [
      K(s.$slots, "default")
    ], 512));
  }
}), Ks = ["d", "stroke-width"], Ys = /* @__PURE__ */ U({
  __name: "VEdgeBackground",
  props: {
    id: {},
    state: {},
    sourcePos: { default: void 0 },
    targetPos: { default: void 0 }
  },
  setup(t) {
    const e = t, { scale: n } = de(), o = ct(), {
      handleEdgePointerDownEvent: i,
      handleEdgePointerOverEvent: s,
      handleEdgePointerOutEvent: a,
      handleEdgeClickEvent: c,
      handleEdgeDoubleClickEvent: u,
      handleEdgeContextMenu: h
    } = Ye(), f = C(() => {
      const l = e.state.position;
      if (e.state.loop) {
        const { radius: d, isLargeArc: b, isClockwise: x } = e.state.loop, [m, g] = d, w = b ? 1 : 0, E = x ? 1 : 0;
        return `M ${l.p1.x} ${l.p1.y} A ${m} ${g} 0 ${w} ${E} ${l.p2.x} ${l.p2.y}`;
      } else {
        if (o.type === "straight" || !e.state.curve)
          return `M ${l.p1.x} ${l.p1.y} L ${l.p2.x} ${l.p2.y}`;
        {
          const d = [...e.state.curve.control, { x: l.p2.x, y: l.p2.y }], b = [];
          return b.push(`M ${l.p1.x} ${l.p1.y}`), Nt(d, 2).forEach(([x, m]) => b.push(`Q ${x.x} ${x.y} ${m.x} ${m.y}`)), b.join(" ");
        }
      }
    }), r = C(() => (e.state.line.stroke.width + 10) * n.value);
    return (l, d) => (S(), $("path", {
      class: ce({ "v-ng-line-background": !0, selectable: t.state.selectable }),
      d: f.value,
      stroke: "transparent",
      "stroke-width": r.value,
      fill: "none",
      onPointerdown: d[0] || (d[0] = ge((b) => M(i)(t.id, b), ["stop"])),
      onPointerenterPassive: d[1] || (d[1] = (b) => M(s)(t.id, b)),
      onPointerleavePassive: d[2] || (d[2] = (b) => M(a)(t.id, b)),
      onClick: d[3] || (d[3] = ge((b) => M(c)(t.id, b), ["stop"])),
      onDblclick: d[4] || (d[4] = ge((b) => M(u)(t.id, b), ["stop"])),
      onContextmenu: d[5] || (d[5] = (b) => M(h)(t.id, b))
    }, null, 42, Ks));
  }
}), Qs = /* @__PURE__ */ U({
  __name: "VEdgeBackgrounds",
  setup(t) {
    const { edgeStates: e, edgeGroupStates: n, layouts: o } = me();
    return (i, s) => (S(!0), $(X, null, Q(M(n).edgeGroups, ({ summarize: a, edges: c }) => (S(), $(X, null, [
      a ? ae("", !0) : (S(!0), $(X, { key: 0 }, Q(c, (u, h) => (S(), H(Ys, {
        key: h,
        id: h,
        state: M(e)[h],
        "source-pos": M(o).nodes[u.source],
        "target-pos": M(o).nodes[u.target]
      }, null, 8, ["id", "state", "source-pos", "target-pos"]))), 128))
    ], 64))), 256));
  }
}), Js = ["d", "stroke", "stroke-width", "stroke-dasharray", "stroke-linecap"], lo = /* @__PURE__ */ U({
  __name: "VLine",
  props: {
    p1: {},
    p2: {},
    config: {}
  },
  setup(t) {
    const e = t, { scale: n } = de(), o = C(() => e.config.width * n.value), i = C(() => Ke(e.config.dasharray, n.value)), s = C(() => {
      const a = e.config.animate ? rt(e.config.dasharray) * e.config.animationSpeed * n.value : !1;
      return a ? { "--animation-speed": a } : void 0;
    });
    return (a, c) => (S(), $("path", {
      class: ce({ "v-ng-line": !0, animate: t.config.animate }),
      d: `M ${t.p1.x} ${t.p1.y} L ${t.p2.x} ${t.p2.y}`,
      stroke: t.config.color,
      "stroke-width": o.value,
      "stroke-dasharray": i.value,
      "stroke-linecap": t.config.linecap,
      style: Ge(s.value)
    }, null, 14, Js));
  }
}), er = ["d", "stroke", "stroke-width", "stroke-dasharray", "stroke-linecap"], tr = /* @__PURE__ */ U({
  __name: "VArc",
  props: {
    p1: {},
    p2: {},
    radius: {},
    isLargeArc: { type: Boolean },
    isClockwise: { type: Boolean },
    config: {}
  },
  setup(t) {
    const e = t, { scale: n } = de(), o = C(() => e.config.width * n.value), i = C(() => Ke(e.config.dasharray, n.value)), s = C(() => {
      const c = e.config.animate ? rt(e.config.dasharray) * e.config.animationSpeed * n.value : !1;
      return c ? { "--animation-speed": c } : void 0;
    }), a = C(() => {
      const { p1: c, p2: u, radius: h, isLargeArc: f, isClockwise: r } = e, [l, d] = h, b = f ? 1 : 0, x = r ? 1 : 0;
      return `M ${c.x} ${c.y} A ${l} ${d} 0 ${b} ${x} ${u.x} ${u.y}`;
    });
    return (c, u) => (S(), $("path", {
      class: ce({ "v-ng-line": !0, animate: t.config.animate }),
      d: a.value,
      stroke: t.config.color,
      "stroke-width": o.value,
      "stroke-dasharray": i.value,
      "stroke-linecap": t.config.linecap,
      style: Ge(s.value),
      fill: "none"
    }, null, 14, er));
  }
}), nr = ["d", "stroke", "stroke-width", "stroke-dasharray", "stroke-linecap", "marker-start", "marker-end"], or = /* @__PURE__ */ U({
  __name: "VEdgeCurved",
  props: {
    state: {},
    config: {},
    markerStart: { default: void 0 },
    markerEnd: { default: void 0 }
  },
  setup(t) {
    const e = t, { scale: n } = de(), o = C(() => {
      var f, r;
      const c = e.state.position, u = [...(r = (f = e.state.curve) == null ? void 0 : f.control) != null ? r : [], { x: c.p2.x, y: c.p2.y }], h = [];
      return h.push(`M ${c.p1.x} ${c.p1.y}`), Nt(u, 2).forEach(([l, d]) => h.push(`Q ${l.x} ${l.y} ${d.x} ${d.y}`)), h.join(" ");
    }), i = C(() => e.config.width * n.value), s = C(() => Ke(e.config.dasharray, n.value)), a = C(() => {
      const c = e.config.animate ? rt(e.config.dasharray) * e.config.animationSpeed * n.value : !1;
      return c ? { "--animation-speed": c } : void 0;
    });
    return (c, u) => (S(), $("path", {
      class: ce({ "v-ng-line": !0, animate: t.config.animate }),
      d: o.value,
      fill: "none",
      stroke: t.config.color,
      "stroke-width": i.value,
      "stroke-dasharray": s.value,
      "stroke-linecap": t.config.linecap,
      style: Ge(a.value),
      "marker-start": t.markerStart,
      "marker-end": t.markerEnd
    }, null, 14, nr));
  }
}), ir = /* @__PURE__ */ U({
  __name: "VEdge",
  props: {
    id: {},
    state: {},
    sourcePos: { default: void 0 },
    targetPos: { default: void 0 }
  },
  setup(t) {
    const e = ct();
    return (n, o) => t.state.loop ? (S(), H(tr, ne({ key: 0 }, t.state.position, {
      radius: t.state.loop.radius,
      "is-large-arc": t.state.loop.isLargeArc,
      "is-clockwise": t.state.loop.isClockwise,
      class: [{ selectable: t.state.selectable, hover: t.state.hovered, selected: t.state.selected }, "v-ng-edge"],
      config: t.state.line.stroke,
      "marker-start": t.state.sourceMarkerId ? `url('#${t.state.sourceMarkerId}')` : void 0,
      "marker-end": t.state.targetMarkerId ? `url('#${t.state.targetMarkerId}')` : void 0
    }), null, 16, ["radius", "is-large-arc", "is-clockwise", "class", "config", "marker-start", "marker-end"])) : M(e).type == "straight" || !t.state.curve ? (S(), H(lo, ne({
      key: 1,
      "data-edge-id": t.id
    }, t.state.position, {
      class: [{ selectable: t.state.selectable, hover: t.state.hovered, selected: t.state.selected }, "v-ng-edge"],
      config: t.state.line.stroke,
      "marker-start": t.state.sourceMarkerId ? `url('#${t.state.sourceMarkerId}')` : void 0,
      "marker-end": t.state.targetMarkerId ? `url('#${t.state.targetMarkerId}')` : void 0
    }), null, 16, ["data-edge-id", "class", "config", "marker-start", "marker-end"])) : (S(), H(or, {
      key: 2,
      "data-edge-id": t.id,
      class: ce([{ selectable: t.state.selectable, hover: t.state.hovered, selected: t.state.selected }, "v-ng-edge"]),
      state: t.state,
      config: t.state.line.stroke,
      "marker-start": t.state.sourceMarkerId ? `url('#${t.state.sourceMarkerId}')` : void 0,
      "marker-end": t.state.targetMarkerId ? `url('#${t.state.targetMarkerId}')` : void 0
    }, null, 8, ["data-edge-id", "class", "state", "config", "marker-start", "marker-end"]));
  }
}), sr = ["cx", "cy", "r", "fill", "stroke", "stroke-width", "stroke-dasharray"], rr = ["x", "y", "width", "height", "rx", "ry", "fill", "stroke", "stroke-width", "stroke-dasharray"], ft = /* @__PURE__ */ U({
  __name: "VShape",
  props: {
    baseX: { default: 0 },
    baseY: { default: 0 },
    config: {}
  },
  setup(t) {
    const e = t, { scale: n } = de(), o = B(e.baseX), i = B(e.baseY), s = B(0), a = B("#000000"), c = B(void 0), u = B(0), h = B(0), f = B(0), r = B(0);
    return ue(() => {
      var d;
      const l = n.value;
      s.value = e.config.strokeWidth * l, a.value = (d = e.config.strokeColor) != null ? d : "none", c.value = Ke(e.config.strokeDasharray, l), e.config.type === "circle" ? (o.value = e.baseX, i.value = e.baseY, u.value = e.config.radius * l) : (h.value = e.config.width * l, f.value = e.config.height * l, r.value = e.config.borderRadius * l, o.value = e.baseX - h.value / 2, i.value = e.baseY - f.value / 2);
    }), (l, d) => t.config.type === "circle" ? (S(), $("circle", {
      key: 0,
      class: "v-ng-shape-circle",
      cx: o.value,
      cy: i.value,
      r: u.value,
      fill: t.config.color,
      stroke: a.value,
      "stroke-width": s.value,
      "stroke-dasharray": c.value
    }, null, 8, sr)) : (S(), $("rect", {
      key: 1,
      class: "v-ng-shape-rect",
      x: o.value,
      y: i.value,
      width: h.value,
      height: f.value,
      rx: r.value,
      ry: r.value,
      fill: t.config.color,
      stroke: a.value,
      "stroke-width": s.value,
      "stroke-dasharray": c.value
    }, null, 8, rr));
  }
}), ar = ["rx", "ry", "fill", "transform"], lr = ["x", "y", "dominant-baseline", "font-family", "font-size", "fill"], cr = ["x", "dy", "dominant-baseline"], ht = /* @__PURE__ */ U({
  __name: "VLabelText",
  props: {
    text: {},
    x: { default: 0 },
    y: { default: 0 },
    dominantBaseline: { default: "central" },
    config: {}
  },
  setup(t) {
    const e = t, n = Bo(), { scale: o } = de(), i = C(() => {
      var m, g;
      return (g = (m = e.text) == null ? void 0 : m.toString().split(/\r?\n/)) != null ? g : "";
    }), s = C(() => {
      var m;
      return (m = n["font-size"]) != null ? m : e.config.fontSize * o.value;
    }), a = C(() => s.value * e.config.lineHeight), c = C(() => {
      const m = e.dominantBaseline;
      return m === "hanging" ? 0 : m === "central" ? -(a.value * (i.value.length - 1)) / 2 : -a.value * (i.value.length - 1);
    }), u = B(), h = B(""), f = Ee({ x: 0, y: 0, width: 0, height: 0 }), r = C(() => {
      var p, v;
      const m = e.config.background;
      if (!m) return f;
      let g, w;
      m.padding instanceof Object ? (g = m.padding.vertical, w = m.padding.horizontal) : (g = (p = m.padding) != null ? p : 0, w = (v = m.padding) != null ? v : 0);
      const E = a.value - s.value;
      return {
        x: f.x - w * o.value,
        y: f.y - g * o.value - E / 2,
        width: f.width + w * 2 * o.value,
        height: f.height + g * 2 * o.value + E
      };
    });
    let l;
    const d = () => {
      e.config.background && e.config.background.visible ? !l && u.value && (l = x(u.value, f, h)) : (l == null || l.disconnect(), l = void 0);
    };
    Be(() => d()), F(
      () => e.config.background && e.config.background.visible,
      (m, g) => {
        m != g && d();
      }
    ), Xe(() => {
      l == null || l.disconnect(), l = void 0;
    });
    function b(m, g, w) {
      var p;
      const E = m.getBBox();
      g.x = E.x, g.y = E.y, g.width = E.width, g.height = E.height, w.value = (p = m.getAttribute("transform")) != null ? p : void 0;
    }
    function x(m, g, w) {
      const E = new MutationObserver(() => {
        b(m, g, w);
      });
      return E.observe(m, {
        attributes: !0,
        attributeFilter: ["x", "y", "transform", "font-size"]
      }), b(m, g, w), E;
    }
    return (m, g) => {
      var w, E, p, v, y, k;
      return S(), $(X, null, [
        t.config.background && t.config.background.visible ? (S(), $("rect", ne({
          key: 0,
          class: "v-ng-text-background"
        }, r.value, {
          rx: ((E = (w = t.config.background) == null ? void 0 : w.borderRadius) != null ? E : 0) * M(o),
          ry: ((v = (p = t.config.background) == null ? void 0 : p.borderRadius) != null ? v : 0) * M(o),
          fill: (k = (y = t.config.background) == null ? void 0 : y.color) != null ? k : "#ffffff",
          transform: h.value
        }), null, 16, ar)) : ae("", !0),
        $n("text", ne({
          ref_key: "element",
          ref: u,
          class: "v-ng-text"
        }, m.$attrs, {
          x: t.x,
          y: t.y,
          "dominant-baseline": t.dominantBaseline,
          "font-family": m.$attrs["font-family"] ? `${m.$attrs["font-family"]}` : t.config.fontFamily,
          "font-size": s.value,
          fill: m.$attrs.fill ? `${m.$attrs.fill}` : t.config.color
        }), [
          i.value.length <= 1 ? (S(), $(X, { key: 0 }, [
            Ro(rn(t.text), 1)
          ], 64)) : (S(!0), $(X, { key: 1 }, Q(i.value, (O, L) => (S(), $("tspan", {
            key: L,
            x: t.x,
            dy: L == 0 ? c.value : a.value,
            "dominant-baseline": t.dominantBaseline
          }, rn(O), 9, cr))), 128))
        ], 16, lr)
      ], 64);
    };
  }
}), ur = /* @__PURE__ */ U({
  __name: "VEdgeSummarized",
  props: {
    edges: {},
    layouts: {}
  },
  setup(t) {
    const e = t, n = ct(), {
      handleEdgesPointerDownEvent: o,
      handleEdgesPointerOverEvent: i,
      handleEdgesPointerOutEvent: s,
      handleEdgesClickEvent: a,
      handleEdgesDoubleClickEvent: c,
      handleEdgesContextMenu: u
    } = Ye(), { edgeStates: h } = me(), f = B({ p1: { x: 0, y: 0 }, p2: { x: 0, y: 0 } }), r = B({ x: 0, y: 0 });
    ue(() => {
      const E = Object.keys(e.edges).find((p) => p in h);
      E && (f.value = h[E].position, r.value = {
        x: (f.value.p1.x + f.value.p2.x) / 2,
        y: (f.value.p1.y + f.value.p2.y) / 2
      });
    });
    const l = C(() => Object.keys(e.edges)), d = C(() => I.values(n.summarized.label, e.edges)), b = C(() => I.values(n.summarized.shape, e.edges)), x = C(() => I.values(n.summarized.stroke, e.edges)), m = C(() => l.value.some((E) => h[E].hovered)), g = C(() => l.value.some((E) => h[E].selectable)), w = C(() => l.value.some((E) => h[E].selected));
    return (E, p) => (S(), $("g", {
      class: ce({ "v-ng-line-summarized": !0, hovered: m.value, selectable: g.value, selected: w.value }),
      onPointerdown: p[0] || (p[0] = ge((v) => M(o)(l.value, v), ["stop"])),
      onPointerenterPassive: p[1] || (p[1] = (v) => M(i)(l.value, v)),
      onPointerleavePassive: p[2] || (p[2] = (v) => M(s)(l.value, v)),
      onClick: p[3] || (p[3] = ge((v) => M(a)(l.value, v), ["stop"])),
      onDblclick: p[4] || (p[4] = ge((v) => M(c)(l.value, v), ["stop"])),
      onContextmenu: p[5] || (p[5] = (v) => M(u)(l.value, v))
    }, [
      $e(lo, ne(f.value, {
        config: x.value,
        "data-edge-id": l.value[0]
      }), null, 16, ["config", "data-edge-id"]),
      $e(ft, {
        "base-x": r.value.x,
        "base-y": r.value.y,
        config: b.value
      }, null, 8, ["base-x", "base-y", "config"]),
      $e(ht, {
        text: Object.keys(t.edges).length.toString(),
        x: r.value.x,
        y: r.value.y,
        config: d.value,
        "text-anchor": "middle",
        "dominant-baseline": "central"
      }, null, 8, ["text", "x", "y", "config"])
    ], 34));
  }
}), dr = { class: "v-ng-edge-overlay" }, On = /* @__PURE__ */ U({
  __name: "VEdgeOverlay",
  props: {
    edgeId: { default: void 0 },
    edge: { default: void 0 },
    edges: { default: () => ({}) },
    state: {},
    isSummarized: { type: Boolean }
  },
  setup(t) {
    const e = t, { svg: n } = jt(), { scale: o } = de(), i = ct();
    function s() {
      return e.isSummarized ? I.values(i.summarized.stroke, e.edges) : e.state.line.stroke;
    }
    function a(f) {
      return { source: f.p1, target: f.p2 };
    }
    function c(f) {
      if (f.curve)
        return f.curve.center;
      {
        const r = f.origin.p1, l = f.origin.p2;
        return {
          x: (r.x + l.x) / 2,
          y: (r.y + l.y) / 2
        };
      }
    }
    function u() {
      var l;
      if (!n.value) return 0;
      const f = (l = e.edgeId) != null ? l : Object.keys(e.edges)[0], r = n.value.querySelector(`path[data-edge-id="${f}"]`);
      return r ? r.getTotalLength() : 0;
    }
    function h(f) {
      var b;
      if (!n.value || !isFinite(f)) return e.state.position.p1;
      e.state;
      const r = (b = e.edgeId) != null ? b : Object.keys(e.edges)[0], l = n.value.querySelector(`path[data-edge-id="${r}"]`);
      if (!l) return e.state.position.p1;
      const d = l.getPointAtLength(f);
      return { x: d.x, y: d.y };
    }
    return (f, r) => (S(), $("g", dr, [
      t.isSummarized ? K(f.$slots, "default", {
        key: 0,
        edges: t.edges,
        isSummarized: t.isSummarized,
        stroke: s(),
        position: a(t.state.origin),
        center: c(t.state),
        hovered: t.state.hovered,
        selected: t.state.selected,
        scale: M(o),
        length: u(),
        pointAtLength: h
      }) : K(f.$slots, "default", {
        key: 1,
        edgeId: t.edgeId,
        edge: t.edge,
        edges: { [t.edgeId]: t.edge },
        isSummarized: t.isSummarized,
        stroke: s(),
        position: a(t.state.origin),
        center: c(t.state),
        hovered: t.state.hovered,
        selected: t.state.selected,
        scale: M(o),
        length: u(),
        pointAtLength: h
      })
    ]));
  }
}), fr = /* @__PURE__ */ U({
  __name: "VEdgeGroups",
  props: {
    hasEdgeOverlaySlot: { type: Boolean }
  },
  setup(t) {
    const { edgeStates: e, edgeZOrderedList: n, layouts: o } = me();
    return (i, s) => (S(!0), $(X, null, Q(M(n), (a) => (S(), $(X, null, [
      M(Ci)(a) ? (S(), $(X, { key: 0 }, [
        (S(), H(ur, {
          key: a.key,
          edges: a.group.edges,
          layouts: M(o).nodes
        }, null, 8, ["edges", "layouts"])),
        t.hasEdgeOverlaySlot ? (S(), H(On, {
          key: a.key,
          edges: a.group.edges,
          state: M(e)[Object.keys(a.group.edges)[0]],
          "is-summarized": !0
        }, {
          default: te((c) => [
            K(i.$slots, "default", ne({ ref_for: !0 }, c))
          ]),
          _: 3
        }, 8, ["edges", "state"])) : ae("", !0)
      ], 64)) : (S(), $(X, { key: 1 }, [
        (S(), H(ir, {
          id: a.key,
          key: a.key,
          state: M(e)[a.key],
          "source-pos": M(o).nodes[a.edge.source],
          "target-pos": M(o).nodes[a.edge.target]
        }, null, 8, ["id", "state", "source-pos", "target-pos"])),
        t.hasEdgeOverlaySlot ? (S(), H(On, {
          key: a.key,
          "edge-id": a.key,
          edge: a.edge,
          state: M(e)[a.key],
          "is-summarized": !1
        }, {
          default: te((c) => [
            K(i.$slots, "default", ne({ ref_for: !0 }, c))
          ]),
          _: 3
        }, 8, ["edge-id", "edge", "state"])) : ae("", !0)
      ], 64))
    ], 64))), 256));
  }
}), hr = { class: "v-ng-layer-edges v-ng-graph-objects" }, vr = /* @__PURE__ */ U({
  __name: "VEdgesLayer",
  setup(t) {
    const e = st(), n = C(() => "edge-overlay" in e);
    return (o, i) => (S(), $("g", hr, [
      $e(Qs),
      $e(fr, { "has-edge-overlay-slot": n.value }, Dt({ _: 2 }, [
        n.value ? {
          name: "default",
          fn: te((s) => [
            K(o.$slots, "edge-overlay", Ot(zt(s)))
          ]),
          key: "0"
        } : void 0
      ]), 1032, ["has-edge-overlay-slot"])
    ]));
  }
}), gr = /* @__PURE__ */ U({
  __name: "VEdgeLabelPlace",
  props: {
    edgeId: {},
    edge: {},
    state: {}
  },
  setup(t) {
    const e = t, { scale: n } = de(), o = C(() => Fn(
      e.state.labelPosition,
      e.state.line.stroke,
      e.state.label.margin,
      e.state.label.padding,
      n.value
    ));
    return (i, s) => t.state.loop ? ae("", !0) : K(i.$slots, "default", {
      key: 0,
      edgeId: t.edgeId,
      edge: t.edge,
      config: t.state.label,
      area: o.value,
      hovered: t.state.hovered,
      selected: t.state.selected,
      scale: M(n)
    });
  }
}), pr = /* @__PURE__ */ U({
  __name: "VEdgeLabelsPlace",
  props: {
    edges: {},
    state: {},
    summarizeState: {}
  },
  setup(t) {
    const e = t, { scale: n } = de(), o = C(() => {
      var i, s;
      return Fn(
        e.state.labelPosition,
        (s = (i = e.summarizeState) == null ? void 0 : i.stroke) != null ? s : e.state.line.stroke,
        e.state.label.margin,
        e.state.label.padding,
        n.value
      );
    });
    return (i, s) => t.state.loop ? ae("", !0) : K(i.$slots, "default", {
      key: 0,
      edges: t.edges,
      config: t.state.label,
      area: o.value,
      hovered: t.state.hovered,
      selected: t.state.selected,
      scale: M(n)
    });
  }
}), mr = { class: "v-ng-edge-labels" }, br = /* @__PURE__ */ U({
  __name: "VEdgeLabels",
  props: {
    enableEdgeLabel: { type: Boolean, default: !1 },
    enableEdgesLabel: { type: Boolean, default: !1 }
  },
  setup(t) {
    const { edgeStates: e, edgeGroupStates: n, summarizedEdgeStates: o } = me(), i = C(() => {
      const a = {}, c = {};
      return Object.entries(n.edgeGroups).forEach(([u, h]) => {
        Object.keys(h.edges).length > 0 && (h.summarize ? c[u] = h : a[u] = h);
      }), { individual: a, summarized: c };
    });
    function s(a) {
      return Object.keys(a.edges)[0];
    }
    return (a, c) => (S(), $("g", mr, [
      t.enableEdgeLabel ? (S(!0), $(X, { key: 0 }, Q(i.value.individual, (u, h) => (S(), $(X, { key: h }, [
        (S(!0), $(X, null, Q(u.edges, (f, r) => (S(), H(gr, {
          key: r,
          "edge-id": r,
          edge: f,
          state: M(e)[r]
        }, {
          default: te((l) => [
            K(a.$slots, "edge-label", ne({ ref_for: !0 }, l))
          ]),
          _: 3
        }, 8, ["edge-id", "edge", "state"]))), 128))
      ], 64))), 128)) : ae("", !0),
      t.enableEdgesLabel ? (S(!0), $(X, { key: 1 }, Q(i.value.summarized, (u, h) => (S(), H(pr, {
        key: h,
        edges: u.edges,
        state: M(e)[s(u)],
        "summarize-state": M(o)[s(u)]
      }, {
        default: te((f) => [
          K(a.$slots, "edges-label", ne({ ref_for: !0 }, f))
        ]),
        _: 3
      }, 8, ["edges", "state", "summarize-state"]))), 128)) : ae("", !0)
    ]));
  }
}), yr = /* @__PURE__ */ U({
  __name: "VEdgeLabelsLayer",
  props: {
    enableEdgeLabel: { type: Boolean, default: !1 },
    enableEdgesLabel: { type: Boolean, default: !1 }
  },
  setup(t) {
    return (e, n) => (S(), H(br, {
      "enable-edge-label": t.enableEdgeLabel,
      "enable-edges-label": t.enableEdgesLabel,
      class: "v-ng-layer-edge-labels v-ng-graph-objects"
    }, Dt({ _: 2 }, [
      t.enableEdgeLabel ? {
        name: "edge-label",
        fn: te((o) => [
          K(e.$slots, "edge-label", Ot(zt(o)))
        ]),
        key: "0"
      } : void 0,
      t.enableEdgesLabel ? {
        name: "edges-label",
        fn: te((o) => [
          K(e.$slots, "edges-label", Ot(zt(o)))
        ]),
        key: "1"
      } : void 0
    ]), 1032, ["enable-edge-label", "enable-edges-label"]));
  }
}), wr = /* @__PURE__ */ U({
  __name: "VNodeFocusRing",
  props: {
    id: {},
    state: {},
    pos: { default: void 0 }
  },
  setup(t) {
    const e = t, n = C(() => {
      var a;
      return ((a = e.pos) == null ? void 0 : a.x) || 0;
    }), o = C(() => {
      var a;
      return ((a = e.pos) == null ? void 0 : a.y) || 0;
    }), i = lt(), s = Ee({});
    return ue(() => {
      var c, u, h;
      const a = e.state.shape;
      if (a.type === "circle") {
        const f = {
          type: "circle",
          radius: a.radius + ((c = a.strokeWidth) != null ? c : 0) / 2 + i.focusring.padding + i.focusring.width / 2,
          color: "none",
          strokeWidth: i.focusring.width,
          strokeColor: i.focusring.color,
          strokeDasharray: i.focusring.dasharray
        };
        Object.assign(s, f);
      } else {
        const f = {
          type: "rect",
          width: a.width + ((u = a.strokeWidth) != null ? u : 0) + i.focusring.padding * 2 + i.focusring.width,
          height: a.height + ((h = a.strokeWidth) != null ? h : 0) + i.focusring.padding * 2 + i.focusring.width,
          borderRadius: a.borderRadius > 0 ? a.borderRadius + i.focusring.padding : 0,
          color: "none",
          strokeWidth: i.focusring.width,
          strokeColor: i.focusring.color,
          strokeDasharray: i.focusring.dasharray
        };
        Object.assign(s, f);
      }
    }), (a, c) => (S(), H(ft, {
      class: "v-ng-node-focusring",
      "base-x": n.value,
      "base-y": o.value,
      config: s
    }, null, 8, ["base-x", "base-y", "config"]));
  }
}), Er = { class: "v-ng-layer-nodes-selections" }, xr = /* @__PURE__ */ U({
  __name: "VFocusringLayer",
  setup(t) {
    const { nodeStates: e } = me(), { selectedNodes: n } = ms(), o = Kt();
    return (i, s) => (S(), $("g", Er, [
      (S(!0), $(X, null, Q(M(n), (a) => (S(), H(wr, {
        id: a,
        key: a,
        state: M(e)[a],
        pos: M(o).nodes[a]
      }, null, 8, ["id", "state", "pos"]))), 128))
    ]));
  }
}), kr = ["transform"], zn = /* @__PURE__ */ U({
  __name: "VNode",
  props: {
    id: {},
    state: {},
    pos: { default: void 0 }
  },
  setup(t) {
    const e = t, n = C(() => {
      var r;
      return ((r = e.pos) == null ? void 0 : r.x) || 0;
    }), o = C(() => {
      var r;
      return ((r = e.pos) == null ? void 0 : r.y) || 0;
    }), { scale: i } = de(), {
      handleNodePointerDownEvent: s,
      handleNodePointerOverEvent: a,
      handleNodePointerOutEvent: c,
      handleNodeClickEvent: u,
      handleNodeDoubleClickEvent: h,
      handleNodeContextMenu: f
    } = Ye();
    return (r, l) => (S(), $("g", {
      class: ce({ "v-ng-node": !0, hover: t.state.hovered, selected: t.state.selected }),
      transform: `translate(${n.value} ${o.value})`,
      onPointerdown: l[0] || (l[0] = ge((d) => M(s)(t.id, d), ["stop"])),
      onPointerenterPassive: l[1] || (l[1] = (d) => M(a)(t.id, d)),
      onPointerleavePassive: l[2] || (l[2] = (d) => M(c)(t.id, d)),
      onClick: l[3] || (l[3] = ge((d) => M(u)(t.id, d), ["stop"])),
      onDblclick: l[4] || (l[4] = ge((d) => M(h)(t.id, d), ["stop"])),
      onContextmenu: l[5] || (l[5] = (d) => M(f)(t.id, d))
    }, [
      K(r.$slots, "override-node", {
        nodeId: t.id,
        scale: M(i),
        config: t.state.shape,
        class: ce({ draggable: t.state.draggable, selectable: t.state.selectable })
      }, () => [
        $e(ft, {
          config: t.state.shape,
          class: ce({
            "v-ng-node-default": !0,
            draggable: t.state.draggable,
            selectable: t.state.selectable
          })
        }, null, 8, ["config", "class"])
      ])
    ], 42, kr));
  }
}), Pr = /* @__PURE__ */ U({
  __name: "VNodesLayer",
  setup(t) {
    const e = st(), n = C(() => "override-node" in e), { nodeZOrderedList: o } = me(), i = lt(), s = Kt();
    return (a, c) => n.value ? (S(), H(Fe, {
      key: 0,
      name: M(i).transition,
      css: !!M(i).transition,
      tag: "g",
      class: "v-ng-layer-nodes v-ng-graph-objects"
    }, {
      default: te(() => [
        (S(!0), $(X, null, Q(M(o), (u) => (S(), H(zn, {
          id: u.id,
          key: u.id,
          state: u,
          pos: M(s).nodes[u.id]
        }, {
          "override-node": te((h) => [
            K(a.$slots, "override-node", ne({ ref_for: !0 }, h))
          ]),
          _: 3
        }, 8, ["id", "state", "pos"]))), 128))
      ]),
      _: 3
    }, 8, ["name", "css"])) : (S(), H(Fe, {
      key: 1,
      name: M(i).transition,
      css: !!M(i).transition,
      tag: "g",
      class: "v-ng-layer-nodes v-ng-graph-objects"
    }, {
      default: te(() => [
        (S(!0), $(X, null, Q(M(o), (u) => (S(), H(zn, {
          id: u.id,
          key: u.id,
          state: u,
          pos: M(s).nodes[u.id]
        }, null, 8, ["id", "state", "pos"]))), 128))
      ]),
      _: 1
    }, 8, ["name", "css"]));
  }
}), co = {
  [N.NORTH]: 0,
  [N.NORTH_EAST]: 1,
  [N.EAST]: 2,
  [N.SOUTH_EAST]: 3,
  [N.SOUTH]: 4,
  [N.SOUTH_WEST]: 5,
  [N.WEST]: 6,
  [N.NORTH_WEST]: 7,
  [N.CENTER]: -1
}, Sr = [
  /* N */
  (t, e) => xe(t, 0, e ? 90 : 60),
  /* NE */
  (t, e) => xe(t, 45, e ? 90 : 45),
  /* E */
  (t, e) => xe(t, 90, e ? 60 : 30),
  /* SE */
  (t, e) => xe(t, 135, e ? 90 : 45),
  /* S */
  (t, e) => xe(t, 180, e ? 90 : 60),
  /* SW */
  (t, e) => xe(t, 225, e ? 90 : 45),
  /* W */
  (t, e) => xe(t, 270, e ? 60 : 30),
  /* NW */
  (t, e) => xe(t, 315, e ? 90 : 45)
];
function Mr(t, e, n, o, i) {
  if (i === N.CENTER)
    return N.CENTER;
  const s = [];
  Object.entries(n).forEach(([h, f]) => {
    let r = !1;
    if (f.nodeId === t) {
      const d = o(h);
      d && (r = !0, f = Re(oe({}, f), {
        pos: { x: d.x, y: d.y }
      }));
    }
    const l = (Ut(Bt(f.pos, e)) + 360 + 90) % 360;
    s.push([l, r]);
  });
  const a = Cr(i), u = [
    a,
    (a + 4) % 8,
    // priority is given to diagonals
    (a + 2) % 8,
    (a - 2 + 8) % 8,
    (a + 1) % 8,
    (a - 1 + 8) % 8,
    (a + 3) % 8,
    (a - 3 + 8) % 8
  ].find((h) => s.every((f) => !Sr[h](...f)));
  return u === void 0 ? i : Or(u, i);
}
function xe(t, e, n) {
  t %= 360;
  const o = (e - n + 360) % 360, i = (e + n) % 360;
  return o <= i ? o < t && t < i : o < t || t < i;
}
function Cr(t) {
  var e;
  return (e = co[t]) != null ? e : 0;
}
function Or(t, e) {
  var n, o;
  return (o = (n = Object.entries(co)[t]) == null ? void 0 : n[0]) != null ? o : e;
}
const zr = ["transform"], Tn = /* @__PURE__ */ U({
  __name: "VNodeLabel",
  props: {
    id: {},
    state: {},
    pos: { default: void 0 }
  },
  setup(t) {
    const e = t, n = lt(), { edgeStates: o } = me(), { scale: i } = de(), {
      handleNodePointerDownEvent: s,
      handleNodePointerOverEvent: a,
      handleNodePointerOutEvent: c,
      handleNodeClickEvent: u,
      handleNodeDoubleClickEvent: h,
      handleNodeContextMenu: f
    } = Ye(), r = C(() => {
      var T;
      return ((T = e.pos) == null ? void 0 : T.x) || 0;
    }), l = C(() => {
      var T;
      return ((T = e.pos) == null ? void 0 : T.y) || 0;
    }), d = C(() => e.state.label.direction === N.CENTER ? 0 : e.state.label.margin * i.value), b = B(0), x = B(0), m = B(0), g = B(0), w = C(() => {
      var j;
      const T = e.state.label.direction, z = e.state.label.directionAutoAdjustment;
      if (z === !1)
        return T;
      const A = { x: r.value, y: l.value };
      return z === !0 ? Mr(
        e.state.id,
        A,
        e.state.oppositeNodes,
        (G) => {
          var R, _;
          return (_ = (R = o[G]) == null ? void 0 : R.loop) == null ? void 0 : _.center;
        },
        T
      ) : (j = z({
        nodeId: e.state.id,
        pos: A,
        oppositeNodes: e.state.oppositeNodes
      })) != null ? j : T;
    }), E = C(() => {
      switch (w.value) {
        case N.CENTER:
        case N.NORTH:
        case N.SOUTH:
          return "middle";
        case N.EAST:
        case N.NORTH_EAST:
        case N.SOUTH_EAST:
          return "start";
        case N.WEST:
        case N.NORTH_WEST:
        case N.SOUTH_WEST:
        default:
          return "end";
      }
    }), p = C(() => {
      switch (w.value) {
        case N.NORTH:
        case N.NORTH_EAST:
        case N.NORTH_WEST:
          return "text-top";
        case N.SOUTH:
        case N.SOUTH_EAST:
        case N.SOUTH_WEST:
          return "hanging";
        case N.CENTER:
        case N.EAST:
        case N.WEST:
        default:
          return "central";
      }
    }), v = C(() => {
      switch (w.value) {
        case N.CENTER:
        case N.NORTH:
        case N.SOUTH:
          return 0;
        case N.EAST:
          return x.value;
        case N.WEST:
          return -x.value;
        case N.NORTH_EAST:
        case N.SOUTH_EAST:
          return g.value;
        case N.NORTH_WEST:
        case N.SOUTH_WEST:
        default:
          return -g.value;
      }
    }), y = C(() => {
      switch (w.value) {
        case N.NORTH:
          return -b.value;
        case N.SOUTH:
          return b.value;
        case N.CENTER:
        case N.EAST:
        case N.WEST:
          return 0;
        case N.NORTH_EAST:
        case N.NORTH_WEST:
          return -m.value;
        case N.SOUTH_EAST:
        case N.SOUTH_WEST:
        default:
          return m.value;
      }
    });
    ue(() => {
      const T = i.value, z = e.state.shape;
      if (z.type == "circle") {
        const A = z.radius * T, j = A + d.value, G = Math.sqrt(ie(j, 2) / 2);
        b.value = A + d.value, x.value = A + d.value, m.value = G, g.value = G;
      } else {
        const A = z.borderRadius * T, j = z.width * T, G = z.height * T, R = A + d.value, _ = Math.sqrt(ie(R, 2) / 2);
        b.value = G / 2 + d.value, x.value = j / 2 + d.value, m.value = G / 2 - A + _, g.value = j / 2 - A + _;
      }
    });
    const k = C(() => (T) => n.label.handleNodeEvents ? {
      pointerdown: (z) => {
        z.stopPropagation(), s(T, z);
      },
      pointerenter: (z) => a(T, z),
      pointerleave: (z) => c(T, z),
      click: (z) => {
        z.stopPropagation(), u(T, z);
      },
      dblclick: (z) => {
        z.stopPropagation(), h(T, z);
      },
      contextmenu: (z) => {
        f(T, z);
      }
    } : {}), O = C(() => {
      const T = n.label.handleNodeEvents;
      return {
        "v-ng-node-label": !0,
        hover: T && e.state.hovered,
        selected: T && e.state.selected
      };
    }), L = C(() => {
      const T = n.label.handleNodeEvents;
      return {
        draggable: T && e.state.draggable,
        selectable: T && e.state.selectable
      };
    });
    return (T, z) => (S(), $("g", ne({
      class: O.value,
      transform: `translate(${r.value} ${l.value})`
    }, Vo(k.value(t.id), !0)), [
      K(T.$slots, "override-node-label", {
        nodeId: t.id,
        scale: M(i),
        text: t.state.labelText,
        x: v.value,
        y: y.value,
        config: t.state.label,
        shape: t.state.shape,
        textAnchor: E.value,
        dominantBaseline: p.value,
        class: ce(L.value)
      }, () => [
        $e(ht, {
          text: t.state.labelText,
          x: 0,
          y: 0,
          config: t.state.label,
          "text-anchor": E.value,
          "dominant-baseline": p.value,
          class: ce(L.value),
          transform: `translate(${v.value} ${y.value})`
        }, null, 8, ["text", "config", "text-anchor", "dominant-baseline", "class", "transform"])
      ])
    ], 16, zr));
  }
}), Tr = /* @__PURE__ */ U({
  __name: "VNodeLabelsLayer",
  setup(t) {
    const e = st(), n = C(() => "override-node-label" in e), { nodeZOrderedList: o } = me(), i = lt(), s = Kt(), a = C(() => c(o.value));
    function c(u) {
      return u.filter((h) => {
        var f;
        return h.label.visible && ((f = h.labelText) != null ? f : !1);
      });
    }
    return (u, h) => n.value ? (S(), H(Fe, {
      key: 0,
      name: M(i).transition,
      css: !!M(i).transition,
      tag: "g",
      class: "v-ng-layer-node-labels v-ng-graph-objects"
    }, {
      default: te(() => [
        (S(!0), $(X, null, Q(a.value, (f) => (S(), H(Tn, {
          id: f.id,
          key: f.id,
          state: f,
          pos: M(s).nodes[f.id]
        }, {
          "override-node-label": te((r) => [
            K(u.$slots, "override-node-label", ne({ ref_for: !0 }, r))
          ]),
          _: 3
        }, 8, ["id", "state", "pos"]))), 128))
      ]),
      _: 3
    }, 8, ["name", "css"])) : (S(), H(Fe, {
      key: 1,
      name: M(i).transition,
      css: !!M(i).transition,
      tag: "g",
      class: "v-ng-layer-node-labels v-ng-graph-objects"
    }, {
      default: te(() => [
        (S(!0), $(X, null, Q(a.value, (f) => (S(), H(Tn, {
          id: f.id,
          key: f.id,
          state: f,
          pos: M(s).nodes[f.id]
        }, null, 8, ["id", "state", "pos"]))), 128))
      ]),
      _: 1
    }, 8, ["name", "css"]));
  }
}), Ir = ["d", "stroke", "stroke-width", "stroke-dasharray", "stroke-linecap", "stroke-linejoin"], $r = /* @__PURE__ */ U({
  __name: "VPath",
  props: {
    path: {}
  },
  setup(t) {
    const e = t, { nodeStates: n, edgeStates: o, layouts: i } = me(), { scale: s } = de(), a = jn();
    function c(l) {
      if (l.edges.length === 0) return [];
      const d = I.value(a.margin, l.path) * s.value;
      return gi(
        l,
        n,
        i.nodes,
        o,
        s.value,
        a.curveInNode,
        a.end,
        d
      );
    }
    function u(l) {
      const d = c(l);
      let b = !0;
      return d.map((x) => {
        if (x === null)
          b = !0;
        else {
          if (typeof x == "string")
            return x;
          if (x instanceof Array) {
            x = [...x];
            const m = [];
            if (x.length % 2 === 1) {
              const g = x[0];
              x = x.slice(1), m.push(`L ${g.x} ${g.y}`);
            }
            return Nt(x, 2).map(([g, w]) => m.push(`Q ${g.x} ${g.y} ${w.x} ${w.y}`)), m.join(" ");
          } else {
            const m = b;
            return b = !1, `${m ? "M " : "L "}${x.x} ${x.y}`;
          }
        }
      }).join(" ");
    }
    const h = C(() => {
      const l = e.path;
      return l.selected ? I.values(a.selected, l.path) : l.hovered && a.hover ? I.values(a.hover, l.path) : I.values(a.normal, l.path);
    }), f = C(() => Ke(h.value.dasharray, s.value)), r = C(() => {
      const l = h.value.animate ? rt(h.value.dasharray) * h.value.animationSpeed * s.value : !1;
      return l ? { "--animation-speed": l } : void 0;
    });
    return (l, d) => (S(), $("path", {
      class: ce({
        "v-ng-path": !0,
        animate: h.value.animate,
        clickable: t.path.clickable,
        hoverable: t.path.hoverable
      }),
      d: u(t.path),
      fill: "none",
      stroke: h.value.color,
      "stroke-width": h.value.width * M(s),
      "stroke-dasharray": f.value,
      "stroke-linecap": h.value.linecap,
      "stroke-linejoin": h.value.linejoin,
      style: Ge(r.value)
    }, null, 14, Ir));
  }
}), Lr = /* @__PURE__ */ U({
  __name: "VPaths",
  setup(t) {
    const { pathZOrderedList: e } = me(), n = jn(), {
      handlePathPointerDownEvent: o,
      handlePathPointerOverEvent: i,
      handlePathPointerOutEvent: s,
      handlePathClickEvent: a,
      handlePathDoubleClickEvent: c,
      handlePathContextMenu: u
    } = Ye();
    return (h, f) => (S(), H(Fe, {
      name: M(n).transition,
      css: !!M(n).transition,
      tag: "g",
      class: "v-ng-paths v-ng-layer-paths v-ng-graph-objects"
    }, {
      default: te(() => [
        (S(!0), $(X, null, Q(M(e), (r) => (S(), H($r, {
          key: r.id,
          path: r,
          onPointerdown: (l) => M(o)(r.id, l),
          onPointerenterPassive: (l) => M(i)(r.id, l),
          onPointerleavePassive: (l) => M(s)(r.id, l),
          onClick: ge((l) => M(a)(r.id, l), ["stop"]),
          onDblclick: ge((l) => M(c)(r.id, l), ["stop"]),
          onContextmenu: (l) => M(u)(r.id, l)
        }, null, 8, ["path", "onPointerdown", "onPointerenterPassive", "onPointerleavePassive", "onClick", "onDblclick", "onContextmenu"]))), 128))
      ]),
      _: 1
    }, 8, ["name", "css"]));
  }
}), Ar = /* @__PURE__ */ U({
  __name: "VPathsLayer",
  setup(t) {
    return (e, n) => (S(), H(Lr));
  }
}), Zr = { key: 0 }, Dr = /* @__PURE__ */ U({
  __name: "VNetworkGraph",
  props: {
    nodes: { default: () => ({}) },
    edges: { default: () => ({}) },
    paths: { default: () => ({}) },
    layouts: { default: () => ({}) },
    zoomLevel: { default: 1 },
    selectedNodes: { default: () => [] },
    selectedEdges: { default: () => [] },
    selectedPaths: { default: () => [] },
    configs: { default: () => ({}) },
    layers: { default: () => ({}) },
    eventHandlers: { default: () => ({}) }
  },
  emits: ["update:zoomLevel", "update:selectedNodes", "update:selectedEdges", "update:selectedPaths", "update:layouts"],
  setup(t, { expose: e, emit: n }) {
    const o = [
      "override-node",
      "override-node-label",
      "edge-overlay",
      "edge-label",
      "edges-label"
    ], i = t, s = n, a = st(), c = we(i, "nodes"), u = we(i, "edges"), { objects: h, isInCompatibilityModeForPath: f } = vs(
      we(i, "paths")
    ), r = Qi();
    Object.entries(i.eventHandlers).forEach(([P, D]) => {
      r.on(P, D);
    });
    const l = Ko(we(i, "configs")), d = C(() => {
      const P = new Set(Object.keys(a));
      o.forEach((Z) => P.delete(Z));
      const D = Object.fromEntries(Fo.map((Z) => [Z, []]));
      return Object.assign(
        D,
        Object.entries(i.layers).reduce(
          (Z, [W, ee]) => (P.delete(W), ee in Z ? Z[ee].push(W) : Z[ee] = [W], Z),
          {}
        )
      ), D.root.push(...P), D;
    }), b = C(() => l.view.grid.visible), x = C(() => {
      const P = d.value;
      return b.value || P.background.length > 0 || P.grid.length > 0;
    }), m = Es(l, a), g = B(), w = B(), E = B(), p = B(
      0
      /* INITIAL */
    ), v = C(
      () => p.value !== 0
      /* INITIAL */
    ), y = gs(i, "zoomLevel", s, (P) => (P = Math.max(P, l.view.minZoomLevel), P = Math.min(P, l.view.maxZoomLevel), P)), { svgPanZoom: k, onSvgPanZoomMounted: O, onSvgPanZoomUnmounted: L } = us(w, {
      viewportSelector: ".v-ng-viewport",
      minZoom: l.view.minZoomLevel,
      // temporary
      maxZoom: l.view.maxZoomLevel,
      // temporary
      dblClickZoomEnabled: tn(l.view),
      mouseWheelZoomEnabled: pt(l.view),
      fit: l.view.autoPanAndZoomOnLoad === "fit-content",
      center: l.view.autoPanAndZoomOnLoad !== !1,
      zoomEnabled: l.view.zoomEnabled,
      preventMouseEventsDefault: !1,
      onZoom: (P) => {
        var Z, W;
        if (p.value === 2) return;
        const D = (W = (Z = k.value) == null ? void 0 : Z.getRealZoom()) != null ? W : 1;
        Math.abs(y.value - D) >= 1e-6 && (y.value = D, r.emit("view:zoom", D));
      },
      panEnabled: l.view.panEnabled,
      onPan: (P) => {
        p.value !== 2 && r.emit("view:pan", P);
      }
    });
    qo({ container: g, svg: w, viewport: E, svgPanZoom: k });
    const T = { width: 0, height: 0 }, z = globalThis.ResizeObserver ? new ResizeObserver(() => {
      var D, Z, W;
      if ((D = k.value) == null || D.resize(), !l.view.autoPanOnResize) return;
      const P = (Z = g.value) == null ? void 0 : Z.getBoundingClientRect();
      if (P) {
        const ee = -(T.width - P.width) / 2, Te = -(T.height - P.height) / 2;
        (W = k.value) == null || W.panBy({ x: ee, y: Te });
        const { width: ye, height: mt } = P;
        (T.width !== ye || T.height !== mt) && (Object.assign(T, { width: ye, height: mt }), r.emit("view:resize", { x: P.x, y: P.y, width: ye, height: mt }));
      }
    }) : void 0;
    O(() => {
      var ee, Te, ye;
      const P = q(g.value, "svg-pan-zoom container");
      z == null || z.observe(P), (Te = (ee = l.view).onSvgPanZoomInitialized) == null || Te.call(ee, q(k.value, "svg-pan-zoom instance"));
      const D = P.getBoundingClientRect(), { width: Z, height: W } = D;
      Object.assign(T, { width: Z, height: W }), (ye = E.value) == null || ye.addEventListener("touchstart", nn, { passive: !1 });
    }), L(() => {
      var P;
      z == null || z.disconnect(), (P = E.value) == null || P.removeEventListener("touchstart", nn);
    });
    const A = (P) => {
      var D;
      (D = k.value) == null || D.applyAbsoluteZoomLevel(
        P,
        l.view.minZoomLevel,
        l.view.maxZoomLevel
      );
    };
    F(
      () => l.view.panEnabled,
      (P) => {
        var D;
        (D = k.value) == null || D.setPanEnabled(P);
      }
    ), F(
      () => [
        l.view.zoomEnabled,
        tn(l.view),
        pt(l.view)
      ],
      () => {
        const P = k.value;
        P && Io(
          P,
          l.view.zoomEnabled,
          l.view.doubleClickZoomEnabled,
          l.view.mouseWheelZoomEnabled
        );
      }
    ), F(y, (P) => A(P)), F(
      () => [l.view.minZoomLevel, l.view.maxZoomLevel],
      (P) => {
        A(y.value);
      }
    );
    const { scale: j } = ds(y, l.view);
    O(() => {
      const P = i.zoomLevel;
      A(P);
    });
    const G = () => ve(null, null, function* () {
      var P;
      Object.keys(i.nodes).length > 0 && ((P = k.value) == null || P.updateBBox(), yield Cn());
    }), R = (P) => ve(null, null, function* () {
      var Te, ye;
      const D = !P || P.margin === void 0 ? l.view.fitContentMargin : P.margin;
      yield G();
      const Z = q(w.value).getBoundingClientRect(), W = Os(D, Z), ee = zs(
        q(E.value),
        Z,
        be.nodes,
        y.value,
        W,
        l.view.scalingObjects
      );
      ee ? (A(ee.zoom), (Te = k.value) == null || Te.pan(ee.pos)) : (ye = k.value) == null || ye.center(), r.emit("view:fit", void 0);
    }), _ = () => ve(null, null, function* () {
      var P;
      yield G(), (P = k.value) == null || P.center();
    }), fe = () => {
      var P, D;
      return (D = (P = k.value) == null ? void 0 : P.getViewBox()) != null ? D : {
        top: 0,
        bottom: 0,
        left: 0,
        right: 0
      };
    }, se = (P) => {
      var D;
      return (D = k.value) == null ? void 0 : D.setViewBox(P);
    }, pe = Ct(i, "selectedNodes", c, s);
    F(pe, (P) => r.emit("node:select", Array.from(P)));
    const Oe = Ct(i, "selectedEdges", u, s);
    F(Oe, (P) => r.emit("edge:select", Array.from(P)));
    const ze = Ct(i, "selectedPaths", h, s);
    F(ze, (P) => r.emit("path:select", Array.from(P))), ps(pe, Oe, ze);
    const re = ke(/* @__PURE__ */ new Set()), Le = ke(/* @__PURE__ */ new Set()), Ae = ke(/* @__PURE__ */ new Set()), be = ke({ nodes: {} });
    bs(be), F(
      () => i.layouts,
      () => {
        var P;
        return Object.assign(be.nodes, (P = i.layouts.nodes) != null ? P : {});
      },
      { deep: !0, immediate: !0 }
    ), F(be, () => s("update:layouts", be), { deep: !0 });
    const vt = Ei(), gt = B(!1);
    r.on("node:dragstart", (P) => gt.value = !0), r.on("node:dragend", (P) => gt.value = !1), r.on("view:mode", (P) => {
      var D, Z, W, ee;
      l.view.panEnabled && (P === "default" ? (D = k.value) == null || D.enablePan() : (Z = k.value) == null || Z.disablePan()), l.view.zoomEnabled && (P === "default" ? (W = k.value) == null || W.enableZoom() : (ee = k.value) == null || ee.disableZoom());
    });
    const uo = C(() => l.view.panEnabled || l.view.zoomEnabled || l.node.draggable), { nodeStates: fo, edgeStates: ho, pathStates: vo } = Mi(
      wt(c, pe, re),
      wt(u, Oe, Le),
      wt(h, ze, Ae),
      Ve(l),
      be,
      i.layouts,
      vt,
      j
    ), go = C(() => pt(l.view)), { isBoxSelectionMode: po, selectionBox: Yt, startBoxSelection: mo, stopBoxSelection: bo } = Ki(
      w,
      Ve(be),
      Ve(y),
      fo,
      ho,
      vo,
      pe,
      Oe,
      ze,
      re,
      Le,
      Ae,
      f,
      go,
      l,
      r
    ), Qt = () => ({
      layouts: ke(be.nodes),
      // deprecated parameter.
      nodePositions: we(be, "nodes"),
      nodes: c,
      edges: u,
      configs: Ve(l),
      scale: Ve(j),
      emitter: r,
      svgPanZoom: q(k.value)
    });
    F(
      () => l.view.layoutHandler,
      (P, D) => {
        D.deactivate(), P.activate(Qt());
      }
    );
    const { transitionWhile: yo, transitionOption: Jt } = hs(), wo = C(() => {
      const P = Jt.value;
      return P.enabled ? {
        "--transition-duration": P.duration + "ms",
        "--transition-function": P.timingFunction
      } : {};
    });
    O(() => ve(null, null, function* () {
      if (l.view.onBeforeInitialDisplay) {
        const Z = l.view.onBeforeInitialDisplay();
        Jo(Z) && (yield Z);
      }
      const P = q(k.value, "svg-pan-zoom");
      l.view.layoutHandler.activate(Qt()), yield Cn();
      const D = l.view.autoPanAndZoomOnLoad;
      if (l.view.fit || D !== !1) {
        const Z = Object.keys(i.nodes).length == 0, W = P.getPan();
        if (Z || D === "center-zero") {
          yield G();
          const ee = P.getSizes();
          P.pan({
            x: ee.width / 2,
            y: ee.height / 2
          });
        } else D === "fit-content" || l.view.fit ? yield R() : D === "center-content" ? yield _() : yield G();
        Zt(() => {
          const ee = P.getPan();
          W.x === ee.x && W.y === ee.y && r.emit("view:pan", ee);
        });
      } else
        yield G();
      r.emit("view:load"), p.value = 1;
    })), L(() => {
      p.value = 2, r.emit("view:unload"), l.view.layoutHandler.deactivate();
    });
    function Eo() {
      var P;
      (P = k.value) == null || P.zoomIn();
    }
    function xo() {
      var P;
      (P = k.value) == null || P.zoomOut();
    }
    function ko(P) {
      var D;
      (D = k.value) == null || D.pan(P);
    }
    function Po(P) {
      var D;
      (D = k.value) == null || D.panBy(P);
    }
    function So() {
      return q(k.value).getPan();
    }
    function Mo() {
      const P = q(k.value).getSizes();
      return {
        width: P.width,
        height: P.height,
        viewBox: P.viewBox
      };
    }
    function Co(P) {
      return At(
        q(w.value, "svg"),
        q(E.value, "viewport"),
        P
      );
    }
    function Oo(P) {
      return Hi(
        q(w.value, "svg"),
        q(E.value, "viewport"),
        P
      );
    }
    function zo() {
      return Qn(
        q(w.value, "svg"),
        q(E.value, "viewport"),
        j.value
      ).outerHTML;
    }
    function To() {
      return ve(this, arguments, function* (P = {}) {
        return (yield en(P)).outerHTML;
      });
    }
    function en() {
      return ve(this, arguments, function* (P = {}) {
        return Ui(
          q(w.value, "svg"),
          q(E.value, "viewport"),
          j.value,
          P
        );
      });
    }
    e({
      // methods
      fitToContents: R,
      panToCenter: _,
      getViewBox: fe,
      setViewBox: se,
      transitionWhile: yo,
      startBoxSelection: mo,
      stopBoxSelection: bo,
      zoomIn: Eo,
      zoomOut: xo,
      panTo: ko,
      panBy: Po,
      getPan: So,
      getSizes: Mo,
      translateFromDomToSvgCoordinates: Co,
      translateFromSvgToDomCoordinates: Oo,
      getAsSvg: zo,
      exportAsSvgText: To,
      exportAsSvgElement: en
    });
    function Io(P, D, Z, W) {
      P.setZoomEnabled(D), D && Z ? P.enableDblClickZoom() : P.disableDblClickZoom(), D && W ? P.enableMouseWheelZoom() : P.disableMouseWheelZoom();
    }
    function tn(P) {
      return P.zoomEnabled && P.doubleClickZoomEnabled;
    }
    function pt(P) {
      return P.zoomEnabled && P.mouseWheelZoomEnabled;
    }
    function nn(P) {
      P.stopPropagation();
    }
    return (P, D) => (S(), $("div", {
      ref_key: "container",
      ref: g,
      class: "v-network-graph v-ng-container"
    }, [
      (S(), $("svg", {
        ref_key: "svg",
        ref: w,
        class: ce(["v-ng-canvas", { show: v.value, dragging: gt.value, touches: uo.value, "box-selection-mode": M(po) }]),
        width: "100%",
        height: "100%"
      }, [
        (S(!0), $(X, null, Q(d.value.root, (Z) => K(P.$slots, Z, {
          key: Z,
          scale: M(j)
        })), 128)),
        Object.keys(M(vt).markers).length > 0 ? (S(), $("defs", Zr, [
          (S(!0), $(X, null, Q(M(vt).markers, (Z, W) => (S(), H(Ws, {
            id: W,
            key: W,
            marker: Z,
            scale: M(j)
          }, null, 8, ["id", "marker", "scale"]))), 128))
        ])) : ae("", !0),
        x.value ? (S(), H(Gs, { key: 1 }, {
          default: te(() => [
            (S(!0), $(X, null, Q(d.value.background, (Z) => (S(), $("g", {
              key: Z,
              class: "v-ng-layer"
            }, [
              K(P.$slots, Z, { scale: M(j) })
            ]))), 128)),
            b.value ? (S(), H(Xs, { key: 0 })) : ae("", !0),
            (S(!0), $(X, null, Q(d.value.grid, (Z) => (S(), $("g", {
              key: Z,
              class: "v-ng-layer"
            }, [
              K(P.$slots, Z, { scale: M(j) })
            ]))), 128))
          ]),
          _: 3
        })) : ae("", !0),
        $n("g", {
          ref_key: "viewport",
          ref: E,
          class: ce(["v-ng-viewport", { "v-ng-transition": M(Jt).enabled }]),
          style: Ge(wo.value)
        }, [
          (S(!0), $(X, null, Q(d.value.base, (Z) => (S(), $("g", {
            key: Z,
            class: "v-ng-layer"
          }, [
            K(P.$slots, Z, { scale: M(j) })
          ]))), 128)),
          (S(!0), $(X, null, Q(M(m), (Z) => (S(), $(X, { key: Z }, [
            Z === "edges" ? (S(), H(vr, { key: 0 }, Dt({ _: 2 }, [
              "edge-overlay" in a ? {
                name: "edge-overlay",
                fn: te((W) => [
                  K(P.$slots, "edge-overlay", ne({ ref_for: !0 }, W))
                ]),
                key: "0"
              } : void 0
            ]), 1024)) : Z === "edge-labels" ? (S(), H(yr, {
              key: 1,
              "enable-edge-label": "edge-label" in a,
              "enable-edges-label": "edges-label" in a
            }, {
              "edge-label": te((W) => [
                K(P.$slots, "edge-label", ne({ ref_for: !0 }, W))
              ]),
              "edges-label": te((W) => [
                K(P.$slots, "edges-label", ne({ ref_for: !0 }, W))
              ]),
              _: 3
            }, 8, ["enable-edge-label", "enable-edges-label"])) : Z === "focusring" ? (S(), H(xr, { key: 2 })) : Z === "nodes" ? (S(), H(Pr, { key: 3 }, {
              "override-node": te((W) => [
                K(P.$slots, "override-node", ne({ ref_for: !0 }, W))
              ]),
              _: 3
            })) : Z === "node-labels" ? (S(), H(Tr, { key: 4 }, {
              "override-node-label": te((W) => [
                K(P.$slots, "override-node-label", ne({ ref_for: !0 }, W))
              ]),
              _: 3
            })) : Z === "paths" ? (S(), H(Ar, { key: 5 })) : ae("", !0),
            (S(!0), $(X, null, Q(d.value[Z], (W) => (S(), $("g", {
              key: W,
              class: "v-ng-layer"
            }, [
              K(P.$slots, W, { scale: M(j) })
            ]))), 128))
          ], 64))), 128))
        ], 6),
        M(Yt) ? (S(), H(As, {
          key: 2,
          box: M(Yt),
          config: M(l).view.selection.box
        }, null, 8, ["box", "config"])) : ae("", !0)
      ], 2))
    ], 512));
  }
}), Nr = (t, e) => {
  const n = t.__vccOpts || t;
  for (const [o, i] of e)
    n[o] = i;
  return n;
}, jr = {};
function Br(t, e) {
  return S(), $("defs", null, [
    (S(), H(In("style"), null, {
      default: te(() => [
        K(t.$slots, "default")
      ]),
      _: 3
    }))
  ]);
}
const Rr = /* @__PURE__ */ Nr(jr, [["render", Br]]), Vr = /* @__PURE__ */ U({
  __name: "VEdgeLabel",
  props: {
    area: {},
    config: {},
    text: { default: "" },
    align: { default: "center" },
    verticalAlign: { default: "center" },
    edge: { default: void 0 },
    hovered: { type: Boolean, default: void 0 },
    selected: { type: Boolean, default: void 0 },
    scale: { default: void 0 }
  },
  setup(t) {
    const e = t, n = B(0), o = B(0), i = B("middle"), s = B("central"), a = B(0);
    ue(() => {
      const u = e.area.source, h = e.area.target;
      e.align === "source" ? (u.above.x == h.above.x ? i.value = u.above.y > h.above.y ? "start" : "end" : i.value = u.above.x < h.above.x ? "start" : "end", e.verticalAlign === "above" ? (n.value = u.above.x, o.value = u.above.y, s.value = "text-top") : e.verticalAlign === "below" ? (n.value = u.below.x, o.value = u.below.y, s.value = "hanging") : (n.value = (u.above.x + u.below.x) / 2, o.value = (u.above.y + u.below.y) / 2, s.value = "central")) : e.align === "target" ? (u.above.x == h.above.x ? i.value = u.above.y < h.above.y ? "start" : "end" : i.value = u.above.x > h.above.x ? "start" : "end", e.verticalAlign === "above" ? (n.value = h.above.x, o.value = h.above.y, s.value = "text-top") : e.verticalAlign === "below" ? (n.value = h.below.x, o.value = h.below.y, s.value = "hanging") : (n.value = (h.above.x + h.below.x) / 2, o.value = (h.above.y + h.below.y) / 2, s.value = "central")) : (i.value = "middle", e.verticalAlign === "above" ? (n.value = (u.above.x + h.above.x) / 2, o.value = (u.above.y + h.above.y) / 2, s.value = "text-top") : e.verticalAlign === "below" ? (n.value = (u.below.x + h.below.x) / 2, o.value = (u.below.y + h.below.y) / 2, s.value = "hanging") : (n.value = (u.above.x + h.below.x) / 2, o.value = (u.above.y + h.below.y) / 2, s.value = "central"));
      let f = Y.fromPositions(u.above, h.above).v.angleDegree();
      (f < -90 || f >= 90) && (f = f + 180, f > 180 && (f -= 360)), a.value = f;
    });
    const c = C(() => s.value === "central" && !e.config.background ? Re(oe({}, e.config), {
      background: {
        visible: !0,
        color: "#ffffff",
        padding: {
          vertical: 1,
          horizontal: 4
        },
        borderRadius: 2
      }
    }) : e.config);
    return (u, h) => (S(), H(ht, {
      class: "v-ng-edge-label",
      text: t.text,
      x: n.value,
      y: o.value,
      config: c.value,
      "text-anchor": i.value,
      "dominant-baseline": s.value,
      transform: `rotate(${a.value} ${n.value} ${o.value})`
    }, null, 8, ["text", "x", "y", "config", "text-anchor", "dominant-baseline", "transform"]));
  }
}), Wr = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  VEdgeLabel: Vr,
  VLabelText: ht,
  VNetworkGraph: Dr,
  VShape: ft,
  VStyle: Rr
}, Symbol.toStringTag, { value: "Module" })), Hr = 10;
class Kr extends Zn {
  constructor(e = {}) {
    super(), this.options = e;
  }
  setNodePosition(e, n) {
    const o = this.options.grid || Hr;
    e.value.x = Math.floor(n.x / o) * o, e.value.y = Math.floor(n.y / o) * o;
  }
}
const Yr = function(t) {
  Object.entries(Wr).forEach(([e, n]) => {
    t.component(e, n);
  });
};
export {
  I as Config,
  Kr as GridLayout,
  Fo as LayerPositions,
  N as NodeLabelDirection,
  Zn as SimpleLayout,
  Vr as VEdgeLabel,
  ht as VLabelText,
  Dr as VNetworkGraph,
  ft as VShape,
  Rr as VStyle,
  V as Vector2D,
  qr as configsWithType,
  Yr as default,
  Xr as defineConfigs,
  Gr as getFullConfigs,
  me as useStates,
  bt as withSelf
};
//# sourceMappingURL=index.js.map
