import { ComputedRef, Slot } from 'vue';
import { Configs } from '../common/configs';
import { LayerName } from '../common/types';
export interface LayerSlotProps {
    scale: number;
}
export type LayerSlots = Record<string, Slot<LayerSlotProps>>;
export declare function useBuiltInLayerOrder<T extends Configs>(configs: T, slots: Readonly<LayerSlots>): ComputedRef<LayerName[]>;
